<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.asset.dal.mysql.customerreference.AssetProjectCustomerReferenceMapper">
    <!-- 历史引用也必须保留；不能通过软删除记录绕过客户更正边界。 -->
    <select id="countReferences" resultType="long">
        SELECT (SELECT COUNT(*) FROM (SELECT sn FROM ast_device WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} UNION SELECT device_sn FROM ast_device_project_relationship WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId}) device_references)
    </select>
</mapper>
