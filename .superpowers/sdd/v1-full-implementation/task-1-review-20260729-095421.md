# Review package: T-CP-008

Base: 38c5018
Head: b12767283ee1f1f5ece509cb811091d3101b1f37

## Commits
```text
b127672 feat(pms): 创建资产及V2业务模块骨架
```

## Diff stat
```text
 docker/backend/Dockerfile                          |  4 ++
 pms-module-analytics/pom.xml                       | 14 +++++
 .../yudao/module/pms/analytics/package-info.java   |  5 ++
 pms-module-asset/pom.xml                           | 14 +++++
 .../yudao/module/pms/asset/package-info.java       |  4 ++
 pms-module-integration/pom.xml                     | 14 +++++
 .../yudao/module/pms/integration/package-info.java |  5 ++
 pms-module-outsourcing/pom.xml                     | 14 +++++
 .../yudao/module/pms/outsourcing/package-info.java |  4 ++
 pom.xml                                            |  4 ++
 tasks/todo.md                                      |  6 +-
 .../verify-pms-module-boundaries.ps1               | 64 ++++++++++++++++++++++
 yudao-server/pom.xml                               | 20 +++++++
 13 files changed, 169 insertions(+), 3 deletions(-)
```

## Full diff
```diff
diff --git a/docker/backend/Dockerfile b/docker/backend/Dockerfile
index bf29ede..0f89bef 100644
--- a/docker/backend/Dockerfile
+++ b/docker/backend/Dockerfile
@@ -5,20 +5,24 @@ WORKDIR /workspace
 COPY pom.xml LICENSE lombok.config ./
 COPY yudao-dependencies ./yudao-dependencies
 COPY yudao-framework ./yudao-framework
 COPY yudao-module-system ./yudao-module-system
 COPY yudao-module-infra ./yudao-module-infra
 COPY yudao-module-bpm ./yudao-module-bpm
 COPY pms-module-project ./pms-module-project
 COPY pms-module-engineering ./pms-module-engineering
 COPY pms-module-cutover ./pms-module-cutover
 COPY pms-module-service ./pms-module-service
+COPY pms-module-asset ./pms-module-asset
+COPY pms-module-outsourcing ./pms-module-outsourcing
+COPY pms-module-analytics ./pms-module-analytics
+COPY pms-module-integration ./pms-module-integration
 COPY yudao-server ./yudao-server
 RUN --mount=type=cache,target=/root/.m2 \
     mvn -pl yudao-server -am package -DskipTests
 
 FROM maven:3.9.11-eclipse-temurin-25
 
 WORKDIR /opt/pms
 COPY --from=build /workspace/yudao-server/target/yudao-server.jar ./app.jar
 ENV JAVA_OPTS="-Xms512m -Xmx1024m -Djava.security.egd=file:/dev/./urandom"
 EXPOSE 48080
diff --git a/pms-module-analytics/pom.xml b/pms-module-analytics/pom.xml
new file mode 100644
index 0000000..ff638c0
--- /dev/null
+++ b/pms-module-analytics/pom.xml
@@ -0,0 +1,14 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project xmlns="http://maven.apache.org/POM/4.0.0"
+         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
+         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
+    <modelVersion>4.0.0</modelVersion>
+    <parent>
+        <groupId>cn.iocoder.boot</groupId>
+        <artifactId>yudao</artifactId>
+        <version>${revision}</version>
+    </parent>
+    <artifactId>pms-module-analytics</artifactId>
+    <name>${project.artifactId}</name>
+    <description>PMS 分析域：项目组合、进度、风险、质量与资源的只读分析</description>
+</project>
diff --git a/pms-module-analytics/src/main/java/cn/iocoder/yudao/module/pms/analytics/package-info.java b/pms-module-analytics/src/main/java/cn/iocoder/yudao/module/pms/analytics/package-info.java
new file mode 100644
index 0000000..76302c0
--- /dev/null
+++ b/pms-module-analytics/src/main/java/cn/iocoder/yudao/module/pms/analytics/package-info.java
@@ -0,0 +1,5 @@
+/**
+ * PMS 分析域：只读消费业务数据或事件，可保存可重建的指标快照、汇总和分析模型。
+ * 不拥有项目、任务、设备等业务主数据，也不反向修改业务状态。
+ */
+package cn.iocoder.yudao.module.pms.analytics;
diff --git a/pms-module-asset/pom.xml b/pms-module-asset/pom.xml
new file mode 100644
index 0000000..38a1cf2
--- /dev/null
+++ b/pms-module-asset/pom.xml
@@ -0,0 +1,14 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project xmlns="http://maven.apache.org/POM/4.0.0"
+         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
+         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
+    <modelVersion>4.0.0</modelVersion>
+    <parent>
+        <groupId>cn.iocoder.boot</groupId>
+        <artifactId>yudao</artifactId>
+        <version>${revision}</version>
+    </parent>
+    <artifactId>pms-module-asset</artifactId>
+    <name>${project.artifactId}</name>
+    <description>PMS 资产域：设备、备件、RMA 与技术公告</description>
+</project>
diff --git a/pms-module-asset/src/main/java/cn/iocoder/yudao/module/pms/asset/package-info.java b/pms-module-asset/src/main/java/cn/iocoder/yudao/module/pms/asset/package-info.java
new file mode 100644
index 0000000..2dfd457
--- /dev/null
+++ b/pms-module-asset/src/main/java/cn/iocoder/yudao/module/pms/asset/package-info.java
@@ -0,0 +1,4 @@
+/**
+ * PMS 资产域：设备、备件、RMA 与技术公告。
+ */
+package cn.iocoder.yudao.module.pms.asset;
diff --git a/pms-module-integration/pom.xml b/pms-module-integration/pom.xml
new file mode 100644
index 0000000..ad24684
--- /dev/null
+++ b/pms-module-integration/pom.xml
@@ -0,0 +1,14 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project xmlns="http://maven.apache.org/POM/4.0.0"
+         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
+         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
+    <modelVersion>4.0.0</modelVersion>
+    <parent>
+        <groupId>cn.iocoder.boot</groupId>
+        <artifactId>yudao</artifactId>
+        <version>${revision}</version>
+    </parent>
+    <artifactId>pms-module-integration</artifactId>
+    <name>${project.artifactId}</name>
+    <description>PMS 集成域：外部系统交换、字段映射、同步、重试、对账与监控</description>
+</project>
diff --git a/pms-module-integration/src/main/java/cn/iocoder/yudao/module/pms/integration/package-info.java b/pms-module-integration/src/main/java/cn/iocoder/yudao/module/pms/integration/package-info.java
new file mode 100644
index 0000000..ebc16b6
--- /dev/null
+++ b/pms-module-integration/src/main/java/cn/iocoder/yudao/module/pms/integration/package-info.java
@@ -0,0 +1,5 @@
+/**
+ * PMS 集成域：外部系统交换、字段映射、同步、重试、对账与监控。
+ * 业务数据写入目标领域时必须调用其 API，不直接写入目标领域数据库。
+ */
+package cn.iocoder.yudao.module.pms.integration;
diff --git a/pms-module-outsourcing/pom.xml b/pms-module-outsourcing/pom.xml
new file mode 100644
index 0000000..41821d7
--- /dev/null
+++ b/pms-module-outsourcing/pom.xml
@@ -0,0 +1,14 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project xmlns="http://maven.apache.org/POM/4.0.0"
+         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
+         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
+    <modelVersion>4.0.0</modelVersion>
+    <parent>
+        <groupId>cn.iocoder.boot</groupId>
+        <artifactId>yudao</artifactId>
+        <version>${revision}</version>
+    </parent>
+    <artifactId>pms-module-outsourcing</artifactId>
+    <name>${project.artifactId}</name>
+    <description>PMS 外协域：服务商、转包、合同、结算与付款状态</description>
+</project>
diff --git a/pms-module-outsourcing/src/main/java/cn/iocoder/yudao/module/pms/outsourcing/package-info.java b/pms-module-outsourcing/src/main/java/cn/iocoder/yudao/module/pms/outsourcing/package-info.java
new file mode 100644
index 0000000..165336d
--- /dev/null
+++ b/pms-module-outsourcing/src/main/java/cn/iocoder/yudao/module/pms/outsourcing/package-info.java
@@ -0,0 +1,4 @@
+/**
+ * PMS 外协域：服务商、转包、合同、结算与付款状态。
+ */
+package cn.iocoder.yudao.module.pms.outsourcing;
diff --git a/pom.xml b/pom.xml
index 9079d8e..6f8bcdc 100644
--- a/pom.xml
+++ b/pom.xml
@@ -14,20 +14,24 @@
         <module>yudao-server</module>
         <!-- 各种 module 拓展 -->
         <module>yudao-module-system</module>
         <module>yudao-module-infra</module>
 <!--        <module>yudao-module-member</module>-->
         <module>yudao-module-bpm</module>
         <module>pms-module-project</module>
         <module>pms-module-engineering</module>
         <module>pms-module-cutover</module>
         <module>pms-module-service</module>
+        <module>pms-module-asset</module>
+        <module>pms-module-outsourcing</module>
+        <module>pms-module-analytics</module>
+        <module>pms-module-integration</module>
 <!--        <module>yudao-module-report</module>-->
 <!--        <module>yudao-module-mp</module>-->
 <!--        <module>yudao-module-pay</module>-->
 <!--        <module>yudao-module-mall</module>-->
 <!--        <module>yudao-module-crm</module>-->
 <!--        <module>yudao-module-erp</module>-->
 <!--        <module>yudao-module-iot</module>-->
 <!--        <module>yudao-module-mes</module>-->
 <!--        <module>yudao-module-wms</module>-->
 <!--        <module>yudao-module-im</module>-->
diff --git a/tasks/todo.md b/tasks/todo.md
index 199e74a..b5bf448 100644
--- a/tasks/todo.md
+++ b/tasks/todo.md
@@ -109,26 +109,26 @@ pnpm --dir tests/e2e test
 
 ### Checkpoint CP-B
 
 - [x] 后端、前端、数据库、Redis和BPM可以在全新环境启动。
 - [x] 上游测试、迁移重复执行和基础登录通过。
 - [x] 用户确认界面可用并开始自研业务模块。
 
 | 状态/ID | 任务与范围 | 验收标准 | 验证 | 依赖 | 文件区域 | 规模 |
 | --- | --- | --- | --- | --- | --- | --- |
 | - [x] T-CP-007 | 创建V1业务模块骨架和依赖边界检查 | 创建project、engineering、cutover和service模块；无稳定调用方时不创建空`-api`；依赖符合DEC-013 | `mvn clean verify`；运行模块依赖边界测试 | T-CP-006 | `[ROOT]`、`pms-module-project/`、`pms-module-engineering/`、`pms-module-cutover/`、`pms-module-service/` | M |
-| - [ ] T-CP-008 | 创建资产及V2业务模块骨架 | 创建asset、outsourcing、analytics和integration模块；分析和集成模块不拥有核心主数据 | `mvn clean verify`；运行模块依赖边界测试 | T-CP-007 | `[ROOT]`、`pms-module-asset/`、`pms-module-outsourcing/`、`pms-module-analytics/`、`pms-module-integration/` | M |
+| - [x] T-CP-008 | 创建资产及V2业务模块骨架 | 创建asset、outsourcing、analytics和integration模块；分析和集成模块不拥有核心主数据 | `mvn clean verify`；运行模块依赖边界测试 | T-CP-007 | `[ROOT]`、`pms-module-asset/`、`pms-module-outsourcing/`、`pms-module-analytics/`、`pms-module-integration/` | M |
 
 ### Checkpoint CP-C1
 
-- [ ] 全部计划内PMS模块可以参与统一构建。
-- [ ] 模块边界检查未发现跨`-biz`依赖或空API模块。
+- [x] 全部计划内PMS模块可以参与统一构建。
+- [x] 模块边界检查未发现跨`-biz`依赖或空API模块。
 
 | 状态/ID | 任务与范围 | 验收标准 | 验证 | 依赖 | 文件区域 | 规模 |
 | --- | --- | --- | --- | --- | --- | --- |
 | - [ ] T-CP-009 | 创建测试与追溯骨架 | 单元、集成、契约、E2E、性能和安全目录可执行；测试结果可关联任务和FR | 分别运行骨架健康测试；检查`[TRACE]`新增证据字段 | T-CP-008、T-CP-005 | `[E2E]`、`[PERF]`、`[SEC]`、`[TRACE]`、`pom.xml` | M |
 | - [ ] T-CP-010 | 完成基础工程验收 | 全新检出后可按文档构建、迁移、启动、登录和停止；控制台无未处理错误 | `mvn clean verify`；前端lint/build；真实浏览器登录冒烟 | T-CP-009 | `docs/development.md`、`[E2E]`、`[TRACE]` | S |
 
 ### Checkpoint CP-C2
 
 - [ ] CP-01、CP-02计划目标全部满足。
 - [ ] 任何PMS业务代码尚未绕过规格和任务门禁。
diff --git a/tests/infrastructure/verify-pms-module-boundaries.ps1 b/tests/infrastructure/verify-pms-module-boundaries.ps1
new file mode 100644
index 0000000..7081ffe
--- /dev/null
+++ b/tests/infrastructure/verify-pms-module-boundaries.ps1
@@ -0,0 +1,64 @@
+[CmdletBinding()]
+param()
+
+$ErrorActionPreference = "Stop"
+$repoRoot = [System.IO.Path]::GetFullPath((Join-Path $PSScriptRoot "..\.."))
+$modules = @(
+    "pms-module-project",
+    "pms-module-engineering",
+    "pms-module-cutover",
+    "pms-module-service",
+    "pms-module-asset",
+    "pms-module-outsourcing",
+    "pms-module-analytics",
+    "pms-module-integration"
+)
+$rootPom = Join-Path $repoRoot "pom.xml"
+$serverPom = Join-Path $repoRoot "yudao-server\pom.xml"
+$dockerfile = Join-Path $repoRoot "docker\backend\Dockerfile"
+
+foreach ($required in @($rootPom, $serverPom, $dockerfile)) {
+    if (-not (Test-Path -LiteralPath $required)) {
+        throw "Required assembly file is missing: $required"
+    }
+}
+
+$rootPomContent = Get-Content -Raw -Encoding UTF8 $rootPom
+$serverPomContent = Get-Content -Raw -Encoding UTF8 $serverPom
+$dockerfileContent = Get-Content -Raw -Encoding UTF8 $dockerfile
+
+foreach ($module in $modules) {
+    $modulePath = Join-Path $repoRoot $module
+    $modulePom = Join-Path $modulePath "pom.xml"
+    if (-not (Test-Path -LiteralPath $modulePom)) {
+        throw "PMS module is missing its Maven POM: $modulePom"
+    }
+    if ($rootPomContent -notmatch [regex]::Escape("<module>$module</module>")) {
+        throw "Root Maven reactor does not include $module."
+    }
+    if ($serverPomContent -notmatch [regex]::Escape("<artifactId>$module</artifactId>")) {
+        throw "yudao-server does not assemble $module."
+    }
+    if ($dockerfileContent -notmatch [regex]::Escape("COPY $module ./$module")) {
+        throw "Docker build context does not include $module."
+    }
+
+    $apiModule = Join-Path $modulePath "$module-api"
+    if (Test-Path -LiteralPath $apiModule) {
+        throw "$module must not create an empty API module before a stable cross-module caller exists."
+    }
+
+    $modulePomContent = Get-Content -Raw -Encoding UTF8 $modulePom
+    if ($modulePomContent -match "<artifactId>pms-module-[^<]+-biz</artifactId>") {
+        throw "$module must not directly depend on another PMS -biz module."
+    }
+}
+
+foreach ($readModelModule in @("pms-module-analytics", "pms-module-integration")) {
+    $javaSources = Get-ChildItem -Path (Join-Path $repoRoot $readModelModule) -Recurse -File -Filter "*.java"
+    if ($javaSources | Select-String -Pattern "@TableName") {
+        throw "$readModelModule must not own persistence entities while only its boundary skeleton exists."
+    }
+}
+
+Write-Output "PMS module boundary verification passed."
diff --git a/yudao-server/pom.xml b/yudao-server/pom.xml
index 7776e63..e8677b1 100644
--- a/yudao-server/pom.xml
+++ b/yudao-server/pom.xml
@@ -64,20 +64,40 @@
         <dependency>
             <groupId>cn.iocoder.boot</groupId>
             <artifactId>pms-module-cutover</artifactId>
             <version>${revision}</version>
         </dependency>
         <dependency>
             <groupId>cn.iocoder.boot</groupId>
             <artifactId>pms-module-service</artifactId>
             <version>${revision}</version>
         </dependency>
+        <dependency>
+            <groupId>cn.iocoder.boot</groupId>
+            <artifactId>pms-module-asset</artifactId>
+            <version>${revision}</version>
+        </dependency>
+        <dependency>
+            <groupId>cn.iocoder.boot</groupId>
+            <artifactId>pms-module-outsourcing</artifactId>
+            <version>${revision}</version>
+        </dependency>
+        <dependency>
+            <groupId>cn.iocoder.boot</groupId>
+            <artifactId>pms-module-analytics</artifactId>
+            <version>${revision}</version>
+        </dependency>
+        <dependency>
+            <groupId>cn.iocoder.boot</groupId>
+            <artifactId>pms-module-integration</artifactId>
+            <version>${revision}</version>
+        </dependency>
         <!-- 支付服务。默认注释，保证编译速度 -->
 <!--        <dependency>-->
 <!--            <groupId>cn.iocoder.boot</groupId>-->
 <!--            <artifactId>yudao-module-pay</artifactId>-->
 <!--            <version>${revision}</version>-->
 <!--        </dependency>-->
 
         <!-- 微信公众号模块。默认注释，保证编译速度 -->
 <!--        <dependency>-->
 <!--            <groupId>cn.iocoder.boot</groupId>-->
```
