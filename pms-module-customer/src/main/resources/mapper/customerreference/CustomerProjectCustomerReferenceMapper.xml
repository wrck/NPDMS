<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.customer.dal.mysql.customerreference.CustomerProjectCustomerReferenceMapper">
    <!-- 历史引用也必须保留；不能通过软删除记录绕过客户更正边界。 -->
    <select id="countReferences" resultType="long">
        SELECT (SELECT COUNT(*) FROM cus_project_customer_contact_relation WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
    </select>
</mapper>
