<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.commerce.dal.mysql.contract.ProjectContractRelationMapper">
    <select id="selectByIdentityForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.contract.ProjectContractRelationDO">
        SELECT *
        FROM com_project_contract_relation
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND contract_id = #{query.contractId}
          AND relation_role = #{query.relationRole}
          AND deleted = 0
        FOR UPDATE
    </select>
</mapper>
