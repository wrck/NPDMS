<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.commerce.dal.mysql.scope.DeliveryScopeMapper">

    <select id="selectCurrentVersionsForAcceptanceLock"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.mysql.scope.query.DeliveryScopeVersionLockRow">
        SELECT id AS delivery_scope_id,
               allocation_version
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY id ASC
        FOR UPDATE
    </select>

    <select id="selectActiveByProjectIdForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT *
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND scope_status = 'ACTIVE'
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY order_line_id, id
        FOR UPDATE
    </select>

    <select id="selectCurrentByProjectId"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT * FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY order_line_id, id
    </select>

    <select id="selectCurrentByProjectIdForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT * FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY order_line_id, id
        FOR UPDATE
    </select>

    <select id="selectActiveByOrderLineIdsForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT *
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND order_line_id IN
          <foreach collection="query.orderLineIds" item="orderLineId" open="(" separator="," close=")">
              #{orderLineId}
          </foreach>
          AND scope_status = 'ACTIVE'
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY order_line_id, id
        FOR UPDATE
    </select>

    <select id="selectCurrentByOrderLineIdsForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT *
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND order_line_id IN
          <foreach collection="query.orderLineIds" item="orderLineId" open="(" separator="," close=")">
              #{orderLineId}
          </foreach>
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY order_line_id, id
        FOR UPDATE
    </select>

    <select id="selectCurrentByIdForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT *
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.deliveryScopeId}
          AND effective_to IS NULL
          AND deleted = b'0'
        FOR UPDATE
    </select>

    <sql id="projectScopeFilters">
        tenant_id = #{query.tenantId}
        AND project_id IN
        <foreach collection="query.projectIds" item="projectId" open="(" separator="," close=")">
            #{projectId}
        </foreach>
        AND deleted = b'0'
        <if test="query.projectId != null">
          AND project_id = #{query.projectId}
        </if>
        <if test="query.orderLineId != null">
          AND order_line_id = #{query.orderLineId}
        </if>
        <if test="!query.includeHistory">
          AND effective_to IS NULL
        </if>
    </sql>

    <select id="selectCountByProjectScope" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM com_delivery_scope
        WHERE <include refid="projectScopeFilters"/>
    </select>

    <select id="selectPageByProjectScope"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT *
        FROM com_delivery_scope
        WHERE <include refid="projectScopeFilters"/>
        ORDER BY project_id, order_line_id, effective_from DESC, allocation_version DESC, id DESC
        LIMIT #{query.offset}, #{query.limit}
    </select>

</mapper>
