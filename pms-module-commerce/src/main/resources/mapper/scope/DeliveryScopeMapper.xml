<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.commerce.dal.mysql.scope.DeliveryScopeMapper">

    <select id="selectCurrentVersionsForAcceptanceLock"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.mysql.scope.query.DeliveryScopeVersionLockRow">
        SELECT id AS delivery_scope_id,
               allocation_version
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY id ASC
        FOR UPDATE
    </select>

    <select id="selectActiveByProjectIdForUpdate"
            resultType="cn.iocoder.yudao.module.pms.commerce.dal.dataobject.scope.DeliveryScopeDO">
        SELECT *
        FROM com_delivery_scope
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND scope_status = 'ACTIVE'
          AND effective_to IS NULL
          AND deleted = b'0'
        ORDER BY order_line_id, id
        FOR UPDATE
    </select>

</mapper>
