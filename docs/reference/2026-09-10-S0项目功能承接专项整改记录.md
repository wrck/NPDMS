# S0项目功能承接专项整改记录

- 性质：专项整治，不纳入工程实施链，不维护Feature/Task/DU/Gate状态。
- 依据：[26项裁决记录](2026-09-10-S0项目功能承接裁决记录.md)及本会话后续补充，以用户最新口径为准。
- 工作方式：核对旧功能 → 直接整改新功能 → 按改动验证 → 在本文件记录真实结果。旧页面、旧菜单及历史保持。
- 裁决归档提交：`b8de0998`；后续整改代码尚未提交，未推送。

## 专项整治计划（2026-09-10）

- 编制日期：2026-09-10。
- 性质：独立专项整治，不按照工程实施链推进，不使用项目内置技能。
- 落盘依据：用户已确认的12任务包计划；本次仅补充任务总览和推进安排，不代表各任务已实施或验收。
- 记录方式：保留本文件已有裁决补充、实现及验证记录，实际进展继续在本文件维护，不建立第二套进度台账。

### 一、目标与边界

依据[26项裁决记录](2026-09-10-S0项目功能承接裁决记录.md)及后续已确认补充，将整改拆成12个可独立推进、可验证的业务任务包。

目标是完整承接必要旧功能，接入已裁决的目标实体，完成项目、成员、项目树、WBS、阶段导航和业务办理闭环。

统一边界：

- 功能、页面和交互优先复用；实体不能直接照搬，也不能因新实体已经实现就默认全部保留。
- 项目主实体使用`proj_project`，成员关系使用`ProjectMemberAssignment`，项目父子关系使用`proj_project.parentId`，WBS主实体使用`proj_project_task`；模板沿用升级后的新实现。
- 调整集中在新功能及必要消费者。完整验收前，旧页面、菜单、跳转和提示保持不变。
- 明确的开发数据直接迁移；不建设长期映射、同步或双写，不补造业务历史。
- 不回写PRD/SDS、Feature/Task/DU/Gate状态，不自动提交、合并或推送。

### 二、任务包清单

| 编号／任务包 | 对应裁决 | 主要工作与完成条件 | 依赖 |
|---|---|---|---|
| **Z01 成员统一与项目权限** | 02、17及成员补充 | 使用同一添加界面，按项目角色切换候选人员查询。团队成员候选为当前租户内系统项目经理，但不得因此成为该项目经理。验证新增、调整、移出、失效后的实际项目权限，保留主责规则、其他独立授权和关系历史。 | 当前正在整改，优先收口 |
| **Z02 项目主档与客户关联** | 01、14、15、26 | 核对并补齐主档必要字段及原有效操作，完成选客建项、详情重开、普通编辑回读。无下游引用时允许重新选客并留痕；已有引用时禁止普通编辑换客，列明受影响关系，不自动改挂。 | 延续已有选客建项增量 |
| **Z03 项目树闭环** | 04、05、06、18 | 完成父项目搜索选择、移动、真实层级TreeTable及节点独立分页。验证深层定位、追加加载、失败重试、权限占位和循环拒绝，不拍平后代、不猜测修复异常关系。 | Z02相关主档及关系能力 |
| **Z04 WBS基础维护闭环** | 04、06、08、09、10、11、19 | 补齐创建、编辑、移动、父任务选择、节点分页、负责人／执行人、富文本与筛选。人员来自有效项目成员；深层命中保留祖先路径，责任调整留历史，不恢复预估工时维护。 | Z01、Z02、Z06相关能力 |
| **Z05 WBS进度口径** | 09、22 | 新建项目按适用叶子任务等权汇总，父任务不重复计入，关闭不算完成。验证存量项目口径、历史工时和进度快照未被自动切换或覆盖。 | Z04 |
| **Z06 模板冻结、S0与阶段推进** | 03、13、21 | 沿用升级模板，核对建项冻结关系及执行契约。前后端拒绝新增或改归属生成S0任务；业务保存后按冻结关系自动重判。推进失败保持阶段不变、支持重查，不否定已保存业务、不补图、不自动闭环。 | Z02相关建项能力 |
| **Z07 项目列表与六入口概览** | 07、11、16 | 保留新列表并承接原有效操作；经理筛选匹配全部有效经理成员。概览统一为基本信息、项目树、项目成员、WBS、设备清单、实施范围，自动带入当前项目，保留各模块权限和操作。 | Z01～Z04及既有设备、范围能力 |
| **Z08 阶段／任务导航与视觉** | 16、20 | 左侧动态展示阶段、任务和子任务，不固定层级；点击后右侧直接切换内容。阶段页组织自身绑定业务和任务TreeTable，任务页展示详情及办理界面，不以详情抽屉作为最终方向；完成针对性视觉review。 | Z04、Z06、Z07相关内容 |
| **Z09 施工计划功能承接与review** | 20 | 对照DemoV2中的3.1页签，核对基础信息、计划时间、建议时间、备注、提醒和办理交互，完成新入口必要适配。保留工期数据归属及既定审批规则，先验证独立业务闭环。 | 资料已具备，待review |
| **Z10 阶段／任务实际办理接通** | 12、20 | 按实际绑定逐项接通现有业务页面，自动传入项目、任务和关联对象，支持已有记录关联及原业务创建。阶段与任务各自判定完成，任务完成由服务端回源核验；保留独立业务入口，未接通动作如实提示。 | Z04、Z06、Z08；施工计划部分依赖Z09 |
| **Z11 开发数据承接与场景数据** | 14、24、25 | 明确数据随所属任务包迁移，本包集中核对必要关联及场景覆盖。角色、状态或关联不明确时提交具体问题；不要求新旧逐条一致，不建立额外对照体系，不覆盖旧历史。 | 随各业务包滚动推进 |
| **Z12 专项总复验与旧入口交接** | 23、25 | 核对新菜单、跨包业务场景及旧入口未受影响，汇总真实验收结果。整体验收前不切换或停用旧入口；验收后明确并执行最小入口处置，不默认删除旧接口、表或历史。 | 前述适用任务及验证完成 |

#### Z09参考资料已明确

Demo3.1指[《项目交付页面数据DemoV2.html》的「3.1施工计划」页签](../../需求/项目交付/项目交付页面数据DemoV2.html#page9)，页面标识为`page9`，不存在原件缺失阻塞。

该页签用于功能、信息组织和交互review。其示例阶段、提醒阈值、建议日期、审批及CRM交互须核对既有真实业务规则，不仅凭原型文字新增审批、外部集成或固定阶段关系。

### 三、当前起点与推进顺序

#### 计划编制时的起点

以下为2026-09-10计划编制时的起点，来自现有专项记录及规划阶段的必要代码核对，不代表本次落盘重新完成了运行验证。后续进展以本文件对应任务的实际记录为准，不将本节另作一套持续维护的状态源。

- **Z01进行中**：成员页面仍存在多个添加入口；最新角色过滤、统一添加界面和权限联动尚待完成及复验。系统角色定义按既定口径补齐，不自动给现有用户赋角色或菜单权限。已执行V224保持不变，错误角色选项通过前向迁移修正。
- **Z02已有部分实现和证据**：客户选择建项已取得部分浏览器证据；详情重开、普通编辑回读、登录态公共查询、真实HTTP重放及客户更正仍需完成。
- **Z06已有局部证据**：已有一次建项冻结及S0任务为零的记录，不等于全部阶段推进和禁止S0任务路径已验证。
- **Z12已有新菜单增量**：不代表新旧入口交接或专项总验收完成。
- **Z09资料齐备**：待开展review，不再标记为材料缺失。
- 其余任务先核对实际差异，不直接认定为“全部未实现”。

#### 推进顺序

1. 收口Z01，完成Z02剩余动作。
2. 推进Z06、Z03、Z04、Z05，形成基础业务闭环。
3. 推进Z07、Z08、Z09，再完成Z10对应业务接通。
4. Z11随各包推进，最后由Z12集中完成跨包复验及交接。

默认使用当前工作树串行推进，保留既有未提交改动。依赖按所需能力判断，不因某包仍有无关剩余项而阻塞全部下游。

Z04负责通用任务基础能力，Z09负责施工计划独立业务，Z10只负责绑定接入，避免重复实现业务逻辑。各包负责自身必要迁移，Z11不另造一套迁移实现。

### 四、验证与完成条件

每包采用“核对对应旧功能 → 整改实际差异 → 针对性验证 → 一次记录结果”的方式，不设置工程链审批或重复审计。

重点验证场景：

- **成员与客户**：角色候选过滤、跨租户／无权访问拒绝、授权生效与收回、其他授权保留、客户更正边界。
- **项目树与WBS**：深层父节点选择、多节点独立分页、加载失败重试、筛选祖先路径、责任历史与富文本保存回读。
- **进度与阶段**：叶子任务计算、关闭不算完成、存量口径不变、冻结关系推进、保存成功但推进失败、禁止产生S0任务。
- **业务办理与界面**：真实浏览器完成入口定位、上下文带入、保存重开、既有提交／审批及历史查看；完成判断不得绕过业务Owner。
- **数据与旧功能**：迁移后的必要关系正确，历史未被覆盖，旧菜单、页面和跳转保持原行为。

完成一个任务包必须具备相应业务结果和适用验证证据。构建成功、接口测试通过或SQL查询结果不能代替页面验收；已有证据在前提未变时复用，未执行项目明确保留。

### 五、记录与收口

所有实际进度和结果只更新本文件，每包保留：

- 当前进展及下一步动作。
- 实际修改范围和已交付能力。
- 已执行验证、可复用证据及未执行项。
- 具体待裁决问题及其影响范围。

只将真实语义冲突、缺失关联、依赖不可用或必要授权不足列为阻塞；其他独立工作继续推进。

专项完成表示这12个任务包中的适用承接范围已经复验，不自动推导Feature Done、全部S0～S6业务完成或发布完成。

## 当前：模板配置中心能力增强（专项补充，2026-09-10）

按用户确认的三组增强范围实施：组件目录与完成依据服务端化、前后置关系与准入准出规则配置优化、编辑体验优化。属模板配置中心（升级后新实现）能力补强，不改变第03/13/21项裁决的冻结、S0与推进语义。

### 实际修改范围

后端（`pms-module-*`）：

- `BusinessViewComponentProvider.Component` 增加 `displayName` 声明字段，4个Owner Provider（需求分析、动态表单、现场工勘、验收报告）补中文展示名；组件目录API随实体序列化直出，前端不再维护硬编码组件名单。
- `TaskBusinessObjectProvider` 新增 `completionFactLabels()` 默认方法；ACC（REPORT_EFFECTIVE）与SOL（SURVEY_CONFIRMED、SURVEY_ARCHIVED）声明中文标签。
- `TaskBusinessProviderRegistry` 新增 `completionFactCatalog()` 目录聚合（标签缺失回退编码，未声明事实的Provider不产生条目）。
- `ProjectTemplateController` 新增 `GET /project-templates/actions/completion-fact-catalog`（权限 `pms:project-template:query`），仅供配置展示，不构成授权或完成断言。

前端（`yudao-ui-admin-vue3/src/**/project-templates/`）：

- `TaskBindingEditor`：删除 `componentNames` 硬编码过滤，组件与已注册视图全部来自服务端目录；完成依据选项改为按所选实体类型从完成事实目录匹配；目录加载失败不阻断办理页面与表单加载。
- `RuleEditor`：谓词下拉中文化；`BUSINESS_FACT` 的 factCode 从自由文本改为完成事实目录下拉（含Owner上下文标注）。
- `AdvancedTemplateContentEditor`：门禁事实引用按类型选项化——TASK/MILESTONE/DELIVERABLE 从模板内容选择，STATE 用固定完成码，APPROVAL/PROCESS 保持BPM Key输入；引用类型下拉中文化。
- `StageRelationsEditor`：新增只读阶段关系图预览（`StageGraphPreview.vue`，按显式transitions做拓扑分层渲染，开始/收口/默认分支/循环受阻标记，不生成或推断边）；新增关系时自动生成唯一 `transitionCode` 建议（如 `TR_S1_1`，可改），不再留空。
- `TemplateContentEditor`：阶段导航与任务列表增加上移/下移（重写 `sortOrder`，不产生关系或边）。
- `index.vue`：草稿本地暂存——编辑期间防抖写入 `localStorage`（按模板ID隔离），重开时检测并询问恢复/丢弃；保存草稿、发布成功或确认放弃后清除。服务器草稿仍是唯一权威。

### 已执行验证

- 后端：`pms-module-platform/pms-module-platform-api,pms-module-platform,pms-module-engineering,pms-module-project` 编译通过；定向单测通过——`ProjectTemplateControllerContractTest`（含新端点契约）、`TaskBusinessProviderRegistryTest`（新增，目录聚合与标签回退）、`AcceptanceTaskBusinessObjectProviderTest`、`SiteSurveyTaskBusinessObjectProviderTest`、`BusinessViewApplicationTest`、`BusinessViewProviderApplicationTest`。
- 前端：vitest（`vitest.pms-file.config.ts`）模板与业务视图相关5套件 51/51 通过（含按自动编码行为更新1项断言）；`pnpm build:local` 生产构建通过。
- 类型检查：`ts:check` 存在29个错误，全部位于上游基线自带 `src/views/wms/order/shipment/`（依赖未生成的 `auto-imports.d.ts`），属既有环境问题；本次修改文件无类型错误。未扩大修复范围。

### 未执行项与说明

- 真实浏览器页面验收（目录下拉、图预览、排序、暂存恢复的实际交互）未在本轮执行，待前后端服务运行后按专项方式补做；不以单测与构建替代页面验收。
- 本机新装依赖后 vitest 在 Node 24 + worktree 深路径下因包 `#imports` 解析失败无法启动（上游已知问题，CI 使用 Node 22 短路径不受影响）。临时方案：`node_modules/.trae/`（git忽略目录）内放置 package-imports resolve hook 后正常运跑测试；该hook不进仓库，正式环境不受影响。

## 当前：统一项目成员（第02、17项）

### 最新裁决

1. 成员添加只使用一个界面，随项目角色变化切换人员查询。
2. 服务经理、项目经理、销售代表对应明确系统角色；团队成员候选是当前租户内系统项目经理，不套用正式项目经理的同公司限制。正式经理已有组织、主责及责任范围规则保留。
3. 系统资格与项目角色关系区分：选作团队成员不自动成为该项目经理。指派项目角色时，同步生效该角色对应的当前项目权限；移出或区间失效，收回本关系产生的权限，不误撤其他有效角色或独立授权，不扩大到其他项目或后代。
4. 成员、团队使用同一`ProjectMemberAssignment`关系，不配置需求人数、空角色占位或第二团队实体。职责、备注、加入/调整原因与退出原因分别保存，历史区间不原位覆盖。
5. 之前“全部有效用户均可选”“工程师/外协两个新增项目角色”“分角色各设添加表单”的实施方案已被以上裁决覆盖，不能再据其测试结果宣称最终通过。

### 现状与整改边界

- 原`project-team`提供人员选择、角色、状态、备注和增改删；原新成员面板实际只维护经理。保留可复用功能与现有经理命令，只调整`inheritance/`中的新入口和必要服务，不改旧团队页面或旧实体。
- 新详情已整合为成员入口，新列表成员按钮进入该入口；添加表单及查询正在按最新四种项目角色统一，尚未完成。
- 既有用户模块拥有系统角色资格和分页查询，PROJ拥有项目成员及当前项目权限。不得从PROJ直接读SYSTEM业务表或用客户端角色/公司参数绕过资格。
- 测试库暂缺服务经理、项目经理、销售代表的系统角色定义，需要补齐定义；不自动给已有用户增加系统角色或菜单权限。人员的实际角色配置与项目指派分别走其正常功能。
- 旧团队当前30条（PROJECT_MANAGER、TECH_LEAD、ENGINEER各10条），与新项目没有同租户、同稳定项目编码匹配，未做猜测迁移；TECH_LEAD等不机械改成经理。此前跨旧/新编码查询的排序规则不一致仅用查询级COLLATE处理，未更改旧表排序规则。

### 数据与验证记录

- V224已在固定测试库执行：只补`remark/end_reason`，并添加当时方案的两个普通角色选项。该迁移保持不可变；两个错误可选角色将以前向迁移撤出，字段和既有历史不回退。
- 修正前实现已验证：普通成员新增、编辑、移出、重新加入、停用/跨租户用户拒绝、经理保护、事务回滚、并发及幂等；前端新增7项测试通过。它们只证明当时实现，最新角色过滤、统一添加界面和权限联动仍须重验。
- 当前项目范围计算已有成员关系入口；同步生效/收回权限须验证实际角色动作，不能只检查名单或菜单可见。
- 固定基础设施：`npdms_test`，MySQL `23316`，Redis `26379`；应用沿用户指定前端`19581`、后端`59580`。本机环境文件被Git忽略，不提交凭据，不重建/清理数据库。
- 原临时问题编号`Q-FPROJ-012`的裁决历史归入本节，工程链问题文件不再维护此专项项。已执行迁移中的历史标识不修改。

## 上一项：客户选择与新入口（第01、15、23项）

- 新入口为`/pms-inheritance/projects`和隐藏详情；旧页面/菜单不变。客户只能从主档选择，编码只读。
- 新建项`POST /api/v1/pms/projects`复用原事务/实例化，强制客户编码，客户名称与内部ID由CUS回源确定；保留原幂等机制与独立作用域。普通资料更新不能改写已关联客户身份，客户更正仍按第26项另做。
- 新`GET /api/v1/pms/customers/by-code`和内部按编码摘要查询保持租户、可见范围与详情裁剪。未登录实测返回401且无客户数据；登录态公共REST正向回包仍未单独验证。
- V223新增3条独立菜单，192个历史迁移校验通过，重复迁移为0；原1,860条菜单比较未变。
- 定向证据：后端62项（含5项固定MySQL）、前端4项、类型检查及前后端构建通过。旧页面代码差异为0。此为此前实际结果，不因后续改动重复宣称已重跑。
- 前端运行配置曾缺失，补齐`.env.env.local`并重启后，权限请求正常到达59580，使用既有登录态完成新旧菜单查看和客户选择建项。未读取会话存储或启用Mock认证。
- 历史`TPL-PRE04-NO-EXT v2`缺冻结定义，首次创建失败且没有项目落库；没有补图、回填旧发布版或降低校验。
- 通过模板正式UI复制并发布独立测试模板`S0-INHERIT-ACCEPT-20260910`：模板ID `993009001591`，v1修订 `993009001593`，38条冻结定义；源草稿仍DRAFT/version 1，旧发布版未改。
- 浏览器实际创建`PJT2026000003` / `992203060004`（S0承接选客验收-20260910），客户tenant 1 / ID 1001 / `CUST-001` / 北京华盛科技有限公司与主档一致，仅1个项目。
- 落库7阶段、7份阶段契约、6条流转关系、12任务，S0任务为0；新建项幂等记录1条COMPLETED，PROJECT_CREATE审计SUCCESS。未用此结果宣称12项任务业务办理已完成。
- 仍待验：详情重开、普通编辑回读、登录态公共REST正向回包、真实HTTP重放。此前内置浏览器标签页崩溃，新标签页附着失败；这些项目保留未通过，不以SQL替代页面验收。

## 未扩展范围

本专项未因此完成其余裁决、S0～S6全部业务或最终视觉设计。项目树/WBS、动态阶段/任务导航、Demo3.1施工计划review及客户更正等按对应裁决继续，不能为了专项跳过真实权限、租户、状态或历史保护。
