# SDS Phase 1：系统架构

> 文档状态：`BASELINE`
> 适用基线：PRD V1.6（`docs/baseline/prd-v1.6.md`）
> Requirement ID：PRD V1.6 附录 A.1 的全部 115 项 V1/V2 正式需求；逐项范围与本分册落位见 `docs/traceability/requirement-matrix.md`
> Owner：SDS Phase 1 架构设计；业务 Owner 已签署，见 `docs/design/phase-1-domain-ownership.md`
> 适用规则：上述 Requirement 范围适用于本分册全部章节；章节或表格明确缩小范围时，以其明示范围为准


## 1. 架构结论

【建议】采用“模块化单体 + 领域模块 + 内部事件 + 外部适配器”的起步架构，基础平台提供认证、组织、字典、文件、待办、审计和工作流能力；业务域在同一系统内保持清晰边界。Q3 已确认 V1 优先将现有采集平台作为子应用集成；后续可在不改变 Owner、授权和明文边界的前提下演进为内部模块，不重复建设设备连接与原始采集引擎。

## 2. 系统上下文

| 系统/参与者 | 方向 | 平台职责 |
|---|---|---|
| CRM | 双向/事件 | 接收项目、客户、销售执行信息；回写项目关联结果 |
| ERP | 入向/回写 | 接收合同、订单行和交付范围；回写履约事实 |
| ITR | 双向 | 接收版本/故障/割接触发；回传割接结果 |
| 现有采集平台子应用 | 双向 | 接收任务级短期授权，回调状态、日志和结果引用；不取得用户全量权限 |
| UMC | 按需 | 提供巡检结果解析或报告生成能力 |
| 钉钉/HR/OA/LDAP/AD | 双向或入向 | 协作、考勤、审批通知、身份和组织同步 |
| 用户浏览器/未来桌面端 | 入向 | 响应式 Web 工作台和后续多端复用 API |

## 3. 容器视图

1. 基础平台层：身份、组织、租户、字典、文件、待办、工作流、审计、通知。
2. 业务应用层：项目交付、交付准备与方案、实施执行、验收与闭环、割接、工单与工时、巡检、服务运营、客户与关系、资产管理、合同履约、资源外包、经营分析、设备连接与采集。
3. 设备连接与采集 Context：凭证、凭证授权、采集任务、外部状态原值、结果引用和回调证据；V1 由现有采集平台子应用承载连接、命令执行和原始采集，本平台负责凭证、授权、任务下发和回调，不重复建设采集引擎。
4. 集成适配层：CRM/ERP/ITR/采集平台/UMC/OA等适配器，负责协议、主数据同步、幂等、重试和对账。
5. 数据层：业务交易库、本地同步主数据副本、文件对象存储、审计日志和异步消息；具体表结构在 Phase 2 设计。

## 4. 依赖规则

- 业务模块不得直接访问其他模块 Repository。
- 集成适配器不得直接写业务表，必须调用目标域应用服务。
- 分析模块只读业务事实或快照，不参与交易状态变更。
- 采集子应用使用独立执行身份，不继承用户或管理员全量权限，只接受任务级、短期且不可复用的执行授权；网络和接口契约在 Phase 2 登记具体端点、信任边界和 Owner。
- 外部 CRM、ERP、MES、ITR 的必要主数据同步到本地后供日常查询使用；外部系统保留权威 Owner，平台保存来源版本和同步状态，不以实时接口调用作为唯一查询路径。
- 前端优先响应式布局；桌面客户端作为后续端复用，不改变领域 API 契约。

## 5. 运行与部署假设

Q2 实现工作包已登记独立实现仓库 `E:\AICoding\Projects\NPDMS`、锁定基线提交 `3c54ee1bb3c1d2fa4bad958ea6691956a7ac2464`、基础平台锁定来源、NPDMS 独立开发数据库目标和统一证据批次号 `NPDMS-SDS-P1-20260812-01`；构建入口沿用后端 Maven、宿主机前端 pnpm 和仓库测试脚本。前端冻结安装与生产构建已通过，Q2 实现工作包门禁已解除；具体登记值与核验证据以 `docs/engineering/gates/phase-1/gate-status.md` 为准。本规格仓库不承载正式实现或迁移代码。
