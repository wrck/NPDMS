# 项目模板与执行解耦：实施计划 v1.1

> 编号：NPDMS-TED-IP-1.1；日期：2026-09-17。
> 分支：wrck/NPDMS / codex/liteflow-remediation。
> 设计：[v1.1 设计基线](../specs/2026-09-17-template-execution-decoupling-design-baseline-v1.md)。
> 本版先独立提交，再按 P0～P9 推进。阶段/环节的代码、测试源码和兼容处理完成后提交；真实验证单独记录。

## I00. 本次变更与范围

用户最新要求优先：使用版本，不新增校验 Hash；权限码对应业务动作；复用业务实体/命令输入；页面可配置 URL。v1.0 中多重指纹、默认多重操作版本和业务统一补字段的要求停止执行。旧基础设施内部摘要保留兼容，不以此扩展新配置。

不变目标：业务独立、项目附加约束、独立订阅、证据政策、视图/待办解耦；不迁全领域、不换引擎、不重写旧状态/权限/历史。只增加必要路由、引用、类型、身份与并发校验，不把“不加 Hash”误解成取消业务安全检查。

原完整文档和 100 条 AT 定义保留在提交 `75dfa7d98cade30f708759720400e6b8c01e5ec0`。本版 I14 保留同一批 ID，明确变更断言；旧已通过记录不转成本轮 PASS。

## I01. 顺序、阶段和进度保存

P0 范围/来源 → P1 最小配置与版本冻结 → P2 普通业务独立 → P3 权威结果 → P4 独立订阅 → P5 证据推进 → P6 前后置事实/事务 → P7 通用分派 → P8 页面/待办 → P9 全阶段自审。

每个阶段可包含明确编号的环节。一个环节完成代码、测试源码、旧行为保护后形成一次提交；父阶段在全部出口齐备前保持 IMPLEMENTING，不提前宣称阶段完成。文档修订、P0 准备、生产增量、自审修复分别提交，不混入无关业务。

进度只在 `template-execution-decoupling-progress.md` 记录一次。每条记录有阶段/环节、基准、文件、已做/未做、实际测试与下一项；自身提交由该记录的 Git 历史定位，下一次更新补记真实 SHA，避免为自引用制造额外提交。

提交前重读远端 HEAD。变化时核对目标文件并在新树上重建差量；只允许 force=false 快进。提交后回读父提交、文件集和分支 HEAD。代码提交不授权部署、迁移、服务重启或实例切换。

状态：PLANNED / IMPLEMENTING / PARTIAL / CODE_AND_TESTS_READY / COMMITTED_PENDING_VERIFICATION / VERIFIED（注明范围）/ BLOCKED / ACCEPTED。确定代码缺口不是待真实验证；无环境可以提交齐备代码和测试源码，但不能标验证通过。

## I02. P0：最小范围、字段来源和保护边界

**产物：**当前来源/范围记录及字段责任表；准备阶段不改生产行为。

| 环节 | 工作及出口 |
|---|---|
| P0.01 | 核实当前 HEAD、目标文档、仓库协作约束、构建能力和未提交工作区；读取失败明确记录，不能覆盖未知工作树。 |
| P0.02 | 从真实登记冻结本次 C0 的业务边界、操作清单、权限映射及直接消费者路径；只声明已核实范围。未读的独立配置/回调/结果行为分配到其阶段，不能标已审计。 |
| P0.03 | 为配置/身份注明谁生成、条件性必需和复用方式；无业务修订不造 revision，无历史不启用 NEW_RESULT。 |
| P0.04 | 记录旧快照格式、旧哈希保留路径、实例版本/锁、Outbox/结果来源与保护边界；运行金样和数据库验证在改动对应路径前完成，不要求准备阶段先跑完整系统。 |
| P0.05 | 建立验收/进度入口，明确 C0 缺口的负责阶段及停止条件。 |

出口：本轮改动边界、版本来源、已知代码线索、禁止扩大项和下一环节明确。P0 完成不等于全部旧业务已审计；每个真正改行为的环节仍须先审计该实现和回调。现有检查点可复用事实，但“工具不可写”等历史限制必须重新检查。

## I03. P1：最小配置、目录消歧和不可变模板版本

**目标：**不新增多层 Hash，不要求业务 DTO 增加技术版本，先使配置协议可用且不影响旧运行。

| 环节 | 代码/消费者 | 代码与测试出口 |
|---|---|---|
| P1.01 | 复用现有操作目录，登记原权限码；提供纯解析器，按 Owner/实体/权限和必要动作消歧；接入目录 API | 唯一、零匹配、共用权限、多版本、跨 Owner、冲突登记测试；目录查询无副作用；不是运行授权。不要求重写旧 Controller 业务命令。 |
| P1.02 | 新发布格式以模板发布版本保存完整冻结内容；接入编译、发布、读取、复制和计划直接消费者 | 已发布不可更新；准入/准出/收口变化只通过新发布版本生效；旧 Reader/旧 Hash 保持；缺任一新 Reader 不启用新发布。 |
| P1.03 | 最小操作/订阅/政策/页面模型；规则与操作解析结果随模板内联；复用业务输入描述 | 新字段保存重开无损、纯订阅不需操作/页面、NONE/缺失区分；没有新 policyHash/semanticDigest 等字段。 |
| P1.04 | 业务实体输入和 pageUrl 的公共边界校验，直接消费者编译接入 | 不接受任意实体类/任意写 URL；允许有效站内路由和独立编码参数；不原地修改业务 DTO/旧快照；对应测试齐备。 |

P1 完成须四环节全部集成；仅目录或模型存在不算完整新模板发布能力。P1.01 可先提交，旧操作版本在底层兼容保留，配置者不手动配多版本。

## I04. P2：普通业务独立与权威写路径

对 C0 每项先审计 Commands/Access/Provider、版本/表单/文件回调及原测试，再按以下次序交付：

- P2.01 确定业务自身配置来源，复用原已发布业务配置或实例冻结信息；无活动节点仍可解析。
- P2.02 将普通业务与项目办理分流落到真实应用边界，不仅换外层名称；原权限、内容、状态、范围和乐观并发不丢失。
- P2.03 明确 PROJECT_ENTRY_ONLY/ALL_ENTRIES；来源节点只作追溯，不能自动成为普通业务写前置。
- P2.04 复用已有版本/锁/路由保证一个实例一条权威写路径。只有确实不足时补最小切换记录，不默认所有实体加 writerEpoch。

出口：所有声称独立的 C0 操作具备真实独立配置和路径，实际入口/回调测试齐备。只消费结果的能力不必先迁移同 Owner 全部命令。跨域或旧实现保护冲突只阻断相关单元，不伪造已完成。

## I05. P3：按能力提供权威结果

- P3.01 定义真实对象、结果、可选业务修订及有效性语义；保存/上传/报告发布不冒充业务完成。
- P3.02 复用原业务事务/Outbox 和结果回读，明确唯一生产者，普通与受控入口不造两份同义事实。
- P3.03 按能力区分当前查询、指定结果、历史/变化读取；只有承诺历史复用/新结果政策的来源需要稳定边界与分页/游标。
- P3.04 核对删除、撤销、替代、乱序、保留期和旧结果转换；不能填造历史成功或递增计数假装版本。

出口：所声明能力有真实实现和相应测试；缺历史只禁用对应政策，不禁用不依赖它的合法业务操作。结果引用不要求业务提供项目轮次、政策 Hash 或事件 Schema 组合。

## I06. P4：独立订阅与恢复

- P4.01 新结果路径不以 operationContract 为前提；按 Owner/结果类型/对象范围匹配候选。
- P4.02 订阅按模板发布版本、节点局部键和执行轮次区分；不加 selectorHash/policyHash。
- P4.03 有历史能力时冻结本轮形成基线、分页核对、保存 checkpoint 并接续可靠变化流；证明提交边界，不把 MAX(id) 或到达时间当证明。
- P4.04 各目标独立持久恢复；新订阅可核对早于订阅的结果；游标过期明确进入恢复，不跳过缺口。

出口：纯订阅节点无命令/页面也能处理；接收 ACK、候选、证据和完成不混同。无实际来源能力的政策保持不可启用。

## I07. P5：证据政策与正式推进

- P5.01 按模板内联政策实现 REUSE_EXISTING / NEW_RESULT / PINNED_RESULT，历史事实与当前有效分开。
- P5.02 从新路径去掉“对象曾关联过即排除”；以真实结果和本轮政策判定，同对象新修订可再次采纳。
- P5.03 EXACT_ONE / ANY_MATCHING / ALL_EXPECTED 明确，选择和分页不默取第一项，目标集合变化用发布/执行版本解释。
- P5.04 撤销/替代/追溯纠正覆盖未采纳、已采纳未完成、已完成场景；只走正式异常/返工，不直接倒改历史状态。
- P5.05 转换前重验计划、轮次和事实，调用正式完成/准出/收口 Writer；证据与状态转换分别幂等。

出口：业务成功但项目不满足时保留业务回执并等待；所有声明政策的真实能力都有结果/并发/失效测试，不以对象关联代替证据。

## I08. P6：原输入、按需事实与受控事务

- P6.01 复用业务 SaveReq/Command DTO/实体引用，保留原字段约束、权限和归属检查；项目身份放通道上下文，不让普通业务填技术字段。
- P6.02 按需登记 PROJECT/BUSINESS/COMMAND_INPUT/TRANSACTION_RESULT；未声明字段不可访问，不支持事实禁止相应规则。
- P6.03 PRE → Owner → POST 在真实同事务中；POST 读更新后事实；外部不可逆动作/独立事务不得声明原子回滚。
- P6.04 全入口受控仅在真实应用写边界明确声明，覆盖内部/HTTP/系统入口；普通未声明操作不被全局拦截。
- P6.05 复用既有幂等/Scope/回调证明/追踪；同键不同请求冲突，未知结果不换键或新版本；不新增通用校验 Hash。

出口：声明的事实与事务能力实际接线，四类权限/版本/目标/回滚失败测试齐备。已有幂等组件内部摘要不要求业务维护，也不在本阶段重建。

## I09. P7：注册驱动通用分派

P7.01 用 P1 目录和原权限码绑定消除新公共分派的实体 switch；选择出的操作实际仍重验业务授权。
P7.02 对共用权限的多个动作保留原 operationCode，不合成通用 UPDATE；旧适配器的版本由发布结果承接，不默认增加各层独立版本。
P7.03 C0 逐项接线与契约测试；另一个对象形态的测试夹具证明公共代码无需新增实体分支，但夹具不算真实业务迁移完成。
P7.04 独立兼容升级保持旧模板解释；不能静默使用最新处理器。

出口：真实 C0 运行消费者全部使用对应受信绑定；目录存在不代表业务命令/回调已验证。

## I10. P8：页面 URL、实体交互与待办

P8.01 模板直接选择权限/动作、业务实体类型、pageUrl 和必要规则；隐藏技术版本组合，无 Hash 输入框。
P8.02 已登记站内路由安全加载，参数独立编码；URL 不作为 API，拒绝脚本/未登记外域/穿越/主体与凭据注入。
P8.03 页面复用业务输入与现有查询；权限、节点资格、前置、视图只读分轴展示，提交时重新核验。
P8.04 保留未保存表单，普通刷新不销毁客户端；身份变化明确重开，未知操作先恢复；文件写权限不从发布权限推导。
P8.05 待办只投影正式节点状态；重建和打开不产生业务写入，业务回执不等于节点完成。

出口：阶段直接办理、纯订阅、无页面节点和 C0 真实业务页全部覆盖；仅 URL helper 或纯函数不能标 UI 闭环。

## I11. P9：全阶段源码自审和真实验证记录

重新以最终 HEAD 检查 v1.1 设计、AT/简化测试、当前代码和全部已声明消费者。修复跨环节问题并保留阶段历史，未解决项写明影响与下一步。

必须复核：已发布不可变、旧 Reader/历史回执、权限共用消歧、原实体字段保护、URL 与命令分离、业务独立、订阅独立、同对象新结果、已完成证据失效、事务/Outbox、一实例权威写入、编辑恢复。

源码审查、测试源码、纯 Java/Node、组件测试、真实数据库/API/浏览器分别记账。P9 源码整改可提交但不能自动标全链路 ACCEPTED。

## I12. 提交与保护

每个完成环节的提交只含本环节代码/测试/必要追溯。记录精确基准和未验证项。远端 HEAD 变化先核查，不覆盖并行代码。不创建占位 Provider 或空提交证明进度。

当前版本配置不参与全部旧实例自动切换。对原始不可变快照、审计、事实、来源和历史结果不重写；新增迁移仅准备代码，执行需独立授权。

## I13. 验证层级与记录

L1 纯逻辑；L2 真实组件/序列化/代理（可有明确外围 Mock）；L3 真实数据库/事务/服务；L4 API/浏览器；LA 源码/结构核对。H2、Mock、语法转译均不得冒充 MySQL 或真实业务链路。

一条记录包含：环节、基准/提交、改动、测试源码、实际命令与环境、PASS/FAIL/NOT_RUN、失败原因、剩余能力。部署状态独立。自提交 SHA 以 Git 历史确定，不为填表重复创建提交。

## I14. 100 条 AT 的版本化继承与明确修订

v1.0 的 100 个稳定 ID、场景和层级保留，原文可通过以下不可变 Git 基准读取：

```sh
git show 75dfa7d98cade30f708759720400e6b8c01e5ec0:docs/superpowers/plans/2026-09-17-template-execution-decoupling-implementation-plan-v1.md
```

这是测试追溯，不是继续执行旧 Hash 设计。当前断言按下表替代；未列变更的原业务、安全、回滚、历史及 UI 义务继续有效，全部初始为 NOT_RUN。无需复制一份 100 行状态表或制造更多摘要文件。

| AT ID/集合 | v1.1 当前断言/继承 |
|---|---|
| AT-H01 | 旧发布记录按旧格式读取且不重写；旧内部 Hash 可保留。 |
| AT-H02 | 仅准入规则变化产生新模板发布版本；旧版本内容/行为保持。 |
| AT-H03 | 仅准出变化同样通过新发布版本生效，回读同一冻结程序。 |
| AT-H04 | 适用/收口完整冻结，新发布版本可区分变化。 |
| AT-H05 | 间接决策表/规则闭包随模板内联；改最新资产不影响旧发布版本。 |
| AT-H06 | 操作、订阅、证据语义变化均在新模板发布版本；不独立发政策 Hash。 |
| AT-H07 | 发布后任何同版本内容更新拒绝；序列化顺序不作为授权/身份；有序列表语义保持。 |
| AT-H08 | 实时权限/事实不成为永久授权；布局随版本保存或现有非执行编辑机制，不产生新呈现 Hash。 |
| AT-H09 | 未知新格式/缺冻结内容拒绝；不依赖 hashProfile，不失败后退 legacy。 |
| AT-H10 | 新增执行字段有完整冻结/回读覆盖，禁止同版本原地改义。 |
| AT-C01～C07、C09～C10 | 继承原契约独立、字段无损、失效/删除和预设冻结义务；不再要求每项默认独立版本。 |
| AT-C08 | 底层存在不兼容版本时明确绑定，未指定且多义不取 latest；配置用户不手配版本矩阵。 |
| AT-B01～B10 | 原独立路径、业务约束、缺/多上下文、全入口边界和纯结果能力义务不变。 |
| AT-O01～O06、O08～O12 | 原权限、并发、PRE/POST、派生目标、恢复及事务义务不变。 |
| AT-O07 | 同键同业务意图安全重放、异意图冲突、执行中可恢复；复用原组件，不新增摘要规范。 |
| AT-F01～F08 | 原事实安全、类型、时点、意图冻结和一致性义务保留；真实不兼容格式才需独立版本。 |
| AT-S01～S12 | 原独立订阅、分页/水位/乱序/多目标/恢复测试保留；仅对声明相关能力的来源启用政策，无能力不得伪造。 |
| AT-E01～E14 | 原结果身份、三种获取、两种有效性、选择/集合/失效/正式推进保留；政策随模板版本，不使用 policyHash。 |
| AT-U01～U10 | 原查询无副作用、逐动作权限、只读、编辑/恢复、文件及阶段身份义务不变；增加本表后的 URL 与实体测试。 |
| AT-M01、M04、M06～M08 | 原历史、回执、可证明迁移、防降级及审计义务不变。 |
| AT-M02～M03、M05 | 一实例单权威与并发切换不变，复用原版本/锁；仅必要时增加栅栏，不强制所有实体加 epoch。 |
| AT-A01～A06 | 原扩展、位置解耦、依赖和全范围交付义务不变；独立接口版本仅在实际兼容需求存在时使用。 |

新增简化专项（不计入原 100 条）：SC-01 权限唯一直选；SC-02 共用权限消歧；SC-03 越 Owner/实体拒绝；SC-04 无新 Hash/业务技术版本必填；SC-05 原实体输入和字段/归属校验；SC-06 站内 URL/参数编码；SC-07 非法协议/外域/穿越与凭据拒绝；SC-08 URL 不调用业务命令；SC-09 没有业务修订不造版本；SC-10 无历史只禁相关政策；SC-11 底层多版本不默选最新；SC-12 已发布同版本不可修改。

## I15. 风险与启用边界

主要停止条件：实际需求/写职责冲突、旧保护边界不明、缺失处理器/Reader、确定测试失败、无法证明的证据政策或事务能力。只阻断依赖项，不能宣称“环境未验证”掩盖代码缺口。

新配置可以在对应保存层完成后保存草稿；发布/运行必须按实际依赖接通。目录支持权限直查不等于新快照可发布；URL 可解析不等于实体页面受控提交已适配；结果事件存在不等于证据规则完成。

## I16. 本版起点

修订前 HEAD：`75dfa7d98cade30f708759720400e6b8c01e5ec0`，生产代码仍沿用前序修复。此前本地 P0 检查点不是远端阶段提交；本次重新核查后从 P0 顺序执行。

文档修订提交时 P0=PLANNED，P1～P9=PLANNED。实际后续状态在 [进度记录](template-execution-decoupling-progress.md) 更新；无记录不推导为已完成。本版不把计划修改计为 P0 实施、不自动继承历史测试 PASS。
