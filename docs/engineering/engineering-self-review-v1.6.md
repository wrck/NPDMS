# V1.6 工程化自审报告

> 审查对象：V1.6 需求基线、正式工程链、追溯矩阵及进入 SDS Phase 1 的准备度
> 审查原则：PRD V1.6 是唯一业务语义基线；旧 `specs` 只作参考，不直接驱动设计。
> 审查状态：`PHASE-1-READY-WITH-CONTROLS`

## 1. 已核验事实

| 检查项 | 结果 | 证据 |
|---|---|---|
| PRD 版本与状态 | 通过 | PRD V1.6，正式基线 |
| 基线快照 | 通过 | `docs/baseline/prd-v1.6.md` 与 PRD 字节一致 |
| 快照哈希 | 通过 | `CEE9DF66B3D1B04B1ABCB737AE4CEB3FA06DC77706957A653D6DA02F1B4AE811` |
| 正式需求数量 | 通过 | 115 项；V1 57 项、V2 58 项 |
| 领域归属 | 条件通过 | 已按 PRD V1.6 形成 13 个 PRD-derived 工作映射，待领域负责人签署 |
| PRD 语义门禁 | 通过 | `validate_prd_semantics.py`：0 issues |
| 领域生成门禁 | 通过 | 13 文件、115 formal、22 V3、9 OUT_OF_SCOPE |
| 单元校验 | 通过 | 6/6 |
| 格式检查 | 通过 | `git diff --check` |
| 工程链唯一性 | 通过 | `docs/engineering/00-engineering-chain.md` 为正式入口 |

## 2. 发现与处理

### 必须处理（已处理）

1. 追溯矩阵初版仅识别 90 项需求。已改为以 PRD 附录 A.1 为权威索引，当前生成 115 项且无重复。
2. 旧 `specs` 不能继续作为现行规格基线。已在工程链、基线元数据、追溯矩阵和 SDS README 中标记为参考资料，并新增 PRD-derived Owner 工作映射。
3. 工程链文件曾存在正式文件与增强稿混淆。已恢复 `00-engineering-chain.md` 为唯一正式入口，历史稿移入 `archive/`。

### 进入 Phase 1 前的控制项

| 控制项 | 当前状态 | 处理要求 |
|---|---|---|
| 领域边界与 Owner | `RECONFIRM_REQUIRED` | 以 PRD 115 项需求重新划分，不直接复制旧领域文件 |
| 追溯下游字段 | `NOT_STARTED` | Phase 1 逐项补齐领域、模块、聚合、状态机、权限、计划 API、数据对象和测试类别 |
| SDS 未决问题 | `OPEN` | 只能记录 PRD 明确缺口，不能自行补充业务规则 |
| 方案设计基线 | `NOT_STARTED` | Phase 1 通过前禁止进入数据库、API 和代码实现 |

## 3. 五轴自审结论

- 正确性：基线数量、版本边界、快照和门禁结果一致；追溯矩阵已修正漏项。
- 可读性：正式工程链、基线、设计目录和追溯矩阵入口清晰；历史规格已显式降级。
- 架构：下一阶段应从业务域和聚合边界开始，不从旧文件的模块划分反推架构。
- 安全：设备凭证、临时密码不落库、最小授权和服务端强制校验已进入 PRD，Phase 1 必须映射到权限设计。
- 性能：当前无实现代码，不能宣称性能达标；性能约束只作为 Phase 3 设计输入。

## 4. 阶段结论

可以进入 SDS Phase 1，但必须以 `PHASE-1-READY-WITH-CONTROLS` 状态推进。Phase 1 的退出条件是：115 项正式需求全部有唯一领域归属，核心聚合/状态机/流程/权限边界完成，且 `phase-1-review.md` 明确 `APPROVED`；否则保持 `NOT_READY`，不得进入 Phase 2。
