# 项目独立计划生效专项验证（2026-09-14）

本记录只证明下述实现与实际操作，不代表专项整治整体完成，不新增审批或工程实施门禁。需求依据为本任务已批准的“草稿保存、影响预览、生效分别操作；生效不自动返工，冻结历史不覆盖”。

## 环境与场景

- 固定测试环境：Docker `npdms-50eb-test-mysql-1` / `npdms_test`（23316），Redis 26379；宿主机后端 59280、前端 19081。启动脚本校验数据库名称，未清库、未覆盖用户项目草稿。
- 内置真实浏览器创建项目 `PJT2026000017` / `993109130060`，名称“工前准备计划生效验收20260914”。
- 选用已发布“工前准备交付场景验收”，来源模板版本 `993009001609`。项目包含阶段“工前准备”，以及“现场工勘”“需求分析”任务。
- 本次改版仅将阶段名称改为“工前准备（项目调整）”，不改变绑定，不新增、删除或办理任务。任务子画布曾打开；工具箱单击没有新增节点，不将其计作拖拽验收。

## 实际发现与修复

第一次生效在 19:40:54 失败，后端 `TemplateInstantiator.instantiate` 报“任务状态机版本不能为空”。原因是生效入口只为新增任务解析已发布状态机，而安装器对没有新增任务的改版仍调用单一新版本实例化入口；原安装器测试始终传入非空版本，未覆盖该组合。

- `TemplateInstantiator` 复用原结构映射，增加按任务实例 ID 解析状态机版本的入口。原项目创建入口保留原校验。
- `ProjectPlanStageTaskInstaller` 使用已有任务各自冻结的状态机版本，只有新任务使用本次已发布版本；纯阶段计划无需任务版本。
- 补充四项回归：无新增任务的阶段改名、已有任务改名保留自身版本、纯阶段计划、缺失版本的新任务在写入前拒绝。
- 未引入依赖、兼容层、额外发布系统；没有修改状态机定义、授权及生效事务边界。

## 浏览器及数据库证据

1. 浏览器从项目有效 v1 创建 v2 草稿，修改名称，保存后编辑版本为 1；此时有效计划仍是 v1。
2. 点击“影响预览”，显示预览通过；阶段和两项任务延续第 1 轮，不自动返工。
3. 第一次失败后查询数据库：v1 为 `EFFECTIVE`、v2 为 `DRAFT`，原三个执行仍绑定 v1，原绑定 ID 和轮次未变化。
4. 修复、构建并重启测试后端后，在原页面重试“生效计划 → 确认生效”。19:45:58 生效成功。
5. 项目详情显示新阶段名称及第 1 轮；重新打开项目计划显示“当前项目计划 v2”，没有残留草稿，可从当前版本新建草稿。
6. “执行历史与规则结果”真实窗口同时显示 v2 当前有效、v1 历史版本；原阶段及两项任务仍是第 1 轮。
7. 数据库核对：v1 `2099461923814699009` 为 `SUPERSEDED`；v2 `2099462063522770946` 为 `EFFECTIVE`。生效前后直接逐值比较 v1 的完整 `designer_document` 和 `execution_snapshot`，完全相同（未使用摘要替代比较）。
8. 原执行 ID `2099461923844059137`、`2099461923873419266`、`2099461923881807874` 保持不变，绑定 ID 保持不变；有效计划指针切到 v2，轮次仍为 1，状态分别为 `ACTIVE`、`PENDING`、`PENDING`。
9. 仅一条 `PROJECT_PLAN_APPLY / SUCCESS` 审计；专用 `ProjectRuleReevaluationRequested` Outbox 事件已由技术调度消费为 `DELIVERED`，重试次数 0。未直接修改业务状态或调度表来通过验证。

测试项目页面：`http://localhost:19081/pms/project-management/project-master-detail?projectId=993109130060`。

## 验证命令与结果

使用 JDK 25，Maven 单进程测试及已安装 Mockito agent；本次选择的测试均为单元测试，不访问生产库。

```powershell
mvn -pl pms-module-project -am test '-DforkCount=0' '-Dtest=TemplateInstantiatorTest,ProjectPlanStageTaskInstallerTest,ProjectPlanActivationServiceTest,ProjectManualCreationServiceImplTest' '-Dsurefire.failIfNoSpecifiedTests=false' '-DskipITs=true' -q
mvn -pl yudao-server -am package '-DskipTests' '-DskipITs=true' -q
```

- 测试退出码 0：实例化器 13、阶段任务安装器 16、生效服务 9、原项目创建 22，共 60 项，失败/错误/跳过均为 0。
- 首次命令因 PowerShell 未引用带点的 Maven 参数而未执行测试；引用参数后上述测试执行通过。
- 首次打包因运行中的 Windows Java 占用 JAR 而失败；确认并停止本次测试 Java 后重新打包通过。没有以观察超时为理由重启进程。
- 最终 JAR 时间 19:44:49，19:45:43 启动成功。当前测试 Java PID 36216；进程 ID 只作为本次记录，不应作为后续停止服务的依据。
- 启动日志：忽略目录 `.run/plan-activation-retry-backend.log`；失败定位日志 `.run/plan-activation-backend.log`。日志不替代上述业务验证。

## 验证边界与后续

本次跑通的是“已有阶段元数据改版 → 预览 → 生效 → 重开 → 历史保护”真实路径。新增/删除节点、已办理结果下的改版保护、选择性返工、共享业务对象、阶段直接业务办理、决策表及完整并行运行仍需继续按原专项计划验证，不因这 60 项测试或本次生效成功宣称整体完成。未提交、未推送。

## 19:49～19:59 续验：新增节点、完成历史及任务树同步

以下为上一节之后取得的新证据，不改写上一轮的验证范围。

- 在同一测试项目的任务子画布，通过“从资产库复制任务（可选）”复制标准手工任务，独立命名为“工前准备资料核对”/`PREP_CHECK`；保留 `TASK_NATIVE` 办理及本轮提交条件，不修改原阶段完成条件。
- 保存、预览、生效为 v3（`2099465658003566593`，19:51:20）。预览显示“新增独立执行”，项目任务列表从两项增加到三项；原阶段、现场工勘及需求分析实例未重建。
- 通过实际“任务办理”侧栏执行开始、提交、规则校验完成。开始 19:52:01，提交 19:52:25，完成 19:52:39；新任务 ID `2099466020617924610`，执行 ID `2099466020718587905`，第 1 轮、`DONE`、进度 100%。没有直接写数据库模拟完成。
- 从 v3 创建新草稿，修改任务当前名称为“工前准备资料复核”；预览明确“已完成历史保留”“保留第1轮及原冻结版本”。v4（`2099466539470106626`）于 19:54:08 生效。
- 生效前后直接比较该完成执行的**整行内容**以及 v3 的完整设计文档、执行快照，完全相同。实际历史窗口仍显示原冻结名称“工前准备资料核对”、第 1 轮及原提交/完成时间。
- 此时发现缓存的任务树未响应计划生效，手动刷新后才显示当前新名称。修复 `ProjectTaskPanel.vue`：监听项目 ID/版本更新工作区和任务树；旧请求不得覆盖新工作区或发送过期版本事件。复用 Vue `watch` 与现有刷新令牌，不重建全局状态系统，不重挂载组件来清除用户状态。参考 [Vue Watch Source Types](https://vuejs.org/guide/essentials/watchers.html#watch-source-types)。
- 新增 `ProjectTaskPanel.runtime.spec.ts`，覆盖同项目版本变化刷新、旧版本请求先返回、旧项目请求后返回三条实际行为；既有 `Z06StageProgression.runtime.spec.ts` 的测试装配补传该组件已有必需 `project` 属性，不修改其业务断言。
- 修复后再创建并生效 v5，将当前名称改为“工前准备资料复核（调整）”。**没有点击任务刷新按钮**，列表自动显示新名称及完成 100%；完成执行整行和 v3 冻结定义再次逐值相同。
- 现场工勘、需求分析仍为待分配，阶段仍进行中；本次未替用户办理这两项业务，也未使项目自动关闭。

前端验证（19:58:20）：

```powershell
pnpm exec vitest run --config vitest.pms-file.config.ts src/views/pms/project/project-master-detail/components/ProjectTaskPanel.runtime.spec.ts src/views/pms/project/project-master-detail/components/Z06StageProgression.runtime.spec.ts src/views/pms/project/project-master-detail/components/projectPanelVisibility.runtime.spec.ts --maxWorkers=1
pnpm exec eslint src/views/pms/project/project-master-detail/components/ProjectTaskPanel.vue src/views/pms/project/project-master-detail/components/ProjectTaskPanel.runtime.spec.ts
```

3 文件、11 项测试通过；最终 ESLint 无错误/警告。仅前端刷新逻辑有新实现修改，无新增后端变更，上一节 60 项后端测试证据仍适用，未为前端改动重复运行后端全套。

剩余边界：本轮补齐了新增手工任务与已完成历史下的元数据改版路径；没有验证节点删除、已开始绑定变更拒绝、选择性返工或真实拖拽。代码中独立新建任务入口仍保留旧 `S0` 限制，后续需按已批准的自定义阶段/项目计划版本语义继续整改，不能据旧测试通过宣称这部分符合最终目标。未提交、未推送。

## 20:00～20:06 续验：选择性返工及已办理节点保护

仍使用 `PJT2026000017`，没有操作用户原项目 `993109130058`。

### 选择性返工闭环

- 打开“选择性返工”，只选择 `PREP_CHECK`；原阶段正在运行、其他两项任务未开始，均不能被选为返工节点。
- 预览仅列出所选任务“第 1 轮 → 第 2 轮”。填写原因后确认返工，20:01:15 产生一条 `PROJECT_REWORK / SUCCESS` 审计，专用重评事件已消费，重试次数 0。
- 原任务实例 ID 不变，新执行 `2099468516799229953`、新绑定 `2099468516723732481`、第 2 轮，绑定当前有效计划 v5（`2099467676147462146`）。没有创建多余阶段轮次。
- 新轮次初始状态 `PENDING`，开始、提交、结束及结果快照均为 NULL；任务列表自动显示待分配、进度 0%。没有复用上轮提交或自动完成。
- 原第 1 轮结束结果保留、current 指针关闭。对原四个执行的 ID、版本归属、绑定、轮次、状态、准入/开始/提交/完成事实及结果快照直接逐值比对，返工前后完全一致（不将应变更的当前指针和更新元数据纳入不可变事实比较）。
- 在统一任务界面重新开始、提交、校验完成第 2 轮：开始 20:01:46、提交 20:02:11、完成 20:02:18。
- 真实历史窗口同时显示：第 1 轮“工前准备资料核对”，19:52 提交/完成；第 2 轮“工前准备资料复核（调整）”，20:02 提交/完成。重新完成后再次比对原四个执行办理事实，仍未改写。

### 换绑与删除拒绝路径

- 从 v5 创建本项目 v6 测试草稿；尝试将已办理的 `PREP_CHECK` 从手工办理改为已发布现场工勘页面，并选择有效的“工勘记录已确认”完成事实。只改草稿，不创建或办理工勘业务。
- 保存编辑版本 1、点击影响预览：返回 `STARTED_BINDING_IMMUTABLE / 已开始工作不得改变业务绑定`，生效按钮禁用；不是因为业务页面或完成条件本身无效而阻断。
- 随后在同一测试草稿确认删除该节点，保存编辑版本 2、重新预览：返回 `STARTED_NODE_DELETE_FORBIDDEN / 已开始节点或包含已开始工作的阶段不能直接删除`，生效按钮禁用。
- 数据库确认 v5 仍为 `EFFECTIVE`、v6 为 `DRAFT`；原任务仍存在且为 `DONE`，第 1/2 轮历史及时间保留。仅删除了草稿中的节点，没有删除运行任务或业务记录。
- **v6 编辑版本 2 故意保留为不可生效的负向验收草稿**，供查看删除保护；当前有效计划不受影响。未绕过禁用按钮发起生效请求，不能将本项表述为浏览器已实测绕过前端的直接 API 攻击。

本轮没有实现代码修改，取得的是新的真实浏览器及只读数据库证据，没有重复运行未变化的单元测试。仍未提交、推送或清理历史。

继续项：尚需整改独立任务入口的旧 S0 限制及其与版本计划的关系；办理方式编辑器目前只提供业务页面/表单选项，已保存外部绑定缺少显式切回手工办理的入口，需补齐正常编辑能力。阶段直接办理、结束阶段的必要返工轮次、共享业务对象、决策表及完整并行运行等仍不因本次返工成功而视为完成。

## 20:17～20:22 续验：业务任务切回手工办理

- `TemplateContentEditor.vue` 的任务侧栏补充“切换为手工办理”。复用现有 Element Plus 2.13.7 的 [MessageBox.confirm](https://element-plus.org/en-US/component/message-box.html#confirm)，取消不修改，确认后清除该节点待保存业务选择，为当前节点创建独立的 `TASK_NATIVE_STATUS / DONE` 完成规则，保留准入、退出及权限。原共享规则不修改，规则仍归版本管理；没有增加绑定类型、接口或规则发布步骤。
- 确认弹窗明确提示替换整个完成条件及当前节点名称。确认期间切换草稿、选中节点或只读状态时，不再应用过期操作。阶段仍使用原阶段办理入口，不误用 `TASK_NATIVE`。
- `taskManualHandling.runtime.spec.ts` 新增 6 项行为测试：独立规则及保存重开、取消及未保存业务选择清除、三种过期确认、阶段/只读不提供入口。与原业务绑定 8 项、项目计划编辑 6 项合计 **20 个不同测试通过**，不是将两次命令重复执行的 6 项相加。ESLint、变更空白检查通过。
- 新建独立测试项目 **PJT2026000018 / 993109130061**（工前准备办理方式切换验收20260914），仍从公共模板 `993009001597` 的已发布版本 `993009001609` 创建。Docker 内再次核对 `MYSQL_DATABASE=npdms_test`，只在此隔离环境通过真实浏览器写入。未操作项目 15～17 的业务或草稿。
- 对未开始的“需求分析”确认切为手工，保存为项目草稿 v2 编辑版本 1；关闭并重开后仍显示“任务自身办理 / 以本轮真实办理结果为依据”。影响预览通过，显示延续第 1 轮、不自动返工。
- 正式生效后，项目 v2 `2099473130802065410` 为 `EFFECTIVE`，v1 `2099473011239235585` 为 `SUPERSEDED`。数据库当前契约：现场工勘仍为 `BUSINESS_COMPONENT`，需求分析为 `TASK_NATIVE`，新契约 `2099473542854684673`。公共模板未发布新版本。
- 在统一“任务办理”界面完成需求分析任务 `2099473010979188739` 的真实开始、提交、校验完成：执行 `2099473011272790018`，第 1 轮，开始 `20:21:46.717`、提交 `20:21:57.123`、完成 `20:22:12.683`，最终 `DONE / 100%`。开始后仅显示提交操作，提交后才出现“校验并完成任务”；没有通过页面切换或计划生效直接完成。现场工勘仍 `PENDING_ASSIGN`，阶段仍 `ACTIVE`。
- 已开始绑定不可改的服务端逻辑未改动，继续适用上一节已实测的拒绝证据。本轮不是新增全量权限或直接 API 绕过验收；未重新打包后端、未做全量类型检查、未提交或推送。

发现的后续问题：原示例模板保存的两个任务坐标约为 `(265,143)` 和 `(238,160)`，使任务图形重叠。第一次点击“现场工勘”实际选中“需求分析”，确认框显示了正确的实际选中名称，已取消，再明确选中需求分析执行本次验收。v1/v2 的布局相同，问题不是本次切换新增；后续需要改善重叠节点的定位/选择和示例画布，不能把本次点击记录当作真实拖拽已验收。不自动改写已冻结布局或历史。

## 20:25～20:34 续验：官方布局插件与重叠节点整理

- 新增精确依赖 `@logicflow/layout:2.1.5`，使用官方 Dagre 插件；其 peer 要求 `@logicflow/core:^2.2.5` 与当前核心匹配，Apache-2.0。参考 [官方布局说明](https://github.com/didi/LogicFlow/blob/master/packages/layout/README.md)。只注册 Dagre，不启用 ELK，不自研排版算法；ELK 作为官方包的传递依赖由 pnpm 安装。
- `TemplateFlowCanvas.vue` 增加“整理当前画布”：仅编辑态可用，空画布禁用。调用插件后将坐标交给原 `move` 处理，跨阶段引用仍使用当前画布的独立位置键；不保存插件生成的另一份节点或规则来源，不自动发布、生效或返工。布局渲染期间抑制依赖新增事件，避免将图形重绘解释为业务连线操作。
- 视觉验收发现原 `setTheme` 中的宽高不能设置节点模型尺寸，导致长编码超出边框。按照已安装官方 `RectNodeModel.setAttributes` / `DiamondNodeModel.setAttributes` 使用 `properties.width/height` 或 `rx/ry`，保留原计划尺寸；启用原生 `nodeText.overflowMode=autoWrap`。没有自定义文本换行算法或另造节点类。
- 新增 `canvasLayout.runtime.spec.ts` 4 项：真实 Dagre 算法分离重叠节点、只读及过期点击不写入、空图不运行、插件调用异常不写位置/依赖。只替代 LogicFlow DOM 渲染器，布局计算使用实际安装库；检查依赖身份保留、跨阶段引用仍不可作为连线目标。
- 扩大运行原设计器测试时，`configuration.runtime.spec.ts` 因导入请求层后加载浏览器缓存而在 Node 环境报 `window is not defined`。按相邻测试已有方式 mock 请求层，只隔离未使用的网络依赖，原业务断言不变。随后 7 文件、38 项测试通过；尺寸修正后复跑受影响的设计器 6 文件、30 项及布局 4 项均通过，不将重复次数相加。
- pnpm 自动生成锁文件，除 4 个新增包记录外也归并了部分 CodeMirror/FEEL/min-dash/lodash 传递依赖引用（49 行增加、47 行删除），不是手写锁文件或新增直接版本升级。`pnpm install --frozen-lockfile --offline --ignore-scripts` 成功，确认清单/锁文件一致。原有 BPMN properties-panel、video.js、echarts peer 警告仍在，未以布局需求扩大升级这些模块。
- 依赖审计：镜像源不提供 audit 端点，改用官方 registry 完成查询，生产依赖统计为 low 6、moderate 42、high 33、critical 0；这不是“全依赖安全”通过结论，本轮不进行全前端依赖整改。

真实浏览器仍使用隔离 `npdms_test` 中的 **PJT2026000018 / 993109130061**：

1. 有效计划 v2 只读时没有整理按钮。从当前计划创建 v3 草稿 `2099475414030168065`，进入工前准备任务子画布，主动整理后分别点击现场工勘和需求分析，侧栏标题均准确对应，原先的重叠误选不再发生。
2. 最终保存为 v3 编辑版本 2；关闭重开后任务中心坐标分别为 `(206,156)`、`(458,156)`。浏览器截图确认长编码在节点内换行，没有越界覆盖其他任务。
3. 数据库逐值比较 `JSON_REMOVE(designer_document,'$.layout')`：草稿与有效版本完全一致。阶段主画布坐标不变；v1/v2 冻结布局仍保留原值。三个执行的状态、轮次和实际办理时间与上一节相同，需求分析仍第 1 轮 `DONE`、现场工勘仍 `PENDING`。
4. 影响预览通过并显示“没有执行节点或收口规则变化”。**只保存布局草稿，未生效 v3**，运行仍使用 v2。未修改公共模板或其他项目，未提交、推送、重启后端、全量构建或全量类型检查。

本轮取得的是实际布局、准确选择、保存重开及不改变运行的证据，不将其表述为鼠标拖拽/连线的浏览器验收。原模板重叠布局不自动覆写；后续可在新模板草稿显式整理。独立任务入口旧 S0 限制、阶段直接业务办理、完整并行/共享对象/决策表等仍按专项原目标继续。

## 20:46～20:56 续验：任务结构统一进入计划及 S0 实际办理

- 复核旧 `ProjectTaskCommandService.create` 会直接插入任务、路径和契约，但不创建计划节点及执行轮次。对已有有效计划的项目，旧 create、move、addDependency 现在在持有项目锁并完成权限校验后拒绝结构写入，返回 `PROJECT_TASK_PLAN_CHANGE_REQUIRED`，引导进入项目计划草稿。未增加迁移、兼容层或新的审批。
- `ProjectTaskPanel.vue` 删除旧直接创建表单，改用“新建／调整任务”打开现有 `ProjectPlanEditor`。服务端仅对 ACTIVE 项目、具有 `pms:project-plan:manage` 及项目完整 EDIT 范围的用户返回 `MANAGE_PLAN`，不另加项目经理身份；有计划的任务不再返回 MOVE。办理、指派、进度及资料编辑仍走原正式命令。
- 无有效计划的旧后端命令未在此单元重写，其原 S0 校验仍存在；新的项目任务路径不调用这些命令。不能将本项表述为全仓已删除所有固定阶段实现。前端原 CREATE 表单不再保留，未建设两套新建入口。
- `ProjectTaskCommandServiceTest` 新增 3 项：计划项目直接创建、移动、加依赖均不写结构或版本，移动/依赖拒绝保留审计。`ProjectTaskQueryServiceTest` 新增 3 项：非项目经理的合法计划授权、缺权限/缺 EDIT 范围/关闭状态拒绝、正常办理保留且无 MOVE。两类共 **34 项通过**。首次查询测试失败是 Mockito 重设 `thenAnswer` 时以空参数触发旧桩，改为 `doReturn` 后复跑通过，未降低业务断言。
- 前端旧 Z06 的两条“排除 S0 创建”断言已被新的 S0/自定义阶段计划入口正向测试替代；原 4 条阶段推进保护测试保留。`ProjectTaskPanel`（6）、Z06（4）、`ProjectPlanEditor`（6）、`projectPanelVisibility`（2）共 **18 项通过**；目标文件 ESLint、格式及空白检查通过。没有运行全量前端类型检查或构建。
- 宿主机后端打包通过；核对旧 Java 进程的 JAR/59280 参数后停止并重新启动。固定测试启动脚本再次校验 `MYSQL_DATABASE=npdms_test`，主从数据源均指向测试 23316、Redis 26379。20:51:33 启动成功，Java PID 29748（仅为本次证据），日志 `.run/task-plan-entry-backend.log`。无清库、历史覆盖、提交或推送。

真实浏览器仍只修改 **PJT2026000018 / 993109130061**，没有修改用户项目 `993109130058` 或项目 17 的负向草稿：

1. 更新服务后，任务页出现“新建／调整任务”，点击直接打开既有 v3 草稿。通过可选资产复制“工前准备”，独立改为 **S0 / 工前补充准备**；其任务子画布复制手工任务并改为 **S0_PREP_CHECK / 工前准备资料补核**，保留本轮实际提交完成条件。
2. 保存为 v3 编辑版本 3，运行仍只有原 3 个执行。影响预览列出新增阶段和任务，均为“新增独立执行”；确认后生效 v3 `2099475414030168065`，任务树版本 0 → 1，导航自动新增 S0 和 1 项任务。
3. 新阶段实例 `2099481681675104257` / 执行 `2099481681729630210`，新任务实例 `2099481681637355521` / 执行 `2099481681838682113` / 契约 `2099481681830293505`，都绑定计划 v3、第 1 轮。S0 于 20:54:00.280 自动准入，与 PREP_WORK 同时 ACTIVE，不因编码 S0 被排除。
4. 在统一“任务办理”侧栏开始新任务（20:54:44.444），随后通过阶段行真实提交本轮阶段结果（20:55:10.263）。此时阶段虽已提交但仍 ACTIVE、ended_at=NULL；任务仍 ACTIVE 且未提交，没有隐式丢弃已开始工作。
5. 再回任务侧栏真实提交（20:55:31.015），才出现“校验并完成任务”；完成于 20:55:39.977，DONE/100%。既有规则重评于 20:56:00.332 自动结束 S0；原 PREP_WORK 仍 ACTIVE、现场工勘仍未开始，项目仍进行中。刷新仅读取状态，没有再发阶段结束命令。
6. 对原已完成需求分析执行 `2099473011272790018`，生效前后及新阶段结束后两次比较数据库整行，完全一致，仍属于 v2、第 1 轮，保留原提交/完成时间及 LiteFlow 结果。没有改写旧执行身份或完成历史。

本次增加的是项目任务入口、版本/轮次、S0 正向办理、阶段先提交等待已开始任务及并行分支保留的实际证据。直接绕过 UI 的三个旧命令拒绝由服务测试证明，本轮未另发浏览器外 HTTP 攻击请求。阶段绑定原业务页面的真实完成、结束阶段的必要返工轮次、共享业务对象、真实拖拽连线及 DMN 等仍按完整专项目标继续，不能据此宣称整体完成。

## 20:57～21:03 续验：已结束所属阶段的必要返工轮次

- 继续使用项目 18 有效计划 v3。在“选择性返工”只勾选 `S0_PREP_CHECK`，未勾选阶段或需求分析。预览自动加入 S0，标明“必要所属阶段”，两个目标均从第 1 轮到第 2 轮；其他节点未进入返工目标。
- 20:58:38 确认返工，新增阶段执行 `2099482960287707138`、任务执行 `2099482960396759042`；实例不变，冻结契约分别更新为 `2099482960224792578`、`2099482960325455873`，均绑定当前 v3。新执行初始 PENDING，准入、开始、提交、结束及结果快照均 NULL。
- 返工后对原 5 个执行的身份、版本归属、绑定、轮次、状态及全部办理事实逐值比较，完全一致；仅当前指针按正常返工退休，不将当前指针列入不可变事实比较。需求分析仍为 v2 的第 1 轮完成结果，原 PREP_WORK 及现场工勘不重建。
- 专用返工 Outbox `2098331630163513434` 21:00:00 投递成功、重试 0，S0 第 2 轮自动准入。未通过刷新或直接数据库更新模拟激活。
- 统一任务侧栏重新开始（21:01:03.847）、提交（21:01:16.210）、校验完成（21:01:28.517）第 2 轮。开始后没有完成按钮，提交后才允许校验完成；任务再次 DONE/100%。
- 任务第 2 轮完成后，阶段第 2 轮仍 ACTIVE、submitted_at/ended_at=NULL，页面仍提供“提交办理结果”。21:01:30 已消费的重评事件读取当前事实，未拿第 1 轮的阶段提交结果放行。
- 在明确显示“第 2 轮”的阶段提交框填写新的办理说明并提交；阶段提交 21:02:33.595、结束 21:02:33.631。此后再次逐值比较原 5 条执行事实，完全不变。
- 真实历史页面同时展示 S0 与 S0_PREP_CHECK 各两轮独立的提交/结束时间；需求分析仍只有第 1 轮，现场工勘未开始，原阶段继续进行、项目未关闭。

本段没有改动返工实现，取得新的实际业务证据；未为未变化代码重复运行原单元测试，也不把两轮手工办理等同于阶段绑定业务页面的完成验收。

## 21:03～21:09 续修：阶段资产复制的独立节点身份

- 本次实际草稿中 S0 来源为工前准备预设，其节点键仍为 `stage:S1`。追踪 `designerAssets.ts` 确认阶段复制把源阶段编码当作新节点身份；改名后再次复制同一预设会产生相同 nodeKey，侧栏可能选中旧节点，违反“复制后独立、资产来源不参与运行身份解析”。任务和交付件复制已经使用原生 UUID。
- 最小修复：阶段复制同样使用 `stage:${crypto.randomUUID()}`。只影响新复制节点，不重建、迁移或覆盖已有/冻结节点，不引入库、包装器或另一份规则来源。
- 新增 `designerAssets.runtime.spec.ts` 两项行为测试：同一预设改名后再次复制的独立身份、保存重开身份稳定、来源引用保留及嵌套权限/规则不互相修改；未发布或引用缺失仍拒绝复制。与原图模型 6 项、计划编辑器 6 项共 **14 项通过**，目标 ESLint 与空白检查通过。无后端变更，不重启后端或重跑后端全套。
- 首次真实复制仍使用页面中已加载的旧模块，侧栏错误选中旧 S0；该次副本未保存。按本地浏览器开发指引重新载入页面，重新打开 v4 编辑版本 0，未修改有效计划或历史。
- 在 v4 草稿中先复制预设并改成 `PREP_COPY_A / 工前准备复制甲`，再复制同一预设改成 `PREP_COPY_B / 工前准备复制乙`，整理画布并保存。v4 `2099484513283289089` 编辑版本 1 保持 DRAFT；v3 仍 EFFECTIVE。
- 数据库保存结果：甲键 `stage:c0e99e57-ebd9-42b8-a74f-8819e1f10b06`、乙键 `stage:e4d7b5ab-19fe-4419-9b32-204311181363`，两者 source.definitionRevisionId 同为 `993009001019`，但 nodeKey 和 completionRuleKey 独立。原 S0 键 `stage:S1` 未改，草稿 4 个阶段身份全部唯一。
- 关闭重开后 4 个节点可见，分别选择甲、乙，侧栏名称准确；重新适应画布后乙可正常选择。服务端影响预览通过，仅列出甲、乙“新增独立执行”。**只保存并预览，未生效 v4**，运行仍为原 7 条执行、S0/资料补核第 2 轮已完成，其他分支不变。

本轮未提交、推送或清理测试历史。下一步仍需按专项完整目标推进阶段直接业务办理、共享对象、原模块写入口、决策表及其他尚未验收的分支；本段的独立复制修复不代表这些能力已完成。

## 21:16～21:31 续修：阶段与任务共用执行上下文校验入口

### 已实现范围

- 代码追踪确认阶段业务此前只有 `StageBusinessViewProvider.inspectStage` 页面查询；正式执行校验 API 只支持任务。`ProjectStageCompletionService` 仍把所有绑定节点挡在 `submittedAt == null`，而阶段手工提交服务明确拒绝外部业务绑定。因此阶段直接业务闭环尚未完成，不能靠增加手工确认解决。
- 将现有 `ProjectTaskExecutionApi` 及实现重命名为 `ProjectNodeExecutionApi`，原任务调用方直接改接同一入口，不保留旧 API 包装器。原任务 DTO 和已冻结 JSON 结构不改；阶段使用独立的强类型查询/结果，包含项目、阶段、契约、计划、执行实例及轮次版本。
- 在既有节点执行 Mapper 增加阶段上下文的普通/加锁查询：精确绑定租户、项目有效计划、阶段实例、当前契约及 `STAGE` 执行，节点键按二进制比较；不借用子任务、来源资产或单活动阶段字段。
- 阶段办理可用性要求项目、阶段和本轮执行全部 ACTIVE；写入复核采用原有项目优先锁顺序，并拒绝改版、返工和版本变化后的旧上下文。仍要求原业务模块自行校验权限、数据范围和业务状态。锁复核使用 Spring `MANDATORY` 参与 Owner 写事务，不能无事务调用，依据 [Spring 官方事务传播定义](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/transaction/annotation/Propagation.html#MANDATORY)。
- `ProjectStageBusinessQueryService` 将阶段执行上下文交给原 Owner；缺少有效执行或执行不可写时，只允许 QUERY。冻结视图仍可用于查看已结束历史；活动阶段缺少执行时返回可恢复诊断 `STAGE_EXECUTION_UNAVAILABLE`。需求分析阶段视图同步收紧动作投影。前端 API 类型补充对应执行上下文。
- 没有新增数据库表、迁移、依赖、独立规则系统或审批；本轮没有将执行可用性冒充业务完成，也没有移除阶段完成证据保护。

### 针对性验证

- 为消除 Java 类重命名留下的旧构建产物，正常 Maven clean 仅清理 project、project-api、engineering 的生成 target；未删除源码、业务数据或测试历史。
- `ProjectNodeExecutionApiImplTest` 12、`ProjectStageExecutionApiTest` 10、`ProjectStageBusinessQueryServiceTest` 14、`ProjectTaskBusinessAssociationServiceTest` 8、`ProjectTaskBusinessServiceTest` 27、`ProjectTaskPlanCompletionServiceTest` 10、`RequirementAnalysisExecutionBindingTest` 6、`RequirementAnalysisBusinessObjectProviderTest` 12、`SiteSurveyTaskBusinessObjectProviderTest` 16，共 **115 项**通过，失败/错误/跳过均为 0。包括缺失上下文、不可写状态、换租户、返工后旧实例、版本冲突及原任务消费者回归。
- 新增 `ProjectStageExecutionMapperAcceptanceTest`，使用实际 MyBatis XML 和固定 MySQL `npdms_test:23316`，显式指定项目 18 的浏览器夹具；仅 SELECT / SELECT FOR UPDATE，随后回滚，无数据写入。**1 项通过**，没有跳过。
- 实库活动阶段：PREP_WORK / `994009100182`，契约 `2099473011008548866`，计划 v3 / `2099475414030168065`，执行 `2099473011256012802`，可写。已结束 S0 第 2 轮：阶段 `2099481681675104257`，契约 `2099482960224792578`，执行 `2099482960287707138`，不可写；第 1 轮旧契约 `2099481681717047298` 不返回当前上下文。错租户、错项目及错阶段-契约组合均返回空。
- `stageBusinessPanel.runtime.spec.ts` **4 项**通过；变更的前端 API 文件 ESLint、相关路径 `git diff --check` 通过。
- 整包第一次构建在平台模块测试编译报告已有业务视图/表单类找不到；源码与生成 class 实际存在，`javap` 可读取。未修改平台源码，新构建进程用相同 `mvn -pl yudao-server -am package -DskipTests -DskipITs=true -q` 复核退出码 0。该结果证明本次完整构建成功，不把首次失败解释为已定位的源码缺陷。

### 运行与浏览器边界

- 新 JAR 生成时间 21:27:35。同工作区另一宿主进程于 21:28:05 启动，PID 28736，21:28:41 成功监听 59280。随后本任务尝试启动的重复进程因端口占用退出；确认现有进程的命令、启动时点及 JAR 后复用它，没有再次停止共享服务。
- 真实浏览器重新加载项目 18：PREP_WORK 仍为第 1 轮进行中，S0 为第 2 轮已完成。打开“项目任务 → 现场工勘”，共用“任务办理”界面正常读取；待分配任务显示“开始任务”，业务区域显示“暂无自动关联的业务记录”，原业务视图只提供查询。未开始、提交、关闭任何任务，未生效已有 v4 草稿。
- 本段是统一上下文接入及原页面加载回归，**不是阶段直接业务创建/完成的浏览器验收**。仍需接通原模块按阶段执行上下文办理、业务记录关联至阶段本轮、Owner 真实结果进入统一规则判断，再完成阶段直接办理及返工的真实闭环。完整专项目标保持未完成，未提交或推送。

## 21:51～22:00 续验：轮次关联回归、阶段办理保护及任务面板精简

- 对最新轮次自动关联、Owner 结果及完成判断运行 7 个测试类，共 80 项通过，失败/错误/跳过均为 0；包括延迟事件读取最新执行、旧关联排除、重复事件不新增关系及未知结果不放行。本段未重新进行自动关联的浏览器写入验收。
- 为进行中的阶段直办需求分析接入补充阶段绑定事实、执行开始标记和业务冻结身份测试。`ProjectStageExecutionApiTest`、`ProjectStageHandlingMapperTest`、`ProjectStageWorkBindingFactApiTest`、`RequirementAnalysisExecutionBindingTest` 通过。真实 Mapper XML 在独立 H2/Spring 事务中证明开始标记随失败回滚、重复写入不覆盖首次开始、旧执行历史不变；没有使用应用配置或外部数据库。阶段直办的 Owner 全流程及浏览器验收仍未完成，不能将这些测试当作闭环完成证据。
- 按用户当前浏览器批注，公共任务办理组件取消重复“业务办理”标题、自动关联说明、关联清单及空状态。原业务视图、服务端自动关联、权限、来源交付件与未保存内容保护保留。刷新动作移到唯一的“任务业务办理”标题右侧，仍调用原受保护的刷新方法。
- `TaskBusinessPanel.runtime.spec.ts` 与 `flowPanel.runtime.spec.ts` 共 23 项通过；含空关联时直接显示 Owner 页面、刷新失败保留编辑内容、自动关联记录变化时取消切换不丢内容。ESLint 无错误，已有格式及测试桩 prop 类型警告保留；新增按钮的格式警告已修正。相关前端路径 `git diff --check` 通过。
- 在真实浏览器项目 18 / `993109130061` 的“现场工勘 → 任务办理”弹窗确认：重复关联面板数量为 0；标题与刷新按钮同排，按钮位于右侧。点击“刷新业务结果”后原工勘查询与表格仍可用，任务仍待分配、进度 0%，没有开始、提交、关闭任务或创建业务记录。前端热更新生效，未重启共享后端；弹窗保持打开交还用户。

## 22:03～22:10 续验：阶段直办的 Owner 命令接入及入口一致性

- `RequirementAnalysisStageCommandTest` 调用真实需求分析应用命令和 `RequirementAnalysisExecutionBinding`，外部 API 与持久层使用隔离 mock。证明阶段创建、保存与完成沿用原 Owner 路径；冻结阶段执行身份而非虚构任务；原业务权限不足、过期阶段上下文和表单校验失败不能完成；新阶段执行可创建新的业务修订草稿，已完成来源对象和其冻结快照不改写。这是应用命令测试，不是实库业务闭环。
- 需求分析查询显式指定阶段时只读取该阶段绑定；阶段不可用不会退回项目下可用任务。阶段执行传输保留长整型 ID 字符串，初次创建与修订仍沿用原接口、版本头和幂等头，不额外创建业务命令入口。
- 新接入发现的实际 UI 回归已修复：`watch(() => [身份...])` 在同项目对象重建时误触发表单重载，权限更新可覆盖未保存内容。需求分析页面和阶段容器改为多源身份监听；项目元数据、权限或执行版本刷新不重建 Owner 页面，实际执行实例变化仍触发切换。对应未保存内容、查询次数和组件不卸载断言通过。
- 后端 6 类共 **45 项**通过，失败/错误/跳过均为 0：`ProjectWorkBindingFactApiImplTest` 15、`PreparationControllerTest` 4、`RequirementAnalysisDynamicFormCommandServiceTest` 1、`RequirementAnalysisDynamicFormQueryServiceTest` 8、`RequirementAnalysisStageCommandTest` 5、`RequirementAnalysisBusinessObjectProviderTest` 12。初次运行暴露的控制器反射签名和测试 mock 覆盖问题已修正，未降低原权限断言。
- 前端 `BusinessView.runtime.spec.ts`、`stageBusinessPanel.runtime.spec.ts`、`RequirementAnalysisDynamicForm.runtime.spec.ts`、`stageContext.runtime.spec.ts` 共 **45 项**通过。ESLint 无错误，仅阶段容器既有格式和测试桩 prop 类型警告；相关受控修改路径 `git diff --check` 通过。
- 用户指出任务职责显示不一致，定位为 `ProjectTaskPanel` 遗留默认 `showResponsibilities: true` 经抽屉传入共用页面。将该默认值统一为 `false`；没有删除任务职责业务数据或接口。入口、抽屉及交付流程共 **14 项**测试通过，两个变更文件 ESLint 无警告或错误。
- 真实浏览器项目 18 的同一现场工勘任务弹窗已无“任务职责／调整历史／设置负责人／设置执行人”；任务说明编辑、状态操作、右侧业务刷新和原工勘页面仍在。没有操作业务状态，弹窗继续保持打开。
- 阶段直办的后端变更尚未重新打包部署，本段没有声称阶段业务创建/完成的浏览器验收。阶段本轮业务关联、Owner 结果进入阶段完成规则、节点授权覆盖及完整实库返工仍需继续；整体专项目标保持进行中，未提交或推送。

## 22:26～22:41 续修：阶段自动关联、Owner 完成事实与独立阶段求值

- 阶段和任务共用 `ProjectTaskBusinessAssociationService` 的候选分页、关联维护及历史保护逻辑。新增明确 `stage_id`，阶段不伪装成任务；查询必须且只能带一种节点身份，保留租户与项目范围。阶段接收方绑定有效计划、当前契约、节点键及 ACTIVE 执行实例。
- `V244__stage_business_association_identity.sql` 将 `task_id` 改为可空，加入阶段唯一键、执行索引和任务/阶段互斥约束；没有清库、删除业务表或覆盖旧关联。当前运行库尚未应用此迁移。
- `ProjectTaskBusinessService` 共用自动完成事实收集器；阶段通过 `ProjectNodeExecutionApi` 复核本轮上下文，由原模块 `lockStageCompletionFact` 返回真实办理结果。需求分析已接入同一 Owner 结果读取，未读取私有表单正文，未增加人工确认。没有阶段实现的 Owner 返回不可用，不能据此判为完成。
- `ProjectStageCompletionService` 区分原生提交与真实业务完成，不再要求外部业务节点生成手工 `submittedAt`。BUSINESS_FACT 使用原任务条件求值实现，组合仍走 LiteFlow；完成和退出条件都满足才执行阶段与轮次的版本化结束。已开始的子任务仍必须完成或明确终止。
- 协调器按阶段调用独立事务入口；当前阶段异常记为未知，继续其他独立阶段。没有外层大事务或新增重试层。协调器测试证明异常后的独立分支调用与结果；数据库回滚由独立 SQL 测试验证，本段不将 mock 测试称为完整事务集成验收。
- 完成快照冻结关联 ID、Owner、业务对象和结果版本，不保存业务正文。历史查询继续读取各自冻结计划的规则名称与结果，不伪造手工提交记录。

### 实际验证与修复

- 原测试会话 `93095` 已不存在，旧报告不能证明新增阶段行为。重新运行先发现 `StageCompletionEvidenceTest` 仍用旧构造参数的编译失败；修正调用并新增业务证据序列化验证，没有增加历史兼容包装器。
- 22:31:51 受影响的 9 类测试共 **80 项通过**，失败、错误、跳过均为 0：`ProjectRuntimeCoordinatorTest` 2、`ProjectStageCompletionServiceTest` 9、`StageCompletionEvidenceTest` 3、`PreviousBusinessAssociationsMapperTest` 1、`ProjectTaskBusinessAssociationServiceTest` 11、`ProjectTaskBusinessServiceTest` 30、`StageBusinessAssociationsMapperTest` 1、`ProjectTaskPlanCompletionServiceTest` 10、`RequirementAnalysisBusinessObjectProviderTest` 13。
- 新增阶段结果收集器验证：新记录按当前关联读取 Owner 的草稿/完成结果；不依赖办理时间阈值；旧轮次、混用任务身份及不可用 Owner 均拒绝，原关联证据不改写。保留原任务路径回归。
- 随后 `ProjectExecutionHistoryServiceTest` **6 项通过**，新增已完成阶段业务的冻结规则读取，确认没有手工提交时间、提交人或说明。合计 **86 项**。测试使用 mock、规则引擎 fixture 或独立 H2；没有对运行库执行业务命令。
- `.run/verify-stage-association-v244.ps1` 在现有开发 MySQL 内新建独立数据库 `npdms_stage_association_v244_check_20260914`，仅复制关联表结构并写合成记录，然后执行原始 V244：任务/阶段同数值身份隔离、双空/双有身份拒绝、阶段重复活动关联拒绝、事务回滚、新轮次与旧关联区间保留、原任务每一列不变全部通过。脚本最后仅删除本次新建验证库；`npdms_test` 未修改。
- 受控路径空白检查通过。自审确认使用同一关联与业务事实实现，没有新增规则来源、手工关联操作或业务审批层。

### 打包与运行边界

- 常规 reactor package 在 Spring Boot repackage 阶段无法将默认 `yudao-server.jar` 重命名为 `.original`。实查默认路径此时为 25,366 字节普通包，**不能用于下一次启动**；不能把该次构建报为成功。
- 59280 当前实际由 PID `11220`（22:25:27 启动、`.run/dppms-orders-backend.out.log`）运行，并加载另一条本地验证流程的 integration overlay。本轮没有停止或重启该进程，没有更改其启动参数。后续日志仍有查询成功记录，不据此推断所有未加载代码均健康。
- 为避免继续覆盖在用路径，临时复制 root/server POM，唯一调整 server 模块入口与最终包名，按同一 reactor 构建；独立 package 会话 `43236` exit 0。产物 `yudao-server/target/yudao-server-stage-validation.jar`，22:40:59，192,395,542 字节；manifest 含 `JarLauncher`、`YudaoServerApplication` 和 Spring Boot 4.1.0，含本次 project/engineering 模块。临时两个 POM 已删除，正式 POM 未修改。
- **独立包尚未启动，V244 尚未部署；默认 JAR 的重打包失败仍需在协调本地服务切换时处理。** 不宣称阶段业务创建、自动结束或业务返工的真实浏览器验收。本轮核查浏览器原 tab 已移出会话，当前标签列表为空，没有新建浏览器或登录会话来干扰其他验证。
- 页面接入缺口已定位：`ProjectStageStatusPanel` 当前仅有原生提交入口；应复用已有 `StageBusinessPanel` / 原 Owner 页面提供阶段业务入口，同时保护未保存内容与当前执行上下文。后续还需完成页面接入、联合部署和真实业务/返工验收；整体专项继续进行，未提交或推送。

## 22:43～22:54 续修：阶段入口复用与说明弹窗未保存保护

- 将已有任务抽屉改为 `ProjectNodeWorkbenchDrawer`，直接接收交付流程既有 `ProjectFlowSelection`，阶段/任务均挂载 `ProjectFlowPanel`。原任务调用方和测试直接改接，不保留旧包装器。任务缺失 taskId 不得降级为阶段，阶段也不借用子任务身份；任务职责仍默认隐藏。
- `ProjectStageStatusPanel` 新增每阶段“查看／办理”，保留原生提交入口，业务是否可写仍由原阶段上下文及 Owner 权限决定。标准项目详情和新功能项目详情都传入同一项目对象；组件 key 仅按项目身份固定，项目版本更新改为重新查询状态，不再销毁重建整个阶段区及其未保存业务页面。
- 新增 4 项阶段状态组件行为测试，覆盖准确阶段上下文、不创建手工办理证据、项目版本刷新保持 Owner 页面、业务保存后的刷新失败保持已挂载表单、拒绝不属于已加载项目的阶段。共用抽屉补充明确阶段身份及其离开拒绝、缺少项目/阶段时不挂载。
- 首次测试误用默认 Vite 配置，在收集用例前因 Element Plus CSS 扩展加载失败（0 个用例执行）；随后使用仓库既有 `vitest.pms-file.config.ts`，5 类运行组件用例 **28 项通过**，没有更改测试配置或产品实现来绕过 CSS。
- 真实浏览器项目 18：标准项目详情的 PREP_WORK “查看／办理”打开“阶段办理”，显示工前准备状态、准入/准出说明、原生阶段提示和现场工勘/需求分析两项任务。回到项目任务后，现场工勘仍为 PENDING_ASSIGN、版本 0、进度 0%，原工勘页面和右侧业务刷新保留，无任务职责面板。
- 浏览器负向验证发现真实缺陷：打开任务说明并输入未提交文本后，点击外层关闭按钮会先触发内层通用 Dialog 的遮罩关闭，导致说明编辑器丢失内容而任务抽屉仍打开。未把它当作关闭保护通过。
- 局部修复 `TaskMaintenancePanel`：任务说明 Dialog 绑定 `before-close`，记录打开时 HTML；内容变化或保存进行中时禁止隐式关闭，提示使用保存或明确取消。未修改 Yudao 通用 `Dialog.vue`，未添加另一套保存流程。新增用例通过渲染绑定的关闭回调验证未保存内容保留、无改动正常关闭、保存中和保存失败保护。
- 6 类运行组件测试合计 **36 项通过**（`TaskMaintenancePanel` 8、阶段状态 4、原任务入口/共用抽屉/交付流程/阶段业务其余 24），零失败。3 项直接消费者静态检查通过；同两个静态套件中其余 9 项按名称过滤未执行，不称全套通过。
- 真实浏览器复测：重新打开说明输入同一验证文本，点击外层关闭后说明编辑器及文本仍在；随后主动点击“取消”，未点击保存。任务继续显示“暂无任务说明”、版本 0、待分配 0%，没有办理提交或状态推进。
- 相关生产组件与新阶段运行测试 ESLint 无错误或警告；共用抽屉旧测试桩存在 6 项未定义 prop 类型警告，任务维护原组件存在 2 项原有模板格式/同名变量警告；本次新增测试无 lint 错误。受控改动路径空白检查通过，旧抽屉引用检索为 0。
- 新功能详情首次加载恰逢另一条验证流程重启后台，出现“未选择项目”。检查实际 PID `45776`、22:53:00.846 启动成功日志后重新加载，同一项目及阶段入口恢复，并能打开相同工前准备阶段内容。当前后台使用 `.run/dppms-orders-runtime.jar` 与其独立 integration overlay，不是本专项验证包；本轮没有重启或替换该后台。
- 本轮没有迁移数据库或修改业务历史。V244 在运行库仍未部署，阶段业务的真实创建、自动结束和新轮次办理仍未验收；本段只证明两个页面入口、原任务回归和关闭保护。整体专项保持进行中，未提交或推送。

## 23:02 续修：现场工勘阶段完成事实（不切换共享后台）

- 用户明确选择“暂不切换，继续独立实现”。本轮没有停止、重启或替换 59280 后台，也没有新建运行端口、部署包或执行数据库迁移。
- `SiteSurveyTaskBusinessObjectProvider` 补齐 `lockStageCompletionFact`，与任务共用同一锁定结果读取。先由 PROJ 在现有事务内复核当前阶段执行上下文，再按租户、项目及对象 ID 读取 SOL 真实记录；不伪造 taskId，不另建结果解释器。
- 草稿和驳回均不算完成；已确认和已归档分别输出既有 `SURVEY_CONFIRMED` / `SURVEY_ARCHIVED`。这些事实不等于实施就绪。关联所属轮次继续由统一关联服务校验，Owner 不增加时间阈值、原始记录轮次比对或手工确认；没有修改工勘记录、时间或历史状态。
- 记录缺失、跨租户/项目、错误对象、删除、无效版本或非法状态抛出不可用错误，不能转成 false 后由 NOT 放行。完成读取的负版本拒绝与已有对象查询保持一致。阶段与任务均要求已有事务；多个阶段引用同一记录时，各自返回对应执行上下文的结果版本，没有组件内请求缓存。
- Maven 定向 reactor 测试会话 `65932` exit 0，23:02:27～23:02:53 报告共 **91 项通过**、零失败/错误/跳过：工勘 Provider 36、需求分析 Provider 15、阶段完成服务 9、统一业务服务 31。前一次未加引号的 Maven 测试列表被 PowerShell 解析拒绝，未进入 Maven；引用参数后正常执行，未更改测试配置来绕过失败。
- 本次测试使用 mock Mapper/API 及既有 LiteFlow 内存 fixture，不启动完整应用或访问运行业务库；验证覆盖原任务回归、阶段四种业务状态、过期上下文、非法对象、Owner 结果不可用、多阶段独立证据和事务前置。受控代码/测试路径 `git diff --check` 通过。
- 尚存的真实接入缺口：工勘原创建、更新、删除、确认、驳回、归档及委外关联写操作没有接入节点执行上下文校验；不能只给阶段页面增加写按钮来宣称完成。此轮未开放阶段写操作，亦未将独立菜单约束、阶段业务创建/自动结束/返工标为验收通过。上述写入口及阶段 Owner 页面接入仍需后续实施；浏览器验收等待获准切换专项后台。整体专项未完成，未提交或推送。

## 23:06～23:23 续修：工勘写入口与阶段 Owner 页面接入

- 新增 PROJ 内部 `ProjectBusinessExecutionApi`，由业务 Owner 在既有事务内调用。实现复用有效项目计划快照、当前节点、原任务授权、阶段执行权限和 `ProjectNodeExecutionApi`，没有新规则来源、数据库表或审批流程。按项目先加锁；只接受当前有效计划中的 ACTIVE 业务节点，并检查原业务视图未停用、绑定非只读。不存在模板管理关系的项目仍走原 Owner 权限，受模板管理但缺少有效计划的项目不能据此绕过准入。
- 节点页面携带任务或阶段的明确版本上下文，二者互斥；旧轮次或旧版本失败后不自动替换成最新上下文。独立菜单未指定节点时，仅允许当前唯一可办理绑定；多候选或无候选返回“未找到唯一可办理节点，请从当前阶段或任务入口办理”，不任取首条。共有业务对象不被重新创建，关联逻辑仍由已有自动关联服务负责。
- `SiteSurveyWriteAccess` 保留工勘功能权限及完整项目 MANAGE 范围，再调用 PROJ。原工勘 Service 的创建、更新、删除、确认、驳回、归档、委外关联及委外解除入口均接入；更新按数据库原项目授权，不接受保存请求伪造其他项目。原表单/地点、状态、乐观锁和专用重评 Outbox 路径不变。阶段开始办理在相同 Owner 事务内标记，不伪造手工提交/完成。
- 工勘 Provider 接入 `StageBusinessViewProvider`，阶段和任务复用同一原业务页面及 Owner 权限；阶段查看不创建记录，非可写执行/只读绑定仅给 QUERY。任务上下文响应新增明确执行快照；原页面注册适配与工勘新增/修改/状态操作传递该快照，未改 Yudao 基础接口。
- 工勘表单在打开时复制执行上下文；保存用原观察版本，不随后台换轮或普通元数据刷新静默变更。确认/删除等操作保存点击时的上下文。新轮次变化不会清空本地未保存内容；服务端拒绝时仍保留表单。适配器拒绝混用任务与阶段及跨项目身份，Snowflake 字符串不转数值。

### 验证与明确限制

- Maven 会话 `93232` exit 0（首组 108 项）；补充 Owner 全写入口拒绝与阶段页面权限后，会话 `56945` exit 0；新增明确错误码后，`2649` exit 0，再跑 PROJ 直接消费者 53 项。复用未受后续修改影响的 Owner 报告，本增量共 **117 项不同测试通过**：PROJ 写准入 12、统一业务 31、Controller 3、Host Provider 7；工勘写权限 3、全写入口拒绝 8、地点/原业务 10、状态与 Outbox 6、任务/阶段事实及页面 Provider 37。
- 测试使用 mock Mapper/API、MockMvc 等进程内夹具，不访问运行 MySQL；没有把这些测试称为真实数据库事务回滚、完整应用装配或浏览器业务验收。已有事务传播约束仍须随联合部署验收确认。
- 前端工勘项目上下文、任务业务容器、阶段业务容器 **40 项通过**；注册适配的准确工勘映射和明确执行身份 **2 项通过**。后者按名称过滤，其他 28 项没有执行，不报整套通过。
- 全量运行上述四个前端套件时，已有需求分析 Host 9 项失败：测试 `getCurrent` 返回缺少 `projectId` 的简化工作区和仅包含 preparationId 的草稿，并仍期待额外详情查询；当前 `ProjectRequirementAnalysisPanel.readWorkspaceDetail` 校验工作区/版本项目身份且复用完整版本响应。本增量未修改该生产读取逻辑，没有降低校验或把失败删掉；这 9 项夹具与断言需要后续修复，前端联合回归尚未全绿。
- 新改动的注册适配、任务容器和两个 API 文件 ESLint 无错误/警告。工勘原页面完整 lint 报既有 `readonly` prop 与 computed 同名的 `vue/no-dupe-keys` 一项错误；本轮没有改名或屏蔽规则。相关受控路径 `git diff --check` 通过。
- 委外服务内部调用暂由唯一当前节点定位；多节点共用工勘时的委外快捷入口仍需传递明确执行选择，不能宣称该组合已完成。模板自定义权限策略的完整执行覆盖及其他 Owner 写入口也不在本轮“已完成”范围。
- 用户要求保持共享后台不切换，本轮未停止/重启/部署应用、未迁移数据库、未清理业务数据、未提交/推送。阶段工勘真实新增、自动关联/结束和新轮次办理仍待允许专项包上线后进行真实浏览器验收；整体目标继续进行。

## 23:25～23:41 续修：联测夹具与委外快捷入口执行身份

- 修复 `BusinessView.runtime.spec.ts` 的旧需求分析夹具：工作区明确带项目 ID 和完整版本，当前版本不再期待重复详情查询，显式历史版本仍按精确编号查询；补充跨项目工作区/版本拒绝测试。没有修改生产身份校验。先前 9 项联测失败已解决，首轮相关四套件 72 项全部通过。
- 工勘到原委外页面的跳转仅携工勘、任务/阶段与执行 ID，阶段同时携代码；不在 URL 放业务正文或整份执行快照。目标页通过既有任务/阶段业务上下文 API 重新读取同一执行的最新版本：项目、节点、轮次、Owner、只读或停用状态不符即拒绝，不降级为“取当前任意任务”。这允许工勘保存导致的同轮版本更新，同时拒绝已经返工换轮的旧跳转。
- 委外创建请求和删除请求携明确 `siteSurveyExecution`，原 Service 传入工勘关联/解除操作，继续由上轮统一写入口校验；其他委外操作没有新增审批、状态或手工业务关联入口。创建后返回工勘仍保留同一节点/轮次身份，表单再次打开时重新确认该执行。清空新建委外表单时清除上一业务节点的上下文。
- 快捷 ID 校验改用已有 BusinessView ID 工具，保留 Long/Snowflake 原始十进制字符串；不再拒绝超过 JavaScript 安全整数范围、但仍属于合法 Java Long 的工勘编号。相关工勘领料/外采/换货上下文的原用例一并通过。
- 新增原委外 Vue 页面测试，实际执行其打开、保存及删除处理器；新增节点跳转解析用例覆盖同轮版本刷新、阶段代码编码、跨项目/节点、新轮次、停用、只读、混合或缺失身份。首次页面测试因缺少新增 API mock 和 Vue 模块标记而收集/渲染失败，修正测试依赖桩后重跑，未更改生产逻辑或全局浏览器环境来规避失败。
- 最终前端 8 个套件 **95 项通过**、零失败/跳过：业务宿主、任务/阶段容器、工勘页面、委外页面、执行快捷解析及原快捷相关测试。受控注册/快捷/API/委外页面与测试文件 ESLint 无错误/警告；工勘页面先前记录的 `readonly` 同名 lint 项本轮未处理，不能称全前端 lint 通过。
- 后端原创建/删除上下文传递及写入口拒绝用例通过。新增 `OutsourceSurveyExecutionTransactionTest` 使用真实 Spring 事务代理和每用例独立 H2 内存库，Mapper/工勘调用以 JDBC 答案模拟同事务资源：拒绝执行时回滚委外草稿、删除失败恢复工勘关联、成功创建/删除一起提交均通过。首次失败来自重新设置 Mockito `when(...).thenThrow(...)` 时触发旧删除答案，实际业务命令尚未执行；改用不执行旧答案的 `doThrow` 后，会话 `87972` exit 0、3 项全通过。此前其他 22 项报告仍适用，合计相关 25 项通过。没有把该窄事务资源验证称为全 Owner/MyBatis 业务集成验收。
- 测试库仅在进程内创建并关闭，不访问运行 MySQL；无应用切换、数据库迁移、业务写入、提交或推送。原委外模块其他领域校验/审批完整性、模板自定义权限策略、其余 Owner 入口和真实阶段/返工验收仍需继续，整体目标不标完成。

## 23:44～23:51 续修：节点权限口径收敛及声明校验

- 用户明确选择“原模块逐项授权，不增加节点级门槛”。据此，前述“模板自定义权限策略完整执行”的待办不再意味着需要额外权限解释器：节点权限快照仅为冻结操作说明，不授予权限、不要求同时拥有声明中的全部操作，也不取代原模块逐项权限、数据范围、状态及有效执行上下文检查。既有运行授权路径符合该口径，本轮没有新增运行门槛或改写授权条件。
- 发现并修复独立的发布格式缺口：节点直接编辑的 `policySnapshot` 原先仅检查非空，没有复用可选权限资产的既有格式约束。`TemplateCompiler` 现统一调用 `DeliveryDefinitionPayloadValidator`，拒绝 JSON null、非对象、空/缺失操作集、重复或非法编码、额外字段；即使另有 policyRef，也不能掩盖非法快照。合法内联说明可直接冻结，无需先建资产或单独发布。此次校验仅证明声明结构合法，不声称验证操作编码已经在 Owner 注册。
- 设计及执行模型注释明确上述边界；权限资产表单把“所需操作编码”改为“操作编码说明”，直接说明原模块逐项授权、不增加节点级门槛。保留已冻结历史与原字段，不引入兼容层或迁移。
- 新增编译失败/成功用例和任务、阶段上下文回归：声明含 Owner 未授权的 DELETE 时，不阻止其已授权的 CREATE/PATCH_FORM，也不将 DELETE 加入返回操作；Owner 拒绝 QUERY 时仍拒绝入口。原项目准入、轮次、停用和权限失败用例保留。
- 首轮 26 项通过；扩展回归首次因新增任务测试将 String 快照写成 JsonNode 而编译失败，修正测试字段类型后 Maven 会话 `17548` exit 0。最终六套件 **73 项通过，零失败/错误/跳过**：编译 18、发布依赖 5、资产复制 2、快照 1、阶段业务上下文 15、任务业务上下文 32。测试使用进程内对象和 mock，不访问运行数据库。
- `DefinitionForm.vue` ESLint 无错误/警告，受控变更 `git diff --check` 通过。此次仅修改说明文字，没有新增 UI 操作闭环；未进行新的真实浏览器业务验收。保持共享后台及数据库不变，未打包部署、提交或推送；完整阶段办理和返工验收仍待获准部署专项包，其他 Owner 接入继续按既定计划推进。

## 23:52～23:59 续修：需求分析复用统一业务写准入

- 需求分析原有执行绑定会校验当前轮次，但未调用统一 `ProjectBusinessExecutionApi`，任务/阶段原操作权限及停用业务视图校验没有统一接入。`RequirementAnalysisExecutionBinding.lockCurrent` 现使用准确节点执行选择调用该 API，目标固定为 SOL/REQUIREMENT_ANALYSIS；不退回任意当前节点，不直接读取 PROJ 表，不新增控制器或审批。
- 创建、普通值保存、完成和新版本草稿通过原 `lockManager` 执行该检查；动态表单 Owner policy 的锁定重验通过既有 `lockForWrite` 一并接入。原模块功能权限、项目 MANAGE 范围、项目经理事实、动态表单校验、版本 CAS、审计及专用 Outbox 保留。动态表单 policy 重验原有 MANDATORY 事务约束已核对，SOL 命令继续在原 TransactionTemplate 中执行。
- 原本分开的 beginCurrent/lockCurrent 收敛为一次共享写检查；去除普通值保存和完成在 lockManager 后重复调用的执行重验。共享入口已锁定执行，SOL 不重复锁相同轮次；阶段开始办理由共享入口在当前 Owner 事务内执行，随后读取更新后的执行版本冻结来源。任务直接冻结已校验上下文，已有来源快照不改写。
- 增补创建拒绝、保存/完成/修订拒绝后无业务写入/Outbox、原来源快照不变、任务当前轮次选择、阶段更新后冻结等用例。PROJ 共享入口补充 REQUIREMENT_ANALYSIS 的 BUSINESS_OBJECT 绑定在任务/阶段下的原视图可用与停用拒绝用例，没有为需求分析新增特例。
- Maven 首轮会话 `37680` exit 0；消除重复锁并补齐 PROJ 直接消费者后，会话 `40258` exit 0，六套件 **42 项通过，零失败/错误/跳过**：执行绑定 10、阶段原命令 9、动态表单命令 1、当前动态表单 policy 3、既有文件 policy 5、共享写入口 14。测试为进程内/mock 验证，未访问运行数据库，不称为真实事务回滚或完整浏览器验收；受控变更 `git diff --check` 通过。
- 仍需继续处理需求分析页面显式任务身份传递、同一业务记录跨不同节点办理及相关回归；当前实现仍按原来源节点解析已有记录，不能将本增量称为所有共享对象场景完成。本轮没有修改页面、数据库、运行服务或部署包，没有提交/推送。共享后台不切换，整体专项继续进行。

## 2026-09-15 00:00～00:13：需求分析显式任务入口

- 需求分析适配器现传入并核对明确 taskExecution；有任务 ID 却无执行、跨项目/任务或混合阶段与任务身份时拒绝挂载，不降级为项目默认绑定。工作区查询及任务 Provider 传入 taskId，阶段传 stageId；二者互斥，指定绑定不可用不借用其他节点。原不带节点的独立查询仍保留。
- 创建及修订请求统一使用 `execution: { task }` 或 `{ stage }`，复用 PROJ 公开选择 DTO；前端也与工勘共用结构类型。已直接调整原 PMS 控制器、Service 和消费者，无旧 stageExecution 请求兼容分支。先锁定请求观察到的轮次，再查所选节点的冻结绑定及执行统一写准入；新业务草稿记录该节点来源。原模块授权、业务版本和原完成版本不改写。
- 页面创建请求复制执行上下文；修订在确认弹窗之前固定原版本和执行身份，弹窗期间换轮不把请求悄悄改投新轮次。同一任务/阶段换轮不再触发正文重载，未保存内容保留。此处完成的是创建/修订的明确入口，已有草稿的 PATCH/COMPLETE 及表单文件 policy 仍按记录来源节点解析；跨不同节点办理同一已有草稿尚未完成。
- 后端六套件共 **51 项通过**。首次会话 `4638` 有一项控制器权限测试反射旧 getCurrent 参数签名失败；修正该测试后，会话 `90202` 中控制器 4 项通过，其余未受影响的 47 项报告复用。前端四套件 **66 项通过**，覆盖任务/阶段创建请求、Long 字符串、身份拒绝、原共享容器、换轮保留输入和确认期间固定请求。受控前端 ESLint 及 `git diff --check` 通过。测试使用 mock/自定义 Vue renderer，不是浏览器或数据库业务验收。
- 共享后台仍未切换，未迁移、打包、提交或推送。新版创建请求契约需与专项后端联合部署后再进行真实办理验证；当前共享后台不应被用于验证这套新请求。阶段自动关联/完成、已有草稿跨节点办理及真实返工验收继续推进，不标整体完成。

## 2026-09-15 00:14～00:38：已有需求分析草稿的显式节点办理

- 已有草稿的保存、完成请求携明确任务或阶段执行选择；服务端先重验该轮次和原模块授权，再校验所选节点与原来源具有同一项目、业务目标、表单冻结版本及绑定参数。支持绑定相同业务契约的其他节点办理同一草稿，不新增业务对象、不改写原来源快照或已完成历史。未传选择的独立调用继续通过原来源节点校验，不新增模板节点级权限门槛。
- 工作区按明确节点返回已有草稿的可用操作，节点不可用时只读，不借用其他节点。表单编辑后保留打开时的执行上下文；保存前及完成确认前固定请求，换轮不会将旧编辑悄悄提交到新轮次，失败后保留输入。
- 复用现有动态表单 Owner policy 请求及锁定重验传递请求级上下文；PLT 不解释项目语义，不把上下文混入业务值或持久化为第二份来源。上下文使用防御性 JSON 副本；原事务内授权缓存加入上下文维度，防止同一草稿不同节点串用校验结果。Owner 丢弃上下文会被拒绝，文件接口的无上下文查询不能借用节点上下文缓存。
- 最终 Maven 会话 `42871` exit 0，九套件 **88 项通过，零失败/错误/跳过**：控制器 4、动态表单命令 1、Owner policy 4、工作区查询 10、执行绑定 14、阶段命令 11、业务对象 Provider 15、PLT 实例服务 14、PLT policy 注册表 5。新增用例覆盖同一草稿跨任务/阶段保存完成、业务绑定不一致拒绝、来源保护、不可用节点只读、上下文复制与事务内缓存隔离。测试为进程内/mock 验证，不访问运行 MySQL，不代表应用装配或完整业务事务验收。
- 前端四套件 **66 项通过**，覆盖需求分析请求、业务宿主、阶段与任务容器；旧轮次保存被拒绝时保留本地编辑。受控五文件 ESLint 无错误/警告。业务视图 Provider 构造依赖仅为 PermissionApi，此处未发现所担心的执行绑定循环依赖，未添加延迟注入等规避代码。
- 文件上传、删除尚未携所选节点上下文，不能称共享对象全部操作已完成。共享 59280 后台保持不变，未进行部署、迁移、业务数据写入、提交或推送，也未新增真实浏览器验收。专项仍继续，后续需补齐文件路径并在获准联合部署后验证真实阶段办理、自动完成及返工。

## 2026-09-15 00:39～00:55：需求分析受控附件的所选节点上下文

- 复用原统一文件上传、替换版本、移除附件接口及控件，增加请求级 ownerExecutionContext；不改变文件业务稳定键、原来源、存储归属或历史版本，不新增数据表、文件服务或授权解释器。初始化、上传完成、移除引用及引用集合锁定重验均向动态表单 Owner policy 传递相同上下文，再由 SOL 校验准确节点、轮次、冻结业务绑定和原模块权限。PLT 文件权限及已完成对象不可变限制保留。
- 上传在选中文件时复制上下文，同一上传意图的初始化、完成和重试固定该身份；移除附件在确认弹窗之前固定原引用、版本及节点。新轮次不会悄悄承接旧上传或删除意图，拒绝后不产生前端成功事件，保留文件及重试状态。动态表单仅在复制的运行控件配置中注入上下文，并随页面允许的上下文更新就地调整，不重建正文、不修改设计定义或普通表单值。
- 文件 policy 查询、引用集合查询及其重验 DTO 使用 JSON 副本；原批量事实缓存和事务内 policy 缓存都区分上下文，不能借用其他节点的允许结果。既有幂等请求摘要纳入上下文以识别不同操作意图；未新增摘要算法，也未将整份上下文写入上传会话、审计正文或文件事实。下载、历史查询、无节点的手工动态表单等既有消费者保留原路径。
- 上传完成使用 Spring 原生 JSON multipart part，不自行解析 multipart；控制器 MockMvc 验证实际 JSON 消息转换、完整 Long 执行编号、初始化/移除 JSON body 和非法 JSON 的 HTTP 400。来源：[Spring MVC Multipart](https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-methods/multipart-forms.html)。当前仓库 Spring Boot 4.1.0、Vue 3.5.34，未安装或升级依赖。
- Maven 会话 `49052` exit 0，七套件 **41 项通过**：上传初始化 6、上传完成 2、移除上下文 3、控制器 3、动态表单文件 policy 17、policy 注册表 5、SOL policy 5。覆盖所选任务/阶段传播、陈旧执行拒绝后不写文件/存储、权限不因上下文而授予、共享记录缓存隔离及原只读路径。此前失败分别来自测试仍模拟旧缓存参数、上传夹具引用了其他业务的分类策略，均修正测试后通过，未放宽生产校验。
- 扩展回归会话 `55407` 中 PLT 实例服务 14 项及文件公共契约 6 项通过；文件 Mapper 方法清单 1 项失败。已通过 `git show HEAD` 核对：HEAD 的 FileReferenceMapper 已有 selectActiveSets，而 HEAD 的 exposesOnlyScenarioMapperMethodsAndNoGenericCrud 清单未包含它；本轮未修改 Mapper。保留并记录该既有失败，不宣称全部回归通过。本次合计 **61 项后端通过、1 项既有失败**。
- 前端最终八套件 **79 项通过**，包括文件控件、实际请求编码、受控字段传播、下载和共享任务/阶段业务宿主。首次误用默认 Vitest 配置导致 CSS 收集失败，改用仓库既有 vitest.pms-file.config.ts；新增上传测试随后补齐 ElUpload 测试桩的 on-change 和 clearFiles 行为，通过后未更改生产控件来规避测试。九个受控前端文件 ESLint 无错误/警告，受控 git diff --check 通过。
- 验证仅使用进程内 mock、真实 MVC 消息转换和 Vue 自定义 renderer，不访问运行数据库或文件存储；不是完整事务回滚、服务器装配或真实浏览器业务验收。共享后台未切换，未部署、迁移、提交或推送。真实新增附件、阶段自动关联/完成、返工及联合部署验收继续待完成，专项目标不标完成。

## 2026-09-15 00:56～00:59：业务定时能力现状核对

- 当前 ProjectRuleQuartzRegistrar 仅注册/恢复 projectRuleOutboxDeliveryJob；该任务只消费 ProjectRuleReevaluationRequested，事件是读取当前冻结契约的普通重评唤醒。它不携定时时点、计划版本或节点轮次，不能作为“绝对时间点／相对等待及过期定时保护”已实现的证据。现有普通重评按当前契约重读的行为本身正确，不应强行改成所有业务变更事件都绑定旧计划。
- ProjectRuleCompiler 仅处理条件分组、FIELD、CONSTANT、DECISION 及已注册业务谓词；DeliveryDefinitionPayloadValidator.PREDICATES 没有时间谓词，ProjectRuleFields 也没有当前时钟事实。TemplateDesignerDocument/TemplateExecutionSnapshot 尚无明确时间条件契约。当前业务定时入口、持久唤醒和定时版本/轮次校验仍缺失，而不是已有功能仅待浏览器验收。
- 可复用既有 Quartz、Outbox 到期领取与单层重试，不需要额外调度中心或业务日历。ProjectNodeExecutionDO 已保存本轮 admittedAt、startedAt、endedAt，可作为显式相对计时事实；不能擅自把 admittedAt 或业务记录 createTime 固定为所有等待的起点。需明确相对等待允许选择的参照事件及引用范围，再贯通设计、发布、运行和返工失效语义。本轮仅记录代码核对结果，未引入时间谓词或改变现有重评行为。

## 2026-09-15 01:00～01:09：可选资产的自定义阶段

- V2 直接编译与本地阶段事实查询已支持自定义阶段；可选资产的 STAGE、STATE 条件及门禁引用校验仍固定为 S0～S6，DefinitionForm 下拉框也没有自定义入口。现统一资产与 V2 编译的阶段编码语法（字母开头，最长 32 位），STATE 引用使用实际阶段编码加 _COMPLETED。S0～S6 保留为可选预设，不自动补入模板。
- 发布编译仍校验实际阶段集合、重复编码、悬空门禁和条件目标；可选资产仍要求准确的已发布依赖版本。本次没有新增规则来源、权限门槛、API 或数据表，没有修改旧 TemplatePublishValidator。资产规则的 ALL/ANY 旧表达式范围尚未在本次统一，不称全部资产规则能力完成。
- 资产复制原实现已深复制绑定、权限说明和规则快照，并生成独立 nodeKey；该行为正确，无须增加复制服务。补充自定义编码的多次复制及独立修改回归，来源修订仅作来源说明，不参与节点运行身份。
- Maven 会话 34028 exit 0，六套件 **70 项通过、零失败/错误/跳过**：资产 payload 20、依赖解析 10、资产服务 7、模板编译 19、规则目标 12、本地阶段事实 2。覆盖自定义编码和引用、非法编码拒绝、冻结后不添加预设阶段、悬空引用拒绝，以及现有发布依赖和自定义阶段完成事实。进程内/mock 测试，不访问运行数据库。
- 前端三套件 **13 项通过**：DefinitionForm 2、资产复制 5、设计模型 6。验证输入变更、序列化重开、预设保留、状态引用及副本相互独立；三文件 ESLint 和受控 git diff --check 通过。这些单测不代表服务器保存成功。
- 内置真实浏览器在 19081 模板页面展开可选资产库，未保存的新阶段表单输入并选择 PREP_WORK，填写“工前准备”后阶段值保留；门禁表单选择 STATE 并输入、选中 PREP_WORK_COMPLETED，失焦后保留。随后关闭并重新导航，撤去本次未保存输入。没有点击保存/发布，没有业务数据写入，不将这次输入交互验证描述为保存、发布及运行验收。
- 59280 共享后台保持不变；未部署、迁移、提交或推送。相对等待起点问题仍待用户确认，业务定时及专项联合部署后的真实办理、返工、历史验收仍未完成。

## 2026-09-15 01:10～01:19：资产条件树与版本规则校验统一

- 实查确认条件树编码器会输出编辑节点 id，旧资产规则 schema 拒绝该字段，并且只支持 ALL/ANY 及旧业务谓词。现资产与 ProjectRuleCompiler 共用 DeliveryDefinitionPayloadValidator.rule 的结构校验，接受编辑 id、NOT、CONSTANT、FIELD 和内联 DECISION；保留非法混合结构、未知参数、空组、多个 NOT 操作数及非法本地状态引用拒绝。Compiler 去除重复结构校验，只负责映射为原生 EL 并复用既有字段类型比较校验，不增加布尔解释器。
- 资产发布和依赖解析调用现有 ProjectRulePublicationValidator 新增的条件入口，与版本内规则共用原生组件解析、开放字段与类型、DMN 输入及输出列检查。Owner 业务事实与审批 Provider 继续分别核验，FIELD/CONSTANT/DECISION 不被误当成业务 Provider。错误返回条件位置及受控原因，不回显比较值或引擎原文。原草稿保存仍只保存定义，不执行、注册规则链或推进业务；完整依赖检查在验证/发布时完成。
- 版本内决策 ruleKey 仅在执行副本中展开为明确表快照并移除已解析引用；设计来源保留原 ruleKey，不增第二份可编辑表。独立资产没有版本规则集合，只能携自己的表定义，未解析的其他版本 ruleKey 被拒绝；跨模板仍走复制，不引入全局规则中心。
- 继续使用仓库锁定 LiteFlow 2.16.1，未升级依赖。原生 validateWithEx 的解析校验用途已对照[LiteFlow 官方说明](https://liteflow.cc/pages/395fd0/)；测试额外核对调用前后 FlowBus 规则链集合不变、缺少组件时拒绝。遵循复用优先和代码自审，仅收敛现有校验，不新增配置中心、发布流程或持久化表。
- Maven 会话 17186、34111 和最终错误定位回归 73102 均 exit 0；按最新有效报告合计九套件 **96 项通过，零失败/错误/跳过**：资产 schema 23、依赖解析 10、资产服务 8、模板编译 19、目标引用 12、原生求值 9、发布校验 8、试算 2、版本规则集合 5。覆盖编辑 JSON 保存读取保真、NOT/类型/缺失事实、资产实际解析接入、未开放字段/缺失组件/缺失决策输出拒绝、版本来源不改写和错误位置保留。引擎测试使用现有隔离 fixture，DMN 明确关闭关系库与 schema 管理，业务 Mapper 为 mock；不是运行数据库或完整业务事务验收。
- 前端条件树编码、资产复制及表单三套件 **11 项通过**；本轮未修改前端生产代码。受控 git diff --check 通过。本轮未执行新的浏览器保存发布验收，未切换共享服务、部署、迁移、提交或推送。资产绑定的阶段 BUSINESS_FACT 仍有旧 TASK-only 限制，后续需与已支持的阶段原模块事实路径核对并收敛；不将本增量称为全部资产或完整专项完成。

## 2026-09-15 01:20～01:25：阶段资产业务事实绑定

- 现有 ProjectStageCompletionService 和 ProjectTaskBusinessService 已通过准确阶段执行上下文读取 Owner 的 lockStageCompletionFact；工勘与需求分析均已有实现。资产绑定仍固定要求 TASK，导致运行支持的阶段业务事实无法通过资产发布。现移除该固定节点限制，阶段与任务均可使用 BUSINESS_OBJECT/BUSINESS_COMPONENT 绑定，仍校验准确 Owner、对象类型和事实编码。
- 不是所有已注册任务 Provider 都实现阶段事实读取：默认 lockStageCompletionFact 返回不可用。按 API 设计技能在原 Provider 接口补充只读 supportsStageCompletionFacts 声明，默认 false；仅已有真实阶段实现的工勘、需求分析声明 true。注册表复用唯一 Owner/type 与已声明事实的检查，再检查阶段能力；没有复制另一份事实目录。此声明不是用户开关、业务条件或权限授予，不新增节点级权限门槛。
- 保留原生绑定、DYNAMIC_FORM、COMPOSITE、APPROVAL 与 BUSINESS_FACT 组合的拒绝，避免把尚无对应统一业务写入路径的配置当作可运行绑定。任务专用 Provider、Owner/type 不匹配、其他模块同名事实、重复 Provider 和缺失身份仍失败关闭。资产发布仅读能力元数据，未查询/创建业务对象；历史读取不重新校验能力。
- Maven 会话 91999 exit 0，五套件 **83 项通过**：资产解析 12、资产服务 8、阶段完成 9、需求分析 Provider 16、工勘 Provider 38。后续直接消费者回归 83613 exit 0：业务关联服务 32、Provider 目录 1；本轮合计 **116 项通过，零失败/错误/跳过**。新增阶段两种绑定的嵌套规则通过、任务专用 Provider 拒绝和能力声明无业务调用测试；原有真实 Owner 完成、未知取反拒绝、开始工作保护、轮次和授权测试保持通过。
- 测试使用现有进程内引擎/mock 或本地事务测试代理，不访问运行数据库，不代表真实浏览器或完整持久事务验收。受控 git diff --check 通过；本轮无前端生产修改，未新增浏览器验收，未部署、迁移、提交或推送，59280 不切换。
- 直接节点侧栏发布路径的 ProjectRulePublicationValidator 尚未按每个消费节点校验 BUSINESS_FACT 与 Owner/type/阶段能力的对应关系；当前 TemplateDesignerDependencyValidator 只检查业务视图依赖。本次先完成资产路径，后续需把同一能力检查接入版本内节点规则，避免只有资产路径识别不支持的 Owner。整体专项仍未完成。

## 2026-09-15 01:26～01:32：版本内节点规则的业务绑定校验

- ProjectRulePublicationValidator 现按每个阶段/任务的准入、完成、退出规则槽位检查 BUSINESS_FACT 的办理绑定、准确 Owner/type、声明事实及阶段能力。共享规则只编译一次，随后分别检查各消费节点；一个节点支持不代表另一个节点也支持。错误定位到具体节点、规则槽位和条件路径，不回显业务值或配置字面量。
- TaskBusinessProviderRegistry 的 CompletionBinding 及 supportsBoundCompletionFact 集中同一能力判断，资产解析与节点发布共同调用；没有新增业务服务、数据表、全局规则目录或节点权限门槛。版本规则即使尚未被节点引用，也检查原模块是否声明所需业务事实；独立事实目录仍只是能力元数据，不是实际结果。既有任务/阶段原模块授权和当前轮次取证保持原路径。
- 当前模板校验/发布调用该发布校验器，项目计划 prepare 也调用它，供预览及生效共同使用。新增测试验证绑定失败位置进入计划预览，且不能通过回传无效预览绕过生效拒绝；不新增批准环节。此处证明的是静态绑定能力匹配，不把它当作准入阶段实际事实接入、真实状态推进或浏览器联验完成。
- Maven 首轮 43502 exit 0。扩展回归 22114 有一项新增预览测试因夹具给出 null 执行快照失败，补齐同一测试使用的有效计划快照及运行节点后，88778 exit 0；生产代码未因该失败放宽。按最终有效报告八套件 **78 项通过，零失败/错误/跳过**：发布校验 11、资产解析 12、Provider 目录 1、草稿预览 11、生效 10、原生求值 9、模板编译 19、版本集合 5。最初命令包含仓库不存在的 ProjectTemplateV2ServiceImplTest，未将其计为已执行测试；模板发布调用链由代码核对，不冒充该控制器集成测试。
- 新用例覆盖跨 Owner 共享规则只拒绝不匹配节点、阶段能力与任务能力独立、原生/未接通绑定拒绝、三种规则槽位无绑定拒绝、未声明事实拒绝和只读取 Provider 元数据。测试仍为进程内/mock/既有原生引擎 fixture，不访问运行数据库；受控 git diff --check 通过。
- 无前端生产修改，未新增真实浏览器保存发布验收；未切换 59280、部署、迁移、提交或推送。整体专项保持进行中，继续核对各用途运行事实接入与发布允许范围是否一致，业务定时及完整联合部署验收仍未完成。

## 2026-09-15：显式业务结果来源节点与工作区提交收口

- 按需求方确认，BUSINESS_FACT 可以显式指定同项目版本内的 sourceNodeKey；完成、退出不指定时仍使用本节点。节点侧栏直接选择阶段或任务，事实目录按来源绑定筛选，切换来源会清除不适用的事实编码。发布拒绝悬空来源、不支持的来源绑定、准入读取自身业务结果及独立资产中的版本节点引用；不增加节点级操作权限门槛。
- 运行按来源节点当前有效轮次取证：活动节点复用原模块准确执行上下文；已完成节点读取该轮冻结的布尔事实证据。新轮次未办理、缺失证据或 Owner 异常均为未知，不回退上轮完成结果，NOT 不放行未知。任务和阶段完成均冻结原模块事实，项目改版保留的已完成轮次仍保留原版本证据。影响分析纳入显式来源依赖，试算区分同一事实的不同来源。
- 前序定向 Maven 会话 37295 九套件 69 项通过。扩展回归最初因新增测试遗漏 Mockito argThat 导入而编译失败；补齐导入后会话 65715 exit 0，八套件 90 项通过、零失败/错误/跳过：来源事实 8、任务生命周期 30、阶段完成 9、收口 5、门禁 6、阶段就绪 12、阶段推进 13、运行图 7。测试为进程内/mock 与原生引擎 fixture，不访问运行数据库，不代表已部署联验。
- 前端条件树与来源编辑器两套件 5 项通过，四文件定向 ESLint 通过。真实浏览器仅验证未保存模板侧栏的来源选择及工勘事实筛选；本轮未验证服务器保存、发布或业务推进，不将其计为完整业务验收。
- 需求方追加要求“全部提交”，本次收录当前工作区项目改动，包括项目专项及已有客户、订单、数据集成改动；PID 和 Office 锁文件保留本地。未对全部收录模块重跑完整验收；暂存检查发现其他文件 14 处末尾空行提示，未借提交改写无关实现。未切换 59280、执行迁移或推送。业务定时、相对等待起点确认及联合部署后的完整浏览器闭环仍未完成。

## 2026-09-15 02:00～02:21：绝对时间条件与版本轮次绑定的到期事件

- 按已批准计划补充 TIME_REACHED 条件，参数 at 必须是带时区的 ISO 日期时间；到达边界即满足。解析和比较使用 JDK 25 OffsetDateTime/Instant，组合仍由 LiteFlow 原生 EL 执行。试算提供一个明确的模拟当前时间，与运行使用同一比较方法；缺失或非法模拟时间保持未知，不能通过 NOT 放行。匹配仍限创建时字段，未开放时间条件作为新的匹配字段。参考：[JDK OffsetDateTime](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/time/OffsetDateTime.html)。
- 复用 Quartz 驱动的现有规则 Outbox 投递，不创建业务周期任务、独立调度表或内存等待器。PMS PlatformBusinessEventApi 增加 appendAt，在已有事务中记录 next_retry_time 作为首次可投递时间，retry_count 保持 0；未修改 Yudao 基础接口。创建初始计划、计划改版和选择性返工分别在原命令事务中注册冻结规则的时间边界，返工只为新轮次注册节点事件，收口事件绑定全体当前轮次集合。没有新 SQL、DDL、依赖或运行环境配置。
- ProjectRuleTimerRequested 使用独立事件类型，由规则投递任务接收，不与通知消费者争抢。消息携带计划版本、目标执行 ID、用途、规则引用与到达时间；收口携带当前执行集合。消费在项目命令锁内核对有效版本、轮次及冻结规则时间，旧版本、被返工替换的轮次、已结束节点和已关闭项目不推进状态；有效计划暂不可读按未知重试。一个时间事件只处理其节点事务，失败由现有 Outbox 重试，其他消息继续；已完成任务命令自身发出重评，不额外重复追加。事务边界对照 [Spring 事务传播](https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/tx-propagation.html)。
- 手工任务时间条件到达后可由规则完成，但仍须原任务已真实提交且本轮提交证据存在；未提交任务不能因时间满足自动完成。继续调用冻结任务状态机、原完成守卫和本轮证据持久化，已开始子任务不得被丢弃。准入规则三态保留，时间消息仅准入任务，不代替业务开始或人为提交。
- 前端复用 Element Plus 2.13.7 日期时间控件（Vue 3.5.34），侧栏按浏览器时区显示，存储为明确 UTC 时间；清空不生成默认时间。试算提示填写带时区时间，不提供直接伪造时间条件布尔结果的入口。参考：[Element Plus DateTimePicker](https://element-plus.org/en-US/component/datetime-picker.html)。
- Maven 初次 86692 因新类查询类型导入遗漏编译失败，补齐正确包导入后 4157 exit 0；后续 49413、29956 均 exit 0。按最终有效报告共十四套件 **132 项通过，零失败/错误/跳过**：时间条件 4、时间事件 8、阶段准入 9、规则投递 5、计划初始化 1、计划生效 10、返工 9、任务生命周期 31、任务计划完成 11、发布校验 13、资产解析 12、阶段完成 9、Outbox 写入 2、Outbox 领取 8。覆盖时区同一瞬间、到达边界、非法时间、未知取反、计划/轮次过期、消息重复、定向准入、异常重试、原模块/手工完成及历史保护。测试使用进程内引擎及 mock，不访问运行数据库，不将它们称为实际重启恢复或持久事务联验。
- 前端初次新增测试读取了测试桩不存在的 camelCase 属性，修正为其保留的 model-value 属性后，两套件 **6 项通过**；三个改动文件 ESLint、受控 git diff --check 通过。内置真实浏览器在工前准备验收模板侧栏选择“到达时间”，用键盘输入 2026-09-16 09:30:00 并确认、失焦，保留值并显示 Asia/Shanghai，截图检查控件可见。随后经“关闭并放弃本地编辑”确认撤去输入。没有服务器保存、预检、发布或业务办理，不据此宣称完整 UI 业务闭环完成。
- 本增量提交前自审完成；未切换 59280、迁移或推送。相对等待起点仍待确认；隔离环境下的到点推进、实际重启恢复、数据库并发与完整设计/发布/办理/改版/返工联合浏览器验收仍待完成，整体专项未完成。

## 2026-09-15 02:28～02:36：普通重评与时间事件统一任务准入

- 实查普通重评只激活阶段，任务轮次准入写入只在时间事件内；时间已到但所属阶段尚未激活时，后续普通重评不能补齐任务准入。新增 ProjectTaskAdmissionService 作为两条路径共用的单任务事务命令，复用 ProjectStageAdmissionService.taskAdmissionFact 的所属阶段及冻结规则三态判断，不增加第二套激活规则或节点权限门槛。
- 命令先取得项目锁，核对当前任务、有效计划、当前轮次及执行契约后，只调用现有 activateIfPending 写入 ACTIVE/admittedAt 和审计。未知不写入、继续由现有 Outbox 重试；已准入或已结束轮次不重复写入，已完成旧版本历史保留。没有调用 START、分配、提交或业务完成命令。时间入口原有版本、轮次、冻结规则和到期检查保留。
- 普通任务循环先准入再关联及完成；一个任务准入异常或未知不阻断独立任务。协调器计入任务准入数量，并在发生实际准入时继续一轮重评，直到没有新增进展。计划改版的直接消费者原先把 ACTIVE 任务轮次等同于已开始办理，现接受有 admittedAt、尚无 startedAt 的待分配/待开始任务；实际开始后的删除保护不变。
- 事务复用 Spring REQUIRED：普通重评逐任务开启事务，时间入口加入已取得项目锁的外层事务，无额外重试层。对照 [Spring 事务传播官方说明](https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/tx-propagation.html)。未增加依赖、数据库结构、生产 SQL 或接口端点。
- Maven 会话 93503、30322 均 exit 0；按最新有效报告八套件 **78 项通过，零失败/错误/跳过**：任务准入 6、阶段准入 10、时间事件 8、任务循环 2、运行协调 2、改版执行规划 9、计划生效 10、任务生命周期 31。原生 LiteFlow 测试覆盖时间已到、阶段随后自动激活、普通重评准入任务且不开始办理；其他回归覆盖重复事件、未知/不满足、过期契约和租户范围、独立任务继续及改版历史保护。
- 新增 Spring 事务代理 + 独立 H2 内存写入测试，实证审计异常回滚、外层时间事务失败回滚和普通调用提交；它验证事务传播而非生产 Mapper SQL/MySQL 并发。所有本步测试均未加载运行应用配置或连接共享数据库。git diff --check 和提交前自审通过。
- 本步无前端改动，未新增浏览器验收、部署、迁移或推送，59280 保持不变。整体专项仍未完成；相对等待起点及完整联合部署后的业务验收仍待完成。

## 2026-09-15 02:37～02:43：运行重评的阶段准入事务隔离

- ProjectRuntimeCoordinator 原先调用批量 activateEligible，阶段持久化或审计失败会退出整轮重评，并回滚同批已经激活的独立阶段。现遍历待准入阶段，逐个调用已有 activateStage 事务命令；异常发生在代理回滚后才转为本轮未知，后续阶段、任务和门禁继续处理。只复用现有单节点命令，不另设流程或重试组件。
- 阶段激活数量纳入实际进展，存在新增激活时再判定一轮，直到没有新增状态推进；已激活阶段不会重复写入或审计。UNKNOWN 仍交由规则 Outbox 唯一重试机制处理。项目创建中的原子初始化及时间事件的版本/轮次守卫未改变。
- 新增真实 Spring 事务代理、LiteFlow 与隔离 H2 写入账本测试：三个阶段中第二阶段审计失败，第一与第三阶段的阶段状态、轮次状态和版本提交；第二阶段两类状态与版本回滚，重投不重复激活成功阶段。该测试验证事务边界，不宣称覆盖生产 MySQL Mapper SQL 或真实并发。另有协调测试覆盖准入进展后的再次判定；它使用 mock 规则结果，不代表新增了一种业务谓词。
- 首轮 Maven 10473 中新增测试在覆盖已有 Mockito Answer 时触发旧回调的空参数异常，尚未执行业务命令。替换为 doAnswer 后定向重跑 95800 exit 0；生产校验未放宽。按最新有效报告五套件 **32 项通过，零失败/错误/跳过**：阶段准入 11、运行协调 3、时间事件 8、规则 Outbox 5、既有项目变化触发 5。git diff --check 与提交前自审通过。
- 本次查明 ProjectStageProgressionTrigger 仍在业务提交后以单个新事务同步重评阶段，未覆盖完整运行协调，也没有同事务持久事件保证。它是下一步需要收敛的实际入口；当前创建应用已写专用规则 Outbox，旧切面还会额外调用该触发。不能把本步称为全部事件入口完成。
- 未修改前端、依赖、数据库结构或运行服务，未执行新浏览器验收、部署或推送；59280 保持不变。相对等待及完整联合业务验收仍未完成。

## 2026-09-15 02:44～02:52：成员与客户变更的同事务重评事件

- 服务经理指派、项目经理成员、普通成员维护和客户更正四个原命令，在已有 PlatformCommandExecutionApi.SuccessFacts 中附加专用 ProjectRuleReevaluationRequested 事件。事件只携带项目、租户、操作人及关联上下文；原通知事件及载荷保留，不让规则消费者竞争通知事件。平台原事务负责业务、幂等、审计和 Outbox 同成同败，不另建提交后事件缓存或重试。
- changed=false 不生成规则事件，幂等重放不再执行成功事实工厂。联合成员应用复用内部实际变更命令的事件，不在联合入口另造一份重评；普通成员入口委托经理关系命令后还可能追加成员资料，两次实际写入产生的事件由现有幂等状态命令安全处理，不增加全局事件去重层。
- 移除旧切面对创建、两类经理、联合成员、普通成员、客户更正及任务状态命令的同步阶段重评。这些入口现在由专用持久事件统一推进阶段、任务、门禁与收口；创建原子初始化仍保留。真实 AspectJ 代理测试确认上述八个方法入口不再触发旧同步处理，保留尚未迁移的基础资料与任务结构入口。
- 首轮 Maven 29198 的平台测试通过，项目编译因新增 List 导入遗漏失败；补齐后 18840 的项目回归发现既有服务经理测试还模拟旧 assertCanAssign，而当前 PRIMARY 路径实际调用 assertCanInitiallyAssign。对齐测试桩和断言，保留范围拒绝不得办理/投递的断言，未改生产授权。定向 18418 exit 0。按最新有效报告九套件 **91 项通过，零失败/错误/跳过**：服务经理 7、项目经理 1、普通成员 17、客户更正 9、切面 5、项目创建 12、任务生命周期 31、平台命令 8、平台事件事务 1。
- 新增与扩展测试实际调用命令的业务回调及成功事实工厂，校验事件上下文、通知载荷不变、无变化与重放不产新事件；平台事务用现有隔离 H2 fixture。测试不连接共享数据库；不是这些业务命令的生产 MySQL 联合事务或浏览器验收。git diff --check、提交范围和代码自审通过。
- ProjectManualCreationServiceImpl.updateProject、ProjectTaskCommandService 资料/结构维护与 ProjectTaskMaintenanceService 仍有旧同步重评路径，且部分资料入口缺少明确外层事务，下一步需按各自实际写入边界收敛。未清除相关剩余项，整体专项未完成；本轮未改前端、服务、依赖或数据库结构，未部署、迁移或推送。

## 2026-09-15：资料维护重评事件收尾与全部提交

- 项目资料、任务基本资料补齐外层事务，成功写入后通过现有 PlatformBusinessEventApi 追加专用规则重评事件；项目更新检查实际影响行数，零行不再报告成功或投递事件。任务原有版本 CAS、原模块授权、项目范围、终态及不可变字段保护保持不变。
- 任务负责人、执行人与说明维护复用现有外层事务，只有幂等命令 NEW 才追加重评事件；重放不重复业务写入或事件。原执行人状态转换、通知事实与职责历史保留，没有新增节点级授权或重复办理动作。
- 删除 ProjectStageProgressionAspect、ProjectStageProgressionTrigger 及其专属测试，由写入口事件与事务测试替代；不再提交后同步执行仅覆盖阶段的重评。删除内容可通过 Git 历史恢复。模板管理项目的任务创建、移动和依赖调整仍由现有 requireUnversionedStructure 拒绝，统一使用计划改版；不为旧非版本化结构再增加兼容触发层。
- Maven 会话 11884 exit 0，五组最新报告共 **40 项通过，零失败/错误/跳过**：任务命令 22、任务维护 9、项目资料相关方法 7、收口快照保护相关方法 1、平台事件事务 1。修复新增构造依赖与已删除类的测试引用，没有放宽生产授权或状态校验。
- 三类资料维护新增真实 Spring 事务代理与唯一 H2 内存账本，验证事件追加失败时源写入回滚、再次成功调用后提交；另验证任务职责幂等重放不投递事件。该证据只证明事务边界，不替代生产 Mapper SQL、MySQL 并发或浏览器联合验收；未连接共享数据库，未加载应用运行配置。
- git diff --check、旧触发器残留引用检查及本次代码自审通过。按用户“全部提交”授权收口全部业务代码、测试和本记录；运行 PID 与 Office 临时锁文件保留在工作区、不纳入提交。未推送、未部署、未切换 59280，完整专项及联合浏览器验收仍未完成。

## 2026-09-15：关闭项目的重评事件终止边界

- 发现已关闭项目的积压/重复重评仍会进入节点遍历，门禁的 PROJECT_NOT_ACTIVE 等结果返回 UNKNOWN，导致 Outbox 持续重试；收口允许保留未激活分支，这不是只能人为构造的状态。协调器现于每轮开始复用 ProjectMasterMapper 主键查询，确认当前租户且生命周期为 NORMAL_CLOSED/EXCEPTION_CLOSED 后直接返回已处理，不再读取或修改阶段、任务、门禁及关联历史。
- 项目缺失、租户不符、未识别状态仍返回未知；数据库读取异常由现有 Outbox 重试。只有正式收口命令实际返回 closed=true 时，才终止本次可选分支未知引起的重试；未关闭项目的 UNKNOWN 不会被消除。原收口规则、已开始工作保护与逐节点事务保持不变，没有直接写项目状态，没有新增 SQL、接口、表或重试层。
- 新增真实 ProjectRuntimeCoordinator + ProjectRuleOutboxDeliveryJob 组合测试，覆盖两种关闭状态的迟到/重复事件确认、不访问节点、缺失/跨租户/空状态/读取异常不误确认、实际关闭后终止可选分支重试、未关闭时继续重试。持久化及正式节点命令使用 mocks；不把组合测试描述为真实数据库或浏览器验收。
- Maven 会话 77614 exit 0；五套件 **32 项通过，零失败/错误/跳过**：关闭事件组合 8、运行协调 3、阶段准入 11、正式收口 5、规则 Outbox 5。保留真实 LiteFlow 收口测试及阶段独立事务的隔离 H2 验证；未连接共享数据库或加载运行应用配置。git diff --check 与提交前代码自审通过。
- 继续按“完成一步后提交代码”交付本增量；不推送、不部署、不切换 59280。现场工勘的已有写入口通过 updateChecked/delete 追加专用事件，本次核对该路径正确，未重复改造。需求分析等其他 Owner 结果路径、相对等待及完整联合浏览器验收仍需继续推进，本步不代表专项全部完成。

## 2026-09-15：需求分析结果按来源阶段/任务读取与复验

- 实查 PreparationController 当前调用动态表单命令服务：初始化、保存、完成及新版本草稿均在既有事务中追加专用重评事件，幂等重放不执行业务回调；此路径正确，本步未重复改造。实际缺口在 RequirementAnalysisFactApiImpl：读取已完成记录仍使用项目级默认任务绑定，并强制要求任务身份及来源资产版本，阶段直接办理和同项目多节点绑定无法正确读取事实。
- 现从业务记录已冻结的 templateSnapshot 取得来源阶段/任务身份，通过原 PROJ 显式节点查询读取绑定；不靠来源资产编号解析运行身份，不回退项目默认绑定。来源缺失、跨项目或返回不同节点的绑定拒绝取数，原权限、项目数据范围、业务完成时间、表单/文件事实与完整版本向量校验保留。历史来源快照只读，不改写，也不新增历史兼容投影。
- RequirementAnalysisWorkBindingFact 补充互斥的阶段 ID/版本，复验根据实际节点种类调用既有阶段或任务接口。阶段绑定的 sourceDefinitionVersion 实际为空，因此不再把资产来源版本作为事实可用的必填条件；执行契约、节点、项目与表单版本仍明确校验。
- 同时修正 ProjectWorkBindingFactApiImpl 阶段事实复验调用“阶段必须可写”的错误边界：现在按项目、阶段顺序锁定并核对版本，允许已结束阶段的只读事实复验。真正的 Owner 写入口仍通过 RequirementAnalysisExecutionBinding.lockCurrent 和 ProjectBusinessExecutionApi/ProjectNodeExecutionApi 校验有效办理上下文；未开放已结束阶段写入，未新增表、SQL 或 Yudao 接口。
- Maven 44855、94663 均 exit 0。最终十套件 **100 项通过，零失败/错误/跳过**：需求分析事实 10、业务对象 16、动态命令 1、阶段命令 11、执行绑定 14、工程事件 2、阶段绑定事实 6、任务绑定事实 15、阶段执行上下文 13、任务执行上下文 12。覆盖阶段身份与空资产版本、明确任务来源、缺失/错误来源拒绝、阶段版本漂移、历史指针、原权限、跨租户、终态不可办理及新轮次草稿保留已完成来源。
- 本步测试使用 mocks/已有单元 fixture，不连接共享数据库。两个 Mapper 验收测试仅适配移除的无用构造依赖并参与测试编译，未运行它们的 MySQL 验收；不能将本次结果描述为生产 SQL、联合事务或浏览器验收。git diff --check 与提交前自审通过；未推送、部署或切换 59280，完整专项仍待后续闭环验收。

## 2026-09-15：审批变化的同事务规则重评事件

- 在 PMS 集成模块添加 FlowableProjectRuleEventListener，复用现有 BPM 对 FlowableEventListener Bean 的注册机制，监听启动、完成、取消及特殊结束事件；通过已有 PlatformBusinessEventApi 在引擎事务内追加专用规则重评事件。消息仅携带项目、租户、操作人和关联身份，不写审批结果或业务字段；消费时仍读取原模块已提交事实，不把流程事件成功解释为审批通过。
- 复用审批适配器的受保护身份变量及业务键，校验流程定义、项目和租户；与项目无关的流程不发布事件。Outbox 写入失败向上传播并回滚引擎操作；租户上下文使用现有 TenantUtils 临时恢复，结束后还原。没有修改 Yudao 接口、原审批状态写入、权限、数据库结构或重试策略。事件机制参考 [Flowable 官方监听器 API](https://www.flowable.com/open-source/docs/all-javadocs/org/flowable/engine/delegate/event/AbstractFlowableEngineEventListener.html)。
- 首轮真实引擎测试发现启动事件漏发：本地 Flowable 8 的 ProcessInstanceHelper 将首个子执行对象放入 PROCESS_STARTED，其业务键为空。现通过 DelegateExecution 的流程实例 ID 查询根实例及变量；修正过程中一次编译发现事件接口本身没有 getProcessInstanceId，已按实际 DelegateExecution API 更正，没有放宽业务断言。
- Maven 定向会话 91659 exit 0：监听器 7 项、既有审批适配器 5 项，共 **12 项通过，零失败/错误/跳过**。新测试使用真实 Flowable 8、Spring 事务和唯一命名的隔离 H2；事件接口使用同事务 JDBC 测试账本。覆盖审批通过/驳回的不同事实结果、取消、启动和完成的事件失败回滚、重试、幂等重放、无关流程及无请求上下文时的租户恢复。不加载应用运行配置，不连接共享数据库；不宣称覆盖生产 Outbox SQL、完整 BPM 授权或浏览器验收。
- 按“全部提交”收口本次代码、测试和验证记录；运行 PID 与 Office 临时锁文件不纳入代码提交。未推送、部署或切换 59280，完整联合业务验收与其余专项计划仍未完成。

## 2026-09-15：阶段门禁审批的返工时间证据

- 核对发现 FlowableProjectStageGateProvider 原先只按冻结门禁引用读取最近一次流程，阶段返工后仍可能采纳上轮通过结果。本步在 PMS 查询契约加入 processStartedNotBefore，由项目侧共用入口依据来源阶段的当前执行轮次 createTime 填充；不接受调用方自行扩大历史范围，不使用全项目最新轮次覆盖独立阶段，也不要求将未选择的已完成阶段改写到新计划版本。
- 审批适配器先校验流程归属，再按本轮起点筛选启动时间，随后执行原有的多活动实例检查和结果判断。上轮流程即使在返工后才结束，也不构成本轮证据；上轮仍在运行的流程不计入本轮多活动实例判断。没有本轮流程仍为“不满足”；来源阶段/轮次缺失、重复或不可信，或缺少时间证据时为“未知”，不降级为无时间边界的历史查询。通过仍要求原模块状态为通过且流程已结束。
- 复用现有本模块阶段、当前轮次查询及 Flowable 历史 API，没有新增 SQL、表、配置、脚本插件或历史兼容层；审批流程、已完成记录和其他轮次均不修改。PMS DTO 及全部构造调用点同步调整，非审批 Owner 不依赖此时间字段。API 设计技能用于明确模块间时间证据契约，代码质量技能用于提交前自审；未引入新的工程审批流程。
- Maven 45082 与最终 32628 均 exit 0。最终十一套件 **72 项通过，零失败/错误/跳过**：轮次边界 10、注册表 2、门禁规则 6、阶段完成 9、任务计划完成 11、阶段准入 11、项目收口 5、Flowable 真实引擎事件 9、审批适配器 6、本地门禁事实 2、交付件门禁事实 1。覆盖错误租户/项目、退役或缺失轮次、阶段独立边界、缺少时间不查询历史、时间左闭边界，以及原非审批路径不额外取轮次。
- 新增真实 Flowable 8 + Spring + 唯一 H2 测试，通过引擎可控时钟验证“旧流程返工后才完成仍不满足、新流程完成才满足”和“旧活动流程不导致新轮次未知”，并确认旧流程历史或运行记录仍保留。项目侧 Mapper 使用 mocks，复用现有真实 LiteFlow fixture；未加载共享应用配置，不连接共享数据库，不宣称通过生产 MySQL 联合事务或真实浏览器验收。git diff --check 与提交前自审通过。
- 本步仅完成阶段门禁来源轮次的事实隔离。还发现审批启动/定义选择仍调用 ProjectRuntimeGraphResolver 的单当前阶段路径，需继续改为按实际门禁所属阶段定位，才能完整覆盖并行阶段的办理入口；任务侧直接审批绑定、相对等待及联合浏览器验收不因本步测试通过而视为完成。未推送、部署或切换 59280。

## 2026-09-15：并行阶段的门禁流程启动入口

- ProjectStageAdvanceApplicationService 的流程定义选择与启动入口不再调用旧阶段推进解析器，不再以项目 currentStage、唯一活动阶段、完成规则或下一阶段作为办理前置。共用 ProjectStageGateProcessContextResolver 按实际门禁引用定位当前有效项目计划中的所属阶段、冻结引用和当前轮次；不创建额外激活规则，不求值或推进任何阶段状态。
- 复用既有阶段/门禁/引用/轮次锁查询及阶段执行上下文联查，校验计划身份、冻结引用类型/编码/版本、来源阶段 nodeKey、执行契约与有效轮次。ACTIVE 阶段的有效 ACTIVE 轮次可以办理；保留原 ENTRY 前置门禁在 PENDING 阶段/PENDING 轮次启动的能力。DONE/TERMINATED 阶段和未准入的 EXIT 门禁拒绝新启动，不将办理前置门禁视为阶段已经激活。
- 原项目更新权限、项目管理数据范围、经理/管理员授权、项目版本检查、平台原子幂等边界与审计保持不变。抽出原项目锁定及范围复验逻辑供旧推进命令与新流程入口共用；旧推进的既有职责/门禁检查没有放宽。定义选择也先持有项目锁，保持项目→本地上下文→BPM Owner 的顺序。没有新增 API、SQL、表、依赖、节点级权限或历史兼容路径。
- Maven 45798 exit 0，六套件 **61 项通过，零失败/错误/跳过**：流程上下文 17、阶段应用命令 14、控制器 3、审批轮次边界 10、门禁规则 6、阶段准入 11。新增用例覆盖并行活动阶段中选择非 currentStage 的实际引用、禁止调用旧推进链、前置 ENTRY、不合法终态、冻结引用漂移、计划/租户/项目/轮次/契约失效、项目版本冲突及原操作权限拒绝，保留幂等重放与不隐式推进状态的断言。
- 上述应用与 Mapper 边界使用 mocks，既有规则测试使用真实 LiteFlow fixture；没有加载共享应用配置或连接共享数据库。既有 MySQL 集成测试仅适配新构造依赖并参与编译，未执行数据库验收；本步未运行新浏览器验收，不能将定向测试称为完整联合验收。git diff --check 与提交前代码自审通过，按用户要求本地提交；未推送、部署或切换 59280。
- 继续核对时发现，门禁流程启动尚不记录阶段轮次 startedAt，而计划影响/生效目前通过节点 startedAt、任务办理状态等识别已开始工作。审批进行中的阶段/门禁改版保护仍需进一步接通，不能因本步入口修复而宣称审批、改版与返工已形成完整闭环。任务侧直接审批办理及整体浏览器验收仍未完成。

## 2026-09-15：运行中门禁流程的改版与收口保护

- 新启动的门禁流程仅在所属阶段已激活时，使用既有带计划、契约、版本及当前轮次条件的办理命令记录 startedAt；平台幂等重放不补造办理证据。启动与记录沿用既有事务，条件更新失败抛出异常，不隐式激活阶段或提交业务。自审纠正了前置 ENTRY 的边界：PENDING 阶段不记录阶段开工，由实际运行中的流程保护；审批结束或取消后，未准入分支是否必做仍由模板规则决定。
- PMS Owner API 增加类型化的项目运行流程查询，只返回流程及门禁引用身份。Flowable 实现按当前租户和项目查询根流程实例，排除继承变量的调用子流程，并复验引用、业务键和流程定义。不可用或身份不可信抛出异常，不以空列表冒充无工作；没有读取其他模块业务表、暴露审批业务数据或改动 Yudao 接口。
- 计划预览和生效共用门禁变更检查：运行中的 PROCESS/APPROVAL 引用不能删除、替换或改变归属，无法确认活动状态也阻断相关变更；名称、说明及不涉及流程身份的调整不增加 BPM 查询。阶段改变（包括相同阶段编码换成新 nodeKey）时，旧门禁引用退役并创建新引用，不将旧流程历史重新归属到新阶段。
- 阶段结束前检查本阶段门禁流程，项目收口前检查项目运行流程，已启动工作须结束或明确终止。查询失败返回未知，不因完成或收口规则为真而丢弃审批；其他阶段运行中的流程不阻止独立阶段完成。没有新增固定审批、状态、表、生产 SQL、重试层或历史兼容机制。
- 最终 Maven 项目会话 44761、集成会话 98706 均 exit 0；十三套件共 **129 项通过，零失败/错误/跳过**：项目侧 112、Flowable 集成侧 17。覆盖活动/待准入阶段区别、重复启动不补证据、条件更新冲突、运行引用删除及归属变更、不可用不放行、独立阶段完成、可选前置审批结束后按规则关闭，以及真实流程完成/取消后活动查询更新。首轮一处 Mockito 已抛异常桩的重设错误使用 doReturn 修正，没有放宽业务断言。
- 使用真实 Flowable 8、Spring、MyBatis 和唯一命名隔离 H2，以及现有 LiteFlow fixture 与应用/计划 mocks；未加载共享应用运行配置或连接共享数据库。定向测试不等于生产 MySQL 跨模块联合事务或真实浏览器验收。提交前差异检查与代码自审通过；按用户“全部提交”收口代码、测试和本记录，排除运行 PID 与 Office 临时锁文件。不推送、不部署、不切换 59280；任务侧直接审批办理、相对等待澄清及完整浏览器闭环仍未完成。

## 2026-09-15：改版与返工预览补齐门禁依赖

- 核对返工主路径确认：现有命令仅为已结束的选定节点及必要所属阶段建立新轮次，新执行不复制旧审批实例、提交或完成证据；这些边界正确，未叠加重复校验。发现 ProjectTaskPlanCompletionService 运行时会额外求值任务 gateRef，而 ProjectPlanImpactAnalyzer 原先不读取门禁 conditionRuleKey，也未建立门禁到引用任务的依赖，导致返工或改版预览遗漏经门禁传播的下游影响。
- 复用已有编译规则叶子和依赖遍历，纳入门禁条件规则及任务 gateRef；门禁规则内容变化即使引用 key 不变，也报告门禁条件变化。旧、新计划的依赖仍共同参与影响预览，不创建第二套求值器，不修改准入、完成规则、授权、数据库查询或状态转换。无门禁引用的任务不增加依赖。
- 新增“现场工勘 → 门禁 → 跨阶段需求分析 → 阶段完成 → 项目收口”的影响测试，覆盖独立分支不受影响、已完成下游仅提示不重算、门禁规则变化及移除。返工服务测试进一步验证预览包含门禁和需求分析，但应用仍只重开选中的工勘任务，未选中的需求分析轮次与结果保持不变；原项目/执行版本冲突、权限撤销、幂等与失败保护测试保留。
- Maven 会话 97278 与最终 53708 均 exit 0，八套件 **66 项通过，零失败/错误/跳过**：影响分析 12、返工规划 5、返工应用 10、返工 Mapper 1、计划草稿 11、计划生效 10、门禁规则 6、任务计划完成 11。新测试使用现有规则编译器及应用 mocks，复用真实 LiteFlow 和唯一隔离 H2/MyBatis/Spring 事务测试；不加载共享运行配置、不连接共享数据库。代码质量技能用于自审，Git 技能用于范围核对和本地提交，git diff --check 通过；没有新增流程门禁。
- 此步为影响预览修复，不代表任务直接 APPROVAL 办理已接通：当前任务 Host 注册仅见原生和业务绑定，自动完成入口也尚未纳入 APPROVAL；该链路仍需实施。现有返工面板可显示受影响任务名称，非阶段/任务的门禁暂按 nodeKey 展示。本步未修改前端或执行真实浏览器验收，未推送、部署或切换 59280，完整专项继续推进。

## 2026-09-15：直接审批绑定的精确流程定义冻结

- 接通任务直接审批前，实查发现 APPROVAL 仅要求 approvalDefinitionKey，且 TaskExecutionContractFactory 不保留该 key，只有流程别名、缺少精确定义身份的配置仍可进入执行契约。新增项目特有的 ApprovalWorkBindingSchema，统一要求版本内 WorkBinding 的 approvalDefinitionKey 与 parameters.processDefinitionId；ID 必须为非空字符串，不接受运行时解析最新版本。若参数已有 key，则须与节点 key 一致。没有新增表、配置中心、全局规则发布或历史兼容层。
- 模板编译、发布依赖校验与任务契约冻结复用该定义。执行契约沿用现有 bindingParameterSnapshot 保存 key、精确 ID 及其他参数，不修改设计来源；编译快照和序列化重开继续保留精确 ID。新任务/返工契约不复制旧 approvalInstanceId，已经完成的旧契约与结果只读。直接阶段审批绑定也适用同一发布校验。
- 扩展既有 PMS 流程定义查询 DTO 的 processDefinitionId；提供 ID 时只按该 ID 查询并复验 key、租户和可用性，不回退最新版本。复用 Flowable 8.0.0 原生 [ProcessDefinitionQuery](https://www.flowable.com/open-source/docs/all-javadocs/org/flowable/engine/repository/ProcessDefinitionQuery.html) 的精确 ID、活动状态与租户过滤，未变更 Yudao 接口或跨模块读取业务表。既有仅按 key 的选择/旧门禁调用明确传 null，本步不把它们描述为已完成版本冻结。
- 计划侧未变更的既有绑定保留原冻结引用，不因名称或规则调整要求其成为新可发布定义；新增、替换绑定必须重新校验所选精确定义。发布失败只返回绑定位置及通用原因，不回传原始引擎异常。定义校验不启动流程，不产生规则重评事件。
- 首次编译 61733 因 Jackson 3 deepCopy 返回 JsonNode 而失败；在已验证对象类型后显式转换 ObjectNode 修复。最终 Maven 29778 exit 0，九套件 **92 项通过，零失败/错误/跳过**：模板编译 20、发布依赖 8、任务契约 9、V2 快照边界 5、计划草稿 11、生效 10、返工应用 11、Flowable 引擎事件/定义 11、流程适配器 7。真实 Flowable 8 + Spring + 唯一隔离 H2 覆盖同 key 两次部署后仍按原 ID 返回、错误 key/缺失 ID/跨租户/暂停定义拒绝及定义校验零事件；项目侧用既有编译器和 mocks 验证冻结、参数漂移与历史保护。
- API 设计技能用于收敛模块契约，官方文档技能核对原生定义查询，Git/代码质量技能用于范围核对和自审；git diff --check 通过。没有加载共享运行配置、连接共享数据库或进行浏览器验收，不推送、不部署、不切换 59280。本步只完成直接审批的定义冻结与发布校验，侧栏定义选择、任务流程启动、原审批页面办理、按本轮结果自动完成与联合验收仍须继续；阶段门禁旧启动路径的可选版本/默认最新语义也尚需统一到冻结定义。

## 2026-09-15：任务直接审批的轮次启动与自动完成后端接入

- 新增 PMS ProjectTaskApprovalApi，在 BPM 所属模块实现任务执行轮次适配。启动复用原 BpmProcessInstanceServiceImpl 的创建逻辑，保留原发起权限、自选审批人校验、流程编号、名称与系统变量；仅开放同包创建方法供适配器调用，不改变 Yudao 的 API、Service 接口或请求定义。BPM 模块只新增 PMS project-api 依赖，不依赖项目 biz 或跨模块读取业务表。按冻结的精确定义 ID、key 和租户校验，不回退最新版本；原生 ID 语义核对 [Flowable 8 ProcessInstanceBuilder](https://www.flowable.com/open-source/docs/all-javadocs/org/flowable/engine/runtime/ProcessInstanceBuilder.html)。
- 任务 Host 复用原有权限、角色、项目范围和冻结状态机检查，为 APPROVAL 接通开始、指派、关闭能力，不开放原生手工提交、完成或进度替代审批。开始任务后，在同一项目命令事务内按实际当前执行上下文创建本轮审批；原模块拒绝或事务失败不留下独立审批。流程以 executionId 为关联身份，另验 taskId、contractId、项目、租户、精确定义和开始时间，不使用旧 Long approvalInstanceId 或来源资产编号。重复启动返回该轮已有流程，不重写旧轮次。
- 任务自动完成入口纳入 APPROVAL。先取本轮原 BPM 结果，只有状态通过且流程结束才可继续求值当前计划的完成、退出、门禁规则，再通过已有正式任务状态机结束。拒绝、取消和运行中保持不满足；身份冲突、重复实例、缺失状态、无本轮时间证据及流程结束但无有效结果为未知。审计与轮次历史保存安全的审批实例/定义/结果定位，不写入表单明文。关闭任务前要求本轮运行审批先由原 BPM 结束或明确取消，未开始任务仍可明确关闭。
- 既有 Flowable 事件监听器接入任务审批的开始、结束和取消身份，沿用专用项目规则重评 Outbox；只通知重读事实，不复制审批结果或表单值，不增加重试层。Outbox 失败同事务回滚流程，原逐节点事务继续隔离独立分支。未改表、SQL、历史数据、调度或固定审批步骤。
- 首轮 Maven 63620 exit 0，74 项通过；补齐缺失结果、重复流程及协调服务测试后 Maven 18825 exit 0。最后扩展原生动作对照用例，Maven 23724 定向重跑 TaskNativeBindingHostProviderTest exit 0。最终有效八套件 **99 项通过，零失败/错误/跳过**：原 BPM/Flowable Owner 11、项目审批协调 4、生命周期 34、计划完成 13、Host 7、Flowable 事件 13、逐任务重评 2、任务查询 15。真实引擎在唯一命名 H2 中验证精确定义、原 BPM 创建校验、重复启动、结束结果、旧轮次隔离、事务回滚；项目侧使用 mocks 和现有真实 LiteFlow fixture。没有加载共享应用配置，未连接共享数据库，未声称生产 MySQL 联合事务或浏览器验收通过。
- API 设计技能用于明确 Owner/执行身份边界，官方文档技能核对版本选择，代码质量技能用于自审，Git 技能用于本地提交；git diff --check 通过。本步是后端启动及自动完成接入，不代表全部审批办理完成：当前开始任务入口传空表单变量和自选审批人，侧栏还需接入原表单、自选审批人、流程详情及定义选择；驳回后的同轮重新发起入口也尚未实现。直接阶段 APPROVAL、阶段门禁冻结版本统一、相对等待澄清和完整真实浏览器闭环继续推进。不推送、不部署、不切换 59280；运行 PID 和 Office 临时锁文件保留在本地，不纳入提交。

## 2026-09-15：审批表单契约与同轮重新发起

- 任务动作请求接入原 BPM 表单变量和自选审批人；START 与同轮 APPROVAL 提交均复验当前冻结绑定 ID/版本，沿用原操作授权、项目范围、执行人及项目命令事务。APPROVAL 是已开始任务中的业务提交，不再执行一次任务 START，不改任务轮次或完成历史。原 BPM 继续负责流程创建权限和自选审批人校验；工作台只返回冻结定义、当前轮次及审批实例/结果定位，不返回表单值。
- 驳回或取消后允许新操作在同一轮次创建新审批尝试；运行中或已通过不能重复新建。项目锁内分配尝试序号，读取最新尝试的真实结果；同一操作者和操作键重放返回原实例，不将旧重放变成当前结果。所有尝试仍须符合租户、项目、任务、契约、轮次、精确定义和本轮开始时间，异常身份为未知。旧实例保留，不覆盖历史，不回退最新流程定义。
- 复用访问日志注解关闭任务动作的完整请求记录，审批异常审计仅保留安全错误码，原操作审计及规则重评继续生效。前端直接消费者补传审批 START 的冻结契约身份，并补充类型化表单提交及实例路由接口；没有新增布局或重复审批面板。API/前端技能用于收敛接口和调用方，代码质量技能用于自审，Git 技能用于提交范围核对；未新增第三方依赖、SQL、表或配置。
- 最终 Maven 会话 85911 exit 0，八套件 **118 项通过，零失败/错误/跳过**：原 BPM/Flowable Owner 14、审批协调 6、生命周期 38、计划完成 13、Host 7、控制器 11、任务查询 16、Flowable 事件 13。命令为 `mvn -q -pl pms-module-project,pms-module-integration,yudao-module-bpm -am test "-Dtest=PmsTaskApprovalProcessOwnerTest,ProjectTaskApprovalServiceTest,ProjectTaskLifecycleServiceTest,ProjectTaskPlanCompletionServiceTest,TaskNativeBindingHostProviderTest,ProjectTaskWorkbenchControllerTest,ProjectTaskQueryServiceTest,FlowableProjectRuleEventListenerTest" "-DargLine=-Xms128m -Xmx768m" "-Dsurefire.failIfNoSpecifiedTests=false" "-DfailIfNoTests=false"`。真实 Flowable 使用唯一命名 H2/Spring 事务；项目侧使用 mocks 和现有 LiteFlow fixture，不连接共享数据库。访问日志测试调用原过滤器的日志构造方法及真实方法注解，不声称真实 HTTP 过滤链验收。
- 回归期间修复两处因契约读取顺序变化而失效的测试准备、缺失的 Mockito eq 导入，以及新增日志测试缺少请求用户类型的问题，保留权限、状态及隐私断言。一次 JVM 因本地内存分配失败退出，串行验证并仅在本次 Maven 调用限制测试堆后通过，未调整运行配置或停止共享服务。前端 `pnpm exec vitest run --config vitest.pms-file.config.ts src/views/pms/project/inheritance/detail/taskStateActions.runtime.spec.ts` **5 项通过**；`pnpm ts:check` 未通过，错误位于本轮未修改的 designerAssets、BusinessView、PmsFileExecution、CustomerFormDrawer、客户联系人、ProjectCustomerOverview、ProjectPlanEditor、taskManualHandling、TemplateFlowCanvas 文件，本轮三个修改文件未报错；未重跑 HEAD 基线，不能将全量类型检查写成通过。
- 本次按“全部提交”提交代码、测试和本记录，运行 PID 与 Office 临时锁文件仅保留本地；不推送、不部署、不切换 59280。原 BPM 表单/自选审批人/详情的实际侧栏 UI、定义选择、直接阶段审批、阶段门禁冻结版本统一、相对等待澄清及完整真实浏览器验收仍未完成，本记录不是专项完成结论。

## 2026-09-15：共用任务办理界面的审批表单接入

- 在 ProjectFlowPanel 共用办理区接入 TaskApprovalPanel，项目任务抽屉和交付流程继续使用同一入口。TaskApprovalForm 复用原 BPM 的 form-create 配置解析、ProcessInstanceTimeline/原用户选择器、BPMN/Simple 查看器；查看及实际审批跳转原 BpmProcessInstanceDetail。没有复制原业务数据、改造原 BPM 页面或增加审批中心。通用任务动作不再直接执行 APPROVAL 的 START/SUBMIT/COMPLETE，表单携带冻结契约提交 START 或同轮 APPROVAL；关闭任务仍走原命令和运行中审批保护。
- 表单仅按工作台提供的精确 ID 加载，复验 key、活动状态；原 BPM 定义/预览/详情接口各自授权。普通流程表单支持空字段配置和必选审批人；业务自定义表单明确提示使用原模块业务页面绑定，不将其空提交或跳转到脱离任务上下文的创建页面。运行中、已通过、未知或权限不允许时不展示新提交表单。保留原表单校验、字段权限、候选人选择，字段从只读恢复可写时恢复原验证；失败不清空填写内容，旧预测返回不覆盖新值，清空最后一个字段也重新预测。异步校验期间值发生变化则拒绝提交；提交中阻止离开，未提交内容离开前确认。前端操作幂等复用现有 submissionIdempotency，响应不确定时同请求重试使用同一键。
- 审批预览沿用原 GET 接口与请求定义，但该接口携带 processVariablesStr，故使用既有 ApiAccessLog 注解关闭完整请求日志；原操作日志元数据与查询权限不变。新增测试通过真实过滤器日志构造方法验证不记录参数/响应体，不声称完整 HTTP 过滤链或其他日志渠道均已验收。没有新增 API、依赖、SQL、迁移或运行配置。
- 最终前端四套件 **26 项通过**：taskApprovalForm 7、taskApprovalPanel 6、taskStateActions 5、flowPanel 8；使用 Vue runtime harness 和显式 FormCreate/API/路由替身，覆盖冻结定义、空字段、自选人、失败、乱序、修改中校验、离开保护、原 BPM 详情路由、幂等和直接消费者，不当作浏览器表单验收。命令：`pnpm exec vitest run --config vitest.pms-file.config.ts src/views/pms/project/inheritance/detail/taskApprovalForm.runtime.spec.ts src/views/pms/project/inheritance/detail/taskApprovalPanel.runtime.spec.ts src/views/pms/project/inheritance/detail/taskStateActions.runtime.spec.ts src/views/pms/project/inheritance/detail/flowPanel.runtime.spec.ts`。Maven 89662 exit 0，BpmApprovalPreviewLogTest 1 项及既有真实 Flowable/H2 Owner 14 项，共 **15 项通过**，报告时间 05:22:59/05:23:04；未连接共享数据库。
- `pnpm ts:check` 仍 exit 2，26 个诊断落在上一节列出的九个本轮未修改文件，本步新增/修改组件及测试无类型诊断。真实内置浏览器在项目 993109130061（工前准备办理方式切换验收20260914）进入项目任务并打开现场工勘：原任务资料、单一业务标题、右侧刷新业务结果和业务列表正常显示，任务职责未显示，所读错误日志为空，已查看实际截图。仅执行读取/打开，未启动或修改任务；测试标签已关闭。此场景不是新增审批表单，未部署对应后台，新增审批的提交、办理、驳回重发、自动完成及响应式/键盘联合验收仍缺真实浏览器证据。
- 前端技能用于复用组件与离开/失败交互，源码/官方文档技能核对 Vue 3.5.34 的[深度监听及过期副作用](https://vuejs.org/guide/essentials/watchers.html)和 [form-create v3 API](https://www.form-create.com/v3/instance)，浏览器技能用于既有页面实查并界定证据，代码质量/Git 技能用于自审和本地提交，git diff --check 通过。继续待办：模板侧精确审批定义选择、直接阶段审批、阶段门禁冻结版本统一、相对等待澄清、种子场景和完整联合验收。本步不推送、不部署、不切换 59280，不将专项标记完成。

## 2026-09-15：模板任务侧栏选择精确审批版本

- 任务节点侧栏增加审批流程选择，复用原 BPM 流程定义分页目录和精确 ID 查询，展示名称、标识及版本。仅选择活动的普通流程表单；停用流程及原模块自定义业务表单不可选，后者继续通过业务页面绑定办理。目录失败保留冻结身份，选择时复验精确定义，不自动替换为同名最新版本。只读节点不依赖在线目录；沿用模板编辑或项目计划管理权限及原 BPM 目录查询权限，没有新增节点授权门槛、接口或依赖。
- 从其他办理方式切换前明确确认：完成依据改为本轮审批通过、无附加条件，准入、退出和权限保留；为当前任务创建独立版本内规则，不修改其他节点的共享规则。CONSTANT true 仅表示无附加完成条件，既有后端仍先要求本轮真实审批通过。已有审批任务仅更换绑定版本时保留现有条件。取消、切换节点、替换文档或权限撤销后不应用迟到选择；保存草稿不启动审批，不影响在用版本。
- 前端三套件 **20 项通过**：approvalDefinitionSelect 7、taskApprovalBinding 7、taskManualHandling 6。命令：`pnpm exec vitest run --config vitest.pms-file.config.ts src/views/pms/project/project-templates/approvalDefinitionSelect.runtime.spec.ts src/views/pms/project/project-templates/taskApprovalBinding.runtime.spec.ts src/views/pms/project/project-templates/taskManualHandling.runtime.spec.ts`。首次旧手工办理测试因新增组件引入真实浏览器缓存而报 window 未定义，补齐组件与权限替身后通过，未修改业务断言。使用 Vue runtime harness 和 API 替身，不访问共享数据库；保存重开验证为组件序列化测试，不是服务端持久化或浏览器验收。
- 全量 `pnpm ts:check` 会话 68891 exit 2，仍为 26 条诊断、九个文件，与上一增量记录一致；新增审批选择器、绑定测试及 TemplateContentEditor 无诊断。taskManualHandling 本次仅增加两个测试替身，其既有类型诊断仍未修复，不能称为全量类型检查通过。代码质量与 Git 技能用于自审、范围核对和提交，差异检查通过。本步未取得新增侧栏的完整真实浏览器证据，不宣称设计、发布、办理联合验收完成。
- 按用户“全部提交”收口当前实现、测试及本记录；运行 PID 与 Office 临时锁文件保留本地，不纳入提交。不推送、不部署、不切换共享后台 59280。直接阶段审批、阶段门禁冻结版本统一、相对等待澄清、示例场景与完整真实浏览器闭环仍待推进。

## 2026-09-15：阶段门禁流程按计划冻结定义启动与求值

- PROCESS/APPROVAL 门禁引用沿用现有 refVersion 保存原生精确 processDefinitionId。编译拒绝缺少 ID 的引用，模板发布与项目计划新增/修改引用复验精确 ID、key 和可用性；项目改版仅改名等不涉及引用的变更不重验为新引用，也不修改原提交文档。直接任务/阶段审批绑定复用同一精确定义重验。未新增表、兼容层、全局规则发布或运行时最新版本解析。
- 启动上下文校验有效计划快照与实际门禁引用一致且有冻结 ID；服务端从该引用构造启动命令，客户端可省略 ID 或传相同 ID，其他版本一律拒绝。运行侧定义查询只返回当前冻结定义。Flowable 启动移除空值默认最新分支，使用原生精确 ID 查询，参见 [Flowable 8 ProcessDefinitionQuery](https://www.flowable.com/open-source/docs/all-javadocs/org/flowable/engine/repository/ProcessDefinitionQuery.html)。原项目锁、操作授权、幂等、阶段轮次办理证据和 Outbox 事务保持不变，未附加激活条件。
- 阶段准备度、规则求值和原收口调用方向 Owner 传递冻结 ID，轮次边界补全保留该 ID；审批通过还必须符合精确定义身份。缺少冻结 ID 或其他版本的通过结果返回未知，不以最新定义兜底。既有计划门禁安装器按 refType/code/version 比较，换版本退役旧引用并保留旧流程历史，运行中的引用不能改版；本步复验这些既有行为而未另写安装逻辑。
- 前端旧 ProjectStageGatePanel 移除运行时版本选择，只提示按计划冻结版本办理；失败不显示新的启动成功结果。新项目主界面当前使用 ProjectStageStatusPanel，旧面板不是新门禁审批入口，不能据此宣称新界面已接通。前端/API 技能用于收敛客户端行为及类型注释，未改 Yudao 接口。
- Maven 49488 首轮因新增测试缺少 assertThrows 导入编译失败，已补齐。87719 执行项目侧 109 项，其中 TemplatePublishValidatorTest 完整 30 项有 7 项失败，报 schemaVersion 未知、旧 predicate 未注册，以及 TASK_NATIVE 外部目标断言未命中；这些任务绑定/规则分支与测试数据本步未修改，未重新执行 HEAD 基线，不能将完整旧套件写为通过。失败方法：validContentPasses、missingOptionalTemplateProcessReferenceAccepted、noTasksRequiredForProjectBasicStage、preparationBindingMustMatchTheFixedV1Catalog、preparationBindingAcceptsOnlyDictionaryApprovedExtensionItems、requirementAnalysisBindingAcceptsOnlyTheFrozenV2DynamicFormRevision、taskNativeWithExternalTargetRejected。本步仅更新旧门禁测试的冻结要求，未放宽业务校验或隐藏上述失败。
- 后续 Maven 91345 exit 0，补验直接调用方及真实 Flowable。合并未变化的有效证据，十四组 **132 项定向通过**：模板编译 21、发布依赖 9、旧门禁冻结要求 1（不是完整旧套件）、模板 Owner 3、启动服务 14、上下文 19、轮次 10、本地事实 2、交付件事实 1、运行图 7、阶段准备度 12、改版安装器 12、Flowable 适配器 8、真实引擎事件 13。真实 Flowable 8/Spring 使用唯一命名 H2、关闭异步执行器且不读取共享应用配置，验证重新部署仍启动原冻结 ID、同操作重放、实际通过结果、错误版本返回未知、停用拒绝与既有事务回滚；其余为现有 mocks/LiteFlow fixture。未连接共享数据库，不声称生产 MySQL 联合事务验收。
- 最终定向 Maven 命令：`mvn -q -pl pms-module-project,pms-module-integration -am test "-Dtest=ProjectRuntimeGraphResolverTest,ProjectStageReadinessServiceTest,ProjectPlanGateInstallerTest,TemplatePublishValidatorTest#gateProcessReferenceWithoutFrozenDefinitionRejected,FlowableProjectStageGateProviderTest,FlowableProjectRuleEventListenerTest" "-DargLine=-Xms128m -Xmx768m" "-Dsurefire.failIfNoSpecifiedTests=false" "-DfailIfNoTests=false"`。前端 `pnpm exec vitest run --config vitest.pms-file.config.ts src/views/pms/project/project-master-detail/components/ProjectStageGatePanel.spec.ts src/views/pms/project/project-master-detail/components/Z06StageProgression.runtime.spec.ts` **10 项通过**，含组件失败请求验证及静态接线断言，不等同浏览器验收。全量类型检查 44272 exit 2，仍为原九个文件的 26 条诊断，本步前端文件无诊断。
- Git/代码质量技能用于自审、差异检查及本地提交。不推送、不部署、不切换 59280，不生成测试流程到共享环境，不改历史数据。门禁侧栏精确版本选择、新共用办理区门禁入口、直接阶段审批、旧套件失败修复、相对等待澄清、示例场景和真实浏览器联合闭环继续推进；本步不是专项完成结论。

## 2026-09-15：门禁侧栏选择精确流程版本

- 抽取 GateReferencesEditor，APPROVAL/PROCESS 引用复用现有 ApprovalDefinitionSelect，选择流程名称与版本后写入 refCode/refVersion，不另设可编辑执行规则来源。其他引用保留编码配置；引用类型切换清除旧版本身份。配置前展示直接引用此门禁的任务，保存草稿不启动流程、不覆盖在用版本。
- 使用引用对象身份保持行稳定，取消、删除引用、切换类型或节点、只读、权限撤销及组件卸载后不应用迟到选择。确认动作重新检查当前权限；首轮回归发现权限替身变化后界面计算缓存不能作为提交授权依据，已改为动作时重新读取权限，未降低测试断言。
- 前端四套件最终 **31 项通过**：gateReferencesEditor 11、approvalDefinitionSelect 7、taskApprovalBinding 7、taskManualHandling 6。命令：`pnpm exec vitest run --config vitest.pms-file.config.ts src/views/pms/project/project-templates/gateReferencesEditor.runtime.spec.ts src/views/pms/project/project-templates/approvalDefinitionSelect.runtime.spec.ts src/views/pms/project/project-templates/taskApprovalBinding.runtime.spec.ts src/views/pms/project/project-templates/taskManualHandling.runtime.spec.ts`。包含父编辑器 prepareSave/序列化重开精确 ID 保留、重复引用与异步失效保护；使用组件/API 替身，不访问共享数据库。
- 全量类型检查会话 51341 exit 2，仍为前述九个文件的 26 条诊断；GateReferencesEditor、配套测试及 TemplateContentEditor 没有类型诊断，不将全量类型检查写为通过。此前后端旧套件七项失败未在本步修复或重跑。
- 内置浏览器实际打开“工前准备交付场景验收”（RULE_PREP_SCENE_20260914）的工前准备任务子画布，可见现场工勘与需求分析；通过键盘添加本地未保存门禁并打开引用类型选择。未完成精确版本选择的完整浏览器验证，未保存或发布到共享后台，不能据此宣称发布、流程发起和办理验收完成。
- 按用户“全部提交”收口当前代码、测试与本记录；仅排除运行 PID 和 Office 临时锁文件，保留其本地内容。Git 提交技能用于明确暂存范围，自审及差异检查通过。不推送、不部署、不切换 59280。新共用办理区的阶段门禁入口、直接阶段审批与完整联合验收仍待推进，本次提交不代表专项完成。
