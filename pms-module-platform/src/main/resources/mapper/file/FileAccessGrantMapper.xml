<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.platform.dal.mysql.file.FileAccessGrantMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="row.id">
        INSERT INTO plt_file_access_grant
            (artifact_id, file_version_no, subject_user_id, operation_code,
             business_scope_hash, token_digest, status_code, expires_at, created_at,
             consumed_at, revoked_at, revoked_by, revoke_reason, tenant_id)
        VALUES
            (#{row.artifactId}, #{row.fileVersionNo}, #{row.subjectUserId}, #{row.operationCode},
             #{row.businessScopeHash}, #{row.tokenDigest}, #{row.statusCode}, #{row.expiresAt},
             #{row.createdAt}, #{row.consumedAt}, #{row.revokedAt}, #{row.revokedBy},
             #{row.revokeReason}, #{row.tenantId})
    </insert>
</mapper>
