# F-PROJ-002 V1.8 浏览器验收

结论：`PASS`。

真实Chrome通过CDP访问`http://127.0.0.1:18082`，使用实际登录态和后端`58080`完成以下闭环：

- 草稿刷新恢复、待权威范围拒绝且无副作用、自由组合预览；
- 两个子项目同事务原子创建，作用域完成分配，成功后本地草稿清除；
- 直接下级、全部后代、完整上级链、业务层级、定位五类查询；
- 循环移动拒绝、策略版本可见、进度待计算、全部后代闭环阻断；
- 有限可见用户不能获得未授权业务事实；
- 1440×900、1100×800、820×1000、390×844四档视口均无页面级横向溢出，核心面板可见。

最终运行`failedApiResponses=[]`、`consoleErrors=[]`。机器结果及截图位于`output/f-proj-002-v18/browser/`；桌面和移动截图已人工检查，表格在窄视口内采用局部承载，没有破坏页面整体布局。
