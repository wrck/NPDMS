# F-PROJ-006 V1.8 浏览器验收

结论：`PARTIAL / BLOCKED`。响应式、主题持久化和守卫失败关闭子项通过；正向治理动作仍被 COLLECTION 权威契约缺失阻断，Task 10 与 Feature Done 不关闭。

真实 Chrome `151.0.7922.173` 访问隔离前端 `18083`、后端 `58081`、新建并迁移至 V87 的 MySQL 环境，完成以下核验：

- 1440×900、1024×768、768×900、320×720 四档均可进入项目详情“异常治理”；刷新前后页面级横向溢出均为 `false`。
- ACTIVE 根项目正确显示“回退至 S0”“异常关闭”，不显示“受控重开”；治理历史空态刷新后保持。
- BPM 使用锁定配置键 `project-progress-policy` 后返回正常空事实，不再出现 `PROVIDER_UNAVAILABLE`。
- 回退与异常关闭均先消费服务端守卫；结果为 32 个阻断（1 个 COLLECTION 不可用、31 个 ACTIVE 后代），两类对话框均不显示确认提交按钮，无副作用。
- 320px 下暗色模式与菜单色 `#273352` 可切换并在刷新后保持；所有视口的 `consoleErrors`、`pageErrors` 和 HTTP `>=400` 响应均为空。

未完成项：`CollectionGovernanceGuardProvider` 当前无论端点是否配置均返回 `PROVIDER_UNAVAILABLE`。在设备连接与采集中心的权威端点、响应契约和真实验收证据补齐前，不得伪造空采集事实，也无法通过真实浏览器可信执行回退、异常关闭、受控重开及其历史持久化闭环。

观察项：基础框架“项目配置”抽屉宽度固定 350px，在 320px 视口左侧约裁切 30px，但不产生页面级横向溢出且主题控件可操作。该问题不属于本 Feature，未修改基础框架。

机器结果与截图位于 `output/f-proj-006-v18/browser/`。
