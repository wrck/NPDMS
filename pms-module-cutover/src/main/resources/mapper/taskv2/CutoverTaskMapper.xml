<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.cutover.dal.mysql.taskv2.CutoverTaskMapper">
    <select id="selectForUpdate" resultType="cn.iocoder.yudao.module.pms.cutover.dal.dataobject.taskv2.CutoverTaskDO">
        SELECT *
        FROM cut_task
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.taskId}
          AND deleted = b'0'
        FOR UPDATE
    </select>

    <update id="linkAssessmentIfMatch">
        UPDATE cut_task
        SET current_assessment_id = #{query.assessmentId},
            version = version + 1,
            update_time = NOW(3)
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.taskId}
          AND version = #{query.expectedVersion}
          AND current_assessment_id IS NULL
          AND task_origin = 'NEW_PLATFORM'
          AND current_stage = 'P2'
          AND task_status = 'GRADE_CONFIRMING'
          AND deleted = b'0'
    </update>

    <update id="transitionIfMatch">
        UPDATE cut_task
        SET current_assessment_id = #{query.assessmentId},
            manual_grade = #{query.manualGrade},
            current_stage = #{query.currentStage},
            task_status = #{query.taskStatus},
            version = version + 1,
            update_time = NOW(3)
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.taskId}
          AND version = #{query.expectedVersion}
          AND task_origin = 'NEW_PLATFORM'
          AND current_stage = 'P2'
          AND task_status = 'GRADE_CONFIRMING'
          AND deleted = b'0'
    </update>

    <select id="countLegacyIdentityConflicts" resultType="long">
        SELECT COUNT(1)
        FROM cut_task
        WHERE tenant_id = #{query.tenantId}
          AND deleted = b'0'
          AND ((project_id = #{query.projectId} AND task_no = #{query.taskNo})
            OR legacy_task_id = #{query.legacyTaskId})
    </select>
</mapper>
