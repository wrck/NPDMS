<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.cutover.dal.mysql.checklist.CutoverChecklistMapper">
    <select id="selectCurrentForUpdate"
            resultType="cn.iocoder.yudao.module.pms.cutover.dal.dataobject.checklist.CutoverChecklistDO">
        SELECT *
        FROM cut_cutover_checklist
        WHERE tenant_id = #{query.tenantId}
          AND cutover_task_id = #{query.taskId}
          AND current_marker = 1
          AND deleted = b'0'
        <if test="query.checklistId != null">
          AND id = #{query.checklistId}
        </if>
        FOR UPDATE
    </select>

    <select id="selectCurrent"
            resultType="cn.iocoder.yudao.module.pms.cutover.dal.dataobject.checklist.CutoverChecklistDO">
        SELECT *
        FROM cut_cutover_checklist
        WHERE tenant_id = #{query.tenantId}
          AND cutover_task_id = #{query.taskId}
          AND current_marker = 1
          AND deleted = b'0'
        <if test="query.checklistId != null">
          AND id = #{query.checklistId}
        </if>
    </select>

    <update id="touchDraftIfMatch">
        UPDATE cut_cutover_checklist
        SET version = version + 1,
            update_time = NOW(3)
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.checklistId}
          AND version = #{query.expectedVersion}
          AND status_code = 'DRAFT'
          AND current_marker = 1
          AND deleted = b'0'
    </update>

    <update id="submitIfMatch">
        UPDATE cut_cutover_checklist
        SET status_code = 'SUBMITTED',
            submitted_by = #{query.submittedBy},
            submitted_at = #{query.submittedAt},
            version = version + 1,
            update_time = NOW(3)
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.checklistId}
          AND version = #{query.expectedVersion}
          AND status_code = 'DRAFT'
          AND current_marker = 1
          AND deleted = b'0'
    </update>

    <update id="rematchIfMatch">
        UPDATE cut_cutover_checklist
        SET assessment_id = #{query.assessmentId},
            assessment_version = #{query.assessmentVersion},
            checklist_version = #{query.nextChecklistVersion},
            input_snapshot = #{query.inputSnapshot},
            input_snapshot_hash = #{query.inputSnapshotHash},
            match_trace = #{query.matchTrace},
            config_gap_snapshot = #{query.configGapSnapshot},
            updater = #{query.actorId},
            version = version + 1,
            update_time = NOW(3)
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.checklistId}
          AND version = #{query.expectedVersion}
          AND status_code = 'DRAFT'
          AND current_marker = 1
          AND deleted = b'0'
    </update>
</mapper>
