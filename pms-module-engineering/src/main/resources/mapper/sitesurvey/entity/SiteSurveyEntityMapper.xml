<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.engineering.dal.mysql.sitesurvey.entity.SiteSurveyEntityMapper">
    <!-- REQ-PROJ-004: Owner facts only; never load the survey body or PRE02 JSON roots. -->
    <sql id="taskFactColumns">id, tenant_id, project_id, name, version, status, deleted, confirmed_at, archived_at</sql>
    <sql id="taskObjectScope">
        id = #{query.objectId} AND tenant_id = #{query.tenantId}
        AND project_id = #{query.projectId} AND deleted = 0
    </sql>
    <select id="selectAssociationPage" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.entity.SiteSurveyEntityDO">
        SELECT <include refid="taskFactColumns"/> FROM sol_site_survey
        WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND deleted=0 AND status IN (0,1,3)
        <if test="query.afterId != null">AND id &gt; #{query.afterId}</if>
        ORDER BY id LIMIT #{query.limit}
    </select>
    <select id="selectTaskCandidates" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.entity.SiteSurveyEntityDO">
        SELECT <include refid="taskFactColumns"/> FROM sol_site_survey
        WHERE tenant_id = #{query.tenantId} AND project_id = #{query.projectId} AND deleted = 0
        ORDER BY id DESC LIMIT #{query.limit}
    </select>
    <select id="selectTaskObject" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.entity.SiteSurveyEntityDO">
        SELECT <include refid="taskFactColumns"/> FROM sol_site_survey
        WHERE <include refid="taskObjectScope"/>
    </select>
    <select id="selectTaskObjectForUpdate" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.entity.SiteSurveyEntityDO">
        SELECT <include refid="taskFactColumns"/> FROM sol_site_survey
        WHERE <include refid="taskObjectScope"/> FOR UPDATE
    </select>
    <update id="deleteDraft">
        UPDATE sol_site_survey SET deleted = 1, version = version + 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{query.id} AND tenant_id = #{query.tenantId} AND version = #{query.version}
          AND status = 0 AND deleted = 0 AND outsource_request_id IS NULL
    </update>

    <select id="selectResultInventory" resultType="java.lang.String">
        SELECT id FROM sol_site_survey
        WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND deleted=0
          AND status IN (1,3)
        <if test="query.afterId != null">AND id &gt; #{query.afterId}</if>
        <if test="query.objectIds != null">
            <choose>
                <when test="query.objectIds.size() > 0">
                    AND id IN <foreach collection="query.objectIds" item="id" open="(" separator="," close=")">#{id}</foreach>
                </when>
                <otherwise>AND 1=0</otherwise>
            </choose>
        </if>
        ORDER BY id ASC LIMIT #{query.limit}
    </select>
</mapper>
