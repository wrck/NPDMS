<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.engineering.dal.mysql.sitesurvey.SiteSurveyMapper">
    <update id="deleteDraft">
        UPDATE pms_eng_site_survey SET deleted = 1, version = version + 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{query.id} AND tenant_id = #{query.tenantId} AND version = #{query.version}
          AND status = 0 AND deleted = 0
    </update>
</mapper>
