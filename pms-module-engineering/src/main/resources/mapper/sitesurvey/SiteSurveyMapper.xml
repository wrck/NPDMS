<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.engineering.dal.mysql.sitesurvey.SiteSurveyMapper">
    <!-- REQ-PROJ-004: Owner facts only; never load the survey body or PRE02 JSON roots. -->
    <sql id="taskFactColumns">id, tenant_id, project_id, name, version, status, deleted</sql>
    <sql id="taskObjectScope">
        id = #{query.objectId} AND tenant_id = #{query.tenantId}
        AND project_id = #{query.projectId} AND deleted = 0
    </sql>
    <select id="selectTaskCandidates" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.SiteSurveyDO">
        SELECT <include refid="taskFactColumns"/> FROM pms_eng_site_survey
        WHERE tenant_id = #{query.tenantId} AND project_id = #{query.projectId} AND deleted = 0
        ORDER BY id DESC LIMIT #{query.limit}
    </select>
    <select id="selectTaskObject" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.SiteSurveyDO">
        SELECT <include refid="taskFactColumns"/> FROM pms_eng_site_survey
        WHERE <include refid="taskObjectScope"/>
    </select>
    <select id="selectTaskObjectForUpdate" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.sitesurvey.SiteSurveyDO">
        SELECT <include refid="taskFactColumns"/> FROM pms_eng_site_survey
        WHERE <include refid="taskObjectScope"/> FOR UPDATE
    </select>
    <update id="deleteDraft">
        UPDATE pms_eng_site_survey SET deleted = 1, version = version + 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{query.id} AND tenant_id = #{query.tenantId} AND version = #{query.version}
          AND status = 0 AND deleted = 0 AND outsource_request_id IS NULL
    </update>
</mapper>
