<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.engineering.dal.mysql.arrivalacceptance.ArrivalLineMapper">
    <sql id="columns">
        id, arrival_acceptance_id, line_no, line_revision, scope_type,
        device_id, device_assignment_version, order_line_id, product_code, model_code,
        expected_quantity, accepted_quantity, unit, status, current_marker, version,
        creator, create_time, updater, update_time, deleted, tenant_id
    </sql>
    <sql id="qualifiedColumns">
        l.id, l.arrival_acceptance_id, l.line_no, l.line_revision, l.scope_type,
        l.device_id, l.device_assignment_version, l.order_line_id, l.product_code, l.model_code,
        l.expected_quantity, l.accepted_quantity, l.unit, l.status, l.current_marker, l.version,
        l.creator, l.create_time, l.updater, l.update_time, l.deleted, l.tenant_id
    </sql>
    <sql id="currentWhere">
        tenant_id = #{query.tenantId}
        AND arrival_acceptance_id = #{query.arrivalAcceptanceId}
        AND current_marker = 1
        AND deleted = b'0'
    </sql>
    <select id="selectCurrentList" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.arrivalacceptance.ArrivalLineDO">
        SELECT <include refid="columns"/> FROM imp_arrival_line
        WHERE <include refid="currentWhere"/>
        ORDER BY CASE WHEN scope_type = 'DEVICE' THEN 0 ELSE 1 END,
                 device_id, order_line_id, arrival_acceptance_id, line_no, id
    </select>
    <select id="selectCurrentListForUpdate" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.arrivalacceptance.ArrivalLineDO">
        SELECT <include refid="columns"/> FROM imp_arrival_line
        WHERE <include refid="currentWhere"/>
        ORDER BY CASE WHEN scope_type = 'DEVICE' THEN 0 ELSE 1 END,
                 device_id, order_line_id, arrival_acceptance_id, line_no, id
        FOR UPDATE
    </select>
    <select id="selectConfirmedAcceptedByProject" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.arrivalacceptance.ArrivalLineDO">
        SELECT <include refid="qualifiedColumns"/>
        FROM imp_arrival_line l
        JOIN imp_arrival_acceptance a
          ON a.tenant_id = l.tenant_id
         AND a.id = l.arrival_acceptance_id
         AND a.deleted = b'0'
        WHERE l.tenant_id = #{query.tenantId}
          AND a.project_id = #{query.projectId}
          AND a.status = 'CONFIRMED'
          AND l.status = 'ACCEPTED'
          AND l.current_marker = 1
          AND l.deleted = b'0'
        ORDER BY CASE WHEN l.scope_type = 'DEVICE' THEN 0 ELSE 1 END,
                 l.device_id, l.order_line_id, l.arrival_acceptance_id, l.line_no, l.id
    </select>
</mapper>
