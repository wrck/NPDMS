<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.engineering.dal.mysql.arrivalacceptance.DeliveryEvidenceMapper">
    <sql id="columns">
        id, project_id, source_requirement, source_object_type, source_object_id,
        current_revision_no, acc_sync_status, acc_last_published_at, acc_next_retry_at,
        acc_retry_count, acc_last_event_id, acc_accepted_record_id, acc_archived_record_id,
        version, creator, create_time, updater, update_time, deleted, tenant_id
    </sql>
    <sql id="sourceWhere">
        tenant_id = #{query.tenantId}
        AND source_requirement = #{query.sourceRequirement}
        AND source_object_type = #{query.sourceObjectType}
        AND source_object_id = #{query.sourceObjectId}
        AND deleted = b'0'
    </sql>
    <select id="selectBySource" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.arrivalacceptance.DeliveryEvidenceDO">
        SELECT <include refid="columns"/> FROM imp_delivery_evidence
        WHERE <include refid="sourceWhere"/>
    </select>
    <select id="selectBySourceForUpdate" resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.arrivalacceptance.DeliveryEvidenceDO">
        SELECT <include refid="columns"/> FROM imp_delivery_evidence
        WHERE <include refid="sourceWhere"/>
        FOR UPDATE
    </select>
</mapper>
