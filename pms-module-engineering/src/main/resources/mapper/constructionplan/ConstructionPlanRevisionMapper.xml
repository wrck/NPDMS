<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.engineering.dal.mysql.constructionplan.ConstructionPlanRevisionMapper">

    <sql id="columns">
        id, plan_id, revision_no, calculation_basis_code, start_date, end_date,
        duration_days, source_change_id, frozen_at, effective_at, created_by,
        created_at, version, tenant_id
    </sql>

    <select id="selectForUpdate"
            resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.constructionplan.ConstructionPlanRevisionDO">
        SELECT <include refid="columns"/>
        FROM sol_construction_plan_revision
        WHERE tenant_id = #{query.tenantId}
          AND plan_id = #{query.planId}
          AND id = #{query.revisionId}
        FOR UPDATE
    </select>

    <select id="selectLatestForUpdate"
            resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.constructionplan.ConstructionPlanRevisionDO">
        SELECT <include refid="columns"/>
        FROM sol_construction_plan_revision
        WHERE tenant_id = #{query.tenantId}
          AND plan_id = #{query.planId}
        ORDER BY revision_no DESC, id DESC
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="selectPage"
            resultType="cn.iocoder.yudao.module.pms.engineering.dal.dataobject.constructionplan.ConstructionPlanRevisionDO">
        SELECT <include refid="columns"/>
        FROM sol_construction_plan_revision
        WHERE tenant_id = #{query.tenantId}
          AND plan_id = #{query.planId}
        <choose>
            <when test="query.cursorRevisionNo == null and query.cursorId == null"/>
            <when test="query.cursorRevisionNo != null and query.cursorId != null">
                AND (revision_no &lt; #{query.cursorRevisionNo}
                    OR (revision_no = #{query.cursorRevisionNo} AND id &lt; #{query.cursorId}))
            </when>
            <otherwise>
                AND 1 = 0
            </otherwise>
        </choose>
        ORDER BY revision_no DESC, id DESC
        LIMIT #{query.limit}
    </select>

</mapper>
