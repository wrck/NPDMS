<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.businessresult.ResultEvidenceMapper">
    <select id="selectScanForUpdate" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.businessresult.ResultEvidenceScanDO">
        SELECT * FROM proj_result_evidence_scan WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId}
          AND subscription_id=#{query.subscriptionId} AND subscription_version=#{query.subscriptionVersion} FOR UPDATE
    </select>
    <select id="selectById" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.businessresult.ResultEvidenceScanDO">
        SELECT * FROM proj_result_evidence_scan WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND id=#{query.id}
    </select>
    <insert id="insertScan">
        INSERT INTO proj_result_evidence_scan(id,tenant_id,project_id,subscription_id,subscription_version,through_sequence,after_candidate_id,accumulator,status,version)
        VALUES(#{row.id},#{row.tenantId},#{row.projectId},#{row.subscriptionId},#{row.subscriptionVersion},#{row.throughSequence},#{row.afterCandidateId},#{row.accumulator},#{row.status},#{row.version})
    </insert>
    <update id="advance">
        UPDATE proj_result_evidence_scan SET after_candidate_id=#{query.afterCandidateId},accumulator=#{query.accumulator},status=#{query.status},version=version+1
        WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND id=#{query.id}
          AND version=#{query.expectedVersion} AND status='COLLECTING' AND after_candidate_id &lt;= #{query.afterCandidateId}
    </update>
    <insert id="insertItem">
        INSERT INTO proj_result_evidence_item(id,tenant_id,project_id,scan_id,candidate_id,object_id,result_id,formation_sequence,eligibility,reason,observation)
        VALUES(#{row.id},#{row.tenantId},#{row.projectId},#{row.scanId},#{row.candidateId},#{row.objectId},#{row.resultId},#{row.formationSequence},#{row.eligibility},#{row.reason},#{row.observation})
    </insert>
    <select id="selectItems" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.businessresult.ResultEvidenceItemDO">
        SELECT * FROM proj_result_evidence_item WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId}
          AND scan_id=#{query.scanId} AND id &gt; #{query.afterId} ORDER BY id LIMIT #{query.limit}
    </select>
</mapper>
