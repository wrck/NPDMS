<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.satisfaction.SatisfactionAccessGrantMapper">
    <select id="selectByDigest" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.satisfaction.SatisfactionAccessGrantDO">
        SELECT * FROM acc_satisfaction_access_grant
         WHERE tenant_id=#{query.tenantId} AND token_digest=#{query.tokenDigest}
    </select>
    <select id="selectByDigestForUpdate" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.satisfaction.SatisfactionAccessGrantDO">
        SELECT * FROM acc_satisfaction_access_grant
         WHERE tenant_id=#{query.tenantId} AND token_digest=#{query.tokenDigest} FOR UPDATE
    </select>
    <select id="selectNextVersion" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(grant_version),0)+1 FROM acc_satisfaction_access_grant
         WHERE tenant_id=#{tenantId} AND questionnaire_id=#{questionnaireId}
    </select>
</mapper>
