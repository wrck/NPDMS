<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.satisfaction.SatisfactionResponseMapper">
    <select id="selectByIdentityForUpdate" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.satisfaction.SatisfactionResponseDO">
        SELECT * FROM acc_satisfaction_response
         WHERE tenant_id=#{query.tenantId} AND questionnaire_id=#{query.questionnaireId}
           AND request_id=#{query.requestId} FOR UPDATE
    </select>
    <select id="selectNextResponseNo" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(response_no),0)+1 FROM acc_satisfaction_response
         WHERE tenant_id=#{tenantId} AND questionnaire_id=#{questionnaireId}
    </select>
</mapper>
