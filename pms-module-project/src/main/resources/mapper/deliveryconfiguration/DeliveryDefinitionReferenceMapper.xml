<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.deliveryconfiguration.DeliveryDefinitionReferenceMapper">
  <delete id="deleteDraftReferences">
    DELETE ref FROM proj_delivery_definition_reference ref
    JOIN proj_delivery_definition_revision owner ON owner.id=ref.owner_revision_id AND owner.tenant_id=ref.tenant_id
    WHERE ref.tenant_id=#{query.tenantId} AND ref.owner_revision_id=#{query.ownerRevisionId}
    AND owner.revision_state='DRAFT' AND owner.deleted=0
  </delete>
</mapper>
