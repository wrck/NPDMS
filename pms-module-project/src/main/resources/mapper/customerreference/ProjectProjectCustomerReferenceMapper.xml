<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.customerreference.ProjectProjectCustomerReferenceMapper">
    <!-- 历史引用也必须保留；不能通过软删除记录绕过客户更正边界。 -->
    <select id="countReferences" resultType="long">
        SELECT (SELECT COUNT(*) FROM acc_project_deliverable WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND (source_definition_id IS NULL OR version &gt; 0 OR status &lt;&gt; 'PENDING' OR current_source_version_id IS NOT NULL))
             + (SELECT COUNT(*) FROM proj_project_task WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND (source_definition_id IS NULL OR version &gt; 0 OR status &lt;&gt; 'PENDING_ASSIGN'))
             + (SELECT COUNT(*) FROM acc_acceptance WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM acc_satisfaction_collection_task WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM acc_project_closure WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM proj_task_business_link WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM pms_acc_acceptance WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM pms_acc_archive_document WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM pms_acc_completion_certificate WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM pms_acc_deliverable_checklist WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
             + (SELECT COUNT(*) FROM pms_acc_project_closure WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId})
    </select>
</mapper>
