<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.acceptance.AccProjectDeliverableMapper">

    <select id="selectGateFactForUpdate"
            resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.acceptance.AccProjectDeliverableDO">
        SELECT *
        FROM acc_project_deliverable
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND deliverable_code = #{query.deliverableCode}
          AND deleted = b'0'
        FOR UPDATE
    </select>

    <select id="selectByProjectAndCodeForUpdate"
            resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.acceptance.AccProjectDeliverableDO">
        SELECT id, project_id, deliverable_code, name, stage_code, task_code, required,
               source_definition_id, status, current_source_version_id, archive_status, version,
               creator, create_time, updater, update_time, deleted, tenant_id
        FROM acc_project_deliverable
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND deliverable_code = #{query.deliverableCode}
          AND deleted = b'0'
        FOR UPDATE
    </select>

    <select id="selectByIdForUpdate"
            resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.acceptance.AccProjectDeliverableDO">
        SELECT *
        FROM acc_project_deliverable
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.deliverableId}
          AND deleted = b'0'
        FOR UPDATE
    </select>
    <update id="clearCurrentSource">
        UPDATE acc_project_deliverable
        SET current_source_version_id = NULL,
            archive_status = 'INVALID',
            version = version + 1,
            updater = #{query.updater},
            update_time = NOW()
        WHERE tenant_id = #{query.tenantId}
          AND id = #{query.deliverableId}
          AND current_source_version_id = #{query.expectedCurrentSourceVersionId}
          AND version = #{query.expectedRootVersion}
          AND deleted = b'0'
    </update>
    <sql id="planDefinitionScope">
        tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND id=#{query.id}
        AND version=#{query.expectedVersion} AND deleted=0
    </sql>
    <select id="selectTaskDeliverablesForUpdate" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.acceptance.AccProjectDeliverableDO">
        SELECT * FROM acc_project_deliverable
        WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND task_code=#{query.taskCode} AND deleted=0
        ORDER BY id FOR UPDATE
    </select>
    <!-- Temporary codes are transaction-local and allow swaps without changing instance identity. -->
    <update id="stagePlanCodeForRename">
        UPDATE acc_project_deliverable SET deliverable_code=CONCAT('~plan:',id)
        WHERE <include refid="planDefinitionScope"/>
    </update>
    <update id="updatePlanDefinition">
        UPDATE acc_project_deliverable SET deliverable_code=#{query.definition.deliverableCode},
            name=#{query.definition.name},stage_code=#{query.definition.stageCode},task_code=#{query.definition.taskCode},
            required=#{query.definition.required},version=version+1,updater=#{query.updater},update_time=CURRENT_TIMESTAMP(3)
        WHERE <include refid="planDefinitionScope"/>
    </update>
    <sql id="unhandledPlanDefinition">
        status='PENDING' AND current_source_version_id IS NULL
            AND NOT EXISTS (SELECT 1 FROM acc_project_deliverable_source_version s
                WHERE s.tenant_id=#{query.tenantId} AND s.deliverable_id=acc_project_deliverable.id)
            AND NOT EXISTS (SELECT 1 FROM acc_acceptance a
                WHERE a.tenant_id=#{query.tenantId} AND a.deliverable_id=acc_project_deliverable.id)
            AND NOT EXISTS (SELECT 1 FROM acc_satisfaction_collection_task t
                WHERE t.tenant_id=#{query.tenantId} AND t.deliverable_id=acc_project_deliverable.id)
    </sql>
    <select id="selectPlanDefinitionsForUpdate" resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.acceptance.AccProjectDeliverableDO">
        SELECT * FROM acc_project_deliverable
        WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND deleted=0 ORDER BY id FOR UPDATE
    </select>
    <select id="selectRetirablePlanDefinitionIds" resultType="java.lang.Long">
        SELECT id FROM acc_project_deliverable
        WHERE tenant_id=#{query.tenantId} AND project_id=#{query.projectId} AND deleted=0
          AND <include refid="unhandledPlanDefinition"/> ORDER BY id
    </select>
    <update id="retireUnhandledForPlan">
        UPDATE acc_project_deliverable SET deleted=1,version=version+1,updater=#{query.updater},update_time=CURRENT_TIMESTAMP(3)
        WHERE <include refid="planDefinitionScope"/> AND <include refid="unhandledPlanDefinition"/>
    </update>
</mapper>
