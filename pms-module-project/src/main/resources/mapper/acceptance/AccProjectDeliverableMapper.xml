<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.acceptance.AccProjectDeliverableMapper">

    <select id="selectGateFactForUpdate"
            resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.acceptance.AccProjectDeliverableDO">
        SELECT *
        FROM acc_project_deliverable
        WHERE tenant_id = #{query.tenantId}
          AND project_id = #{query.projectId}
          AND deliverable_code = #{query.deliverableCode}
          AND deleted = b'0'
        FOR UPDATE
    </select>

</mapper>
