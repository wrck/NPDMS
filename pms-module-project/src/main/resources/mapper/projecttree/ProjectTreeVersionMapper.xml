<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.pms.project.dal.mysql.projecttree.ProjectTreeVersionMapper">

    <sql id="columns">
        id, root_project_id, tree_version, status, change_batch_id, node_count,
        path_count, activated_at, failed_reason, version,
        creator, create_time, updater, update_time, deleted, tenant_id
    </sql>

    <select id="selectLatestActive"
            resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.projecttree.ProjectTreeVersionDO">
        SELECT <include refid="columns"/>
        FROM proj_project_tree_version
        WHERE root_project_id = #{rootProjectId}
          AND status = 'ACTIVE'
          AND deleted = b'0'
        ORDER BY tree_version DESC, id DESC
        LIMIT 1
    </select>

    <select id="selectLatest"
            resultType="cn.iocoder.yudao.module.pms.project.dal.dataobject.projecttree.ProjectTreeVersionDO">
        SELECT <include refid="columns"/>
        FROM proj_project_tree_version
        WHERE root_project_id = #{rootProjectId}
          AND deleted = b'0'
        ORDER BY tree_version DESC, id DESC
        LIMIT 1
    </select>
</mapper>
