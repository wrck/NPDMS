# PROJ领域需求：项目治理

> 文档状态：正式基线（来源PRD V1.8）<br>
> 来源基线：`需求/PRD-项目实施交付管理平台.md`（V1.8，正式基线）<br>
> 领域编码：`PROJ`<br>
> 业务Owner：项目治理<br>
> 详细需求：13项

## 1. 领域目标与边界

项目主档、项目层级、任务WBS、计划、团队、风险、阶段和项目关闭。

本领域只定义自身拥有的业务规则、数据、权限和验收标准；跨领域能力通过需求依赖、输入输出和事件协作，不复制其他领域Owner的业务事实。

## 2. 需求清单

| 需求编号 | PRD来源 | 需求名称 | 目标版本 | 优先级 |
| --- | --- | --- | --- | --- |
| PM-01 | 项目创建与指派 | 项目创建与指派 | V1 | P0 |
| PM-07 | 项目业务属性识别与分类 | 项目业务属性识别与分类 | V1 | P1 |
| PM-03 | 项目模板与阶段门禁 | 项目模板与阶段门禁 | V1 | P0 |
| PM-08 | 服务经理自动指派 | 服务经理自动指派 | V1手动指派；V2自动指派 | P1 |
| PM-09 | 人员批量变更 | 人员批量变更 | V2 | P1 |
| PM-02 | 主子项目管理与进度汇总 | 主子项目管理与进度汇总 | V1 | P0 |
| PM-04 | 多级项目权限与拆分 | 主子项目权限与拆分 | V1 | P1 |
| PM-11 | 项目任务管理 | 项目任务管理 | V1任务层级模型、阶段/任务WorkBinding运行时与基础查询；V2甘特展示与受控任务依赖维护 | P1 |
| PM-10 | 项目回退与关闭 | 项目回退与异常关闭 | V1 | P1 |
| PROJ-12 | 项目组合管理 | 项目组合管理 | V2 | P1 |
| PM-05 | 借货项目转销管理 | 借货项目转销管理 | V2 | P1 |
| PM-06 | 项目合同与实施范围追加合并 | 项目合同与实施范围追加合并 | V2 | P1 |
| INT-01 | CRM/ERP项目同步 | CRM/ERP项目同步 | V1 | P0 |

## 3. 详细需求

## PM-01 项目创建与指派

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-01 |
| 所属阶段 | S0 |
| 用户角色 | 工程管理部、服务经理 |
| 优先级 | P0 |
| 目标版本 | V1 |
| 复杂度 | 中 |
| 依赖条件 | 内部强前置：部门主数据（办事处承载为部门）、AST地点主数据（Address/Site/SiteLocation）；集成依赖：CRM/ERP接口（可降级） |

**业务场景与需求描述：**
当CRM执行单或ERP销售订单生成并通过接口同步至平台时，系统应自动创建项目；当接口不可用、紧急立项或内部项目等场景时，工程管理部可手动创建项目；两种方式生成同一种项目业务对象。项目分类必须拆分为四个独立维度：签约方式、项目类别、项目实施方式和CRM重大项目级别（以下简称"项目四维业务属性"），禁止用单一"项目类型/级别"字段混载。CRM来源项目以CRM/渠道订单提供的签约方式、实施方式和重大项目级别为权威值；项目类别由平台维护，CRM重大项目命中时默认初始化为工程类但两个字段分别保存。手工项目由工程管理部受控选择签约方式、项目类别和项目实施方式，CRM重大项目级别保持空（界面显示"不适用"）。

项目创建时按四维条件匹配一个生效模板，冻结模板版本及其流程定义key引用，再实例化阶段、任务、里程碑、交付件和门禁。审批流程在实际发起时由BPM确定定义：默认按流程定义key选取最新生效定义，授权用户也可显式选择同key的历史`processDefinitionId`。生命周期状态码表：ACTIVE（进行中）/ NORMAL_CLOSED（正常闭环）/ NO_TRACKING_CLOSED（不予跟踪闭环）/ EXCEPTION_CLOSED（异常关闭）。多省份节点场景下，服务经理（办事处级语境）为下单办事处服务经理，服务经理（本地实施级语境）为各实施地点服务经理；单省份内场景下，服务经理为实施地点服务经理。

**核心业务规则：**
1. 项目可由CRM执行单/ERP销售订单自动创建，也可由工程管理部手动创建；两种方式生成同一种项目业务对象，并统一初始化为`lifecycle_status=ACTIVE`、`current_stage=S0`（待开始）；尚未完成主责指派时`assignment_status=UNASSIGNED`（待指派），不得形成只存在于接口侧或手工台账中的旁路项目。
2. 自动创建按"来源系统+来源业务单号"幂等处理：同一CRM执行单或ERP销售订单重复推送时更新原同步记录，不重复生成项目；CRM执行单与ERP销售订单属于同一交付事项时关联到同一项目并分别保留来源映射。CRM来源项目默认沿用CRM项目编码，同一CRM项目下多个合同、订单和执行单通过独立关系关联，不派生多个项目编码；无CRM项目编码时才按平台编码规则生成。
3. 自动创建优先采用来源系统权威字段；手动创建必须记录创建原因和创建人。接口恢复后只能将来源单据补关联到现有手工项目（补录仅补空字段，不覆盖已人工录入字段并记录补录日志），不得以同步记录覆盖平台已形成的交付过程数据。
4. 项目创建必须绑定一个生效的项目模板。模板按签约方式、项目类别、项目实施方式和重大项目级别独立匹配；手动创建允许从命中模板中选择并预览。无匹配模板时拒绝创建；存在多个同优先级合法候选时须授权用户显式选择一个模板后方可创建（修订003）。阶段、任务、里程碑、交付件和门禁以创建时模板版本实例化；流程只冻结定义key引用，实际审批实例以BPM启动时选定的`processDefinitionId`为准。
5. 所有成功的正式项目创建路径必须在同一事务写入项目记录、`INITIAL_CREATE`模板匹配决策历史、模板冻结和全部实例要素，任一步失败整体回滚，不持久化项目或任何项目创建草稿、不生成项目编码、不进入S0、不实例化阶段任务（修订001/003）。手动创建失败时服务端返回明确错误和可修正项；页面仅可在当前未刷新会话内保留已输入表单值，平台不写服务端草稿、浏览器持久化存储或离线草稿（修订001）。正式创建使用幂等键，失败请求按幂等状态和错误分类处理，修正业务输入后以新幂等键重新提交（修订001）。
6. V1由工程管理部确认或手动指定服务经理：行政区划与服务办事处映射（`area_code + area_level → department_code`）仅用于候选过滤和提示，最终服务经理由授权人员人工确认（修订002）；多省份节点同时记录下单办事处服务经理（办事处级语境）和各实施地点服务经理（本地实施级语境），单省份项目记录实施地点服务经理。V2自动指派不得改变V1形成的角色层级和数据范围（修订002：V2自动化范围不提前实现）。项目创建后应完成项目经理初始指派：V1由具备项目指派权限的服务经理或工程管理部授权人员在PM-01流程内人工完成；PM-09仅承载V2批量变更，不作为V1初始指派前置。
7. 签约方式、项目类别、项目实施方式、重大项目级别和创建原因使用独立基础平台字典/来源映射。CRM同步不得写入或覆盖平台维护的项目类别；重大项目级别后续修正按PM-07追加`SOURCE_CORRECTION`决策历史并保存只读影响结论，不自动更换冻结模板或重新实例化交付事实（修订003）。
8. 组织与地点承载（修订002）：办事处承载为组织架构中的部门（统一编码字段`system_dept.code`，跨模块关系与快照统一使用`department_id/department_code/department_name`，不引入`office_code`等变体），不建立独立办事处主数据；实施地点以AST拥有的Address/Site/SiteLocation承载，项目与站点为多对多关系并可指定一个当前主站点；站点未维护时保留文本位置作为受控降级并标记地点解析状态（UNRESOLVED），未解析文本不得参与自动办事处解析、自动服务经理指派或结构化数据权限判断；行政区划—部门映射缺失、停用或无效不阻断项目创建。

9. 项目经理初始指派必须建立唯一有效责任区间并记录指派人、生效时间和依据；服务经理或项目经理任一主责缺失时，`assignment_status`保持`UNASSIGNED`，S0不得准出。两类主责均有效后才更新为`ASSIGNED`，通知失败不回滚已成功指派。

**用户故事：**
作为工程管理部，我希望CRM执行单和ERP销售订单生成后系统自动创建项目并按规则指派服务经理，以便项目自动入库、减少人工录入并确保指派规则一致。

作为工程管理部，我希望ERP销售订单同步后自动补全项目商务信息（合同号、金额、设备清单），以便项目商务数据完整。

作为工程管理部，我希望在接口不可用、紧急立项或内部项目等场景下手动创建项目、录入关键信息并选择预览项目模板，以便所有项目纳入平台统一管理并按业务场景加载阶段任务、里程碑、交付件与门禁规则。

作为工程管理部，我希望接口同步失败时系统告警并支持人工补录与重试，接口恢复后能将手工项目与CRM执行单/ERP销售订单关联补录，以便异常可处理且数据源头一致。

作为服务经理，我希望项目创建后按办事处与实施地点规则指派到我名下，并为责任项目完成人工项目经理初始指派，以便S0形成完整责任闭环。

**验收标准：**
- **WHEN** CRM执行单状态变更为"已下单"并通过接口同步至平台
- **THEN** 平台接收执行单数据并校验完整性（客户、办事处、设备清单等必填字段），校验通过后创建项目记录
- **AND** CRM来源项目默认沿用CRM项目编码，同一CRM项目下多个合同、订单和执行单通过独立关系关联、不派生多个项目编码；无CRM项目编码时按平台编码规则生成唯一项目编码
- **AND** 项目初始化为生命周期状态ACTIVE（进行中）、当前阶段S0（待开始）；尚未完成主责指派时指派状态为待指派（UNASSIGNED）
- **AND** 分别加载项目所属办事处（部门）、客户、合同、签约方式、实施方式和CRM重大项目级别，并初始化平台项目类别
- **AND** 按四维业务属性匹配项目模板（PM-03/PM-07）：唯一候选自动决定，多个合法候选由工程管理部显式选择；模板匹配决策历史、模板冻结与全部实例要素在同一事务写入，任一步失败整体回滚（修订003）
- **AND** 按办事处与实施地点规则确定候选服务经理并完成指派（V1由行政区划映射提供候选过滤与提示、授权人员人工确认，V2自动指派）
- **WHEN** ERP销售订单同步至平台
- **THEN** 平台按CRM执行单号或合同号匹配已创建项目并建立关联
- **AND** 补全项目商务信息（合同号、合同金额、销售订单号、设备清单、下单时间）
- **AND** 触发项目业务属性识别与分类（PM-07）及服务经理指派（PM-08）
- **WHEN** 工程管理部在平台内发起"手动创建项目"操作
- **THEN** 系统提供项目创建表单，支持录入项目名称、客户、合同号、办事处、实施地点、市场部、系统部、拓展部、子行业、签约方式、项目类别和实施方式；无CRM来源时重大项目级别保持空（界面显示"不适用"）
- **AND** 表单内提供"项目模板"选择项，展示四维条件和业务场景命中的启用模板列表（PM-03），支持预览模板的阶段定义、任务清单、里程碑节点、交付件要求和准入准出门禁
- **AND** 必填字段校验、模板选择与实例化全部成功后生成项目编码与正式项目记录，状态初始化为ACTIVE/S0（待开始），记录创建方式=手动创建、创建人与创建时间、模板加载方式=手动选择；任一环节失败则拒绝创建，返回明确错误和可修正项，不生成项目编码、不进入S0、不实例化阶段任务、不持久化项目或任何创建草稿（修订001）
- **AND** 未显式选择模板时按四维条件和模板优先级匹配唯一默认模板；无匹配时拒绝创建，多匹配时须显式选择后创建，不静默套用错误模板
- **AND** 正式创建使用幂等键，失败请求按幂等状态和错误分类处理，修正业务输入后以新幂等键重新提交；页面可在当前未刷新会话内保留已输入表单值供修正重提，平台不写服务端草稿、浏览器持久化存储或离线草稿（修订001）
- **AND** 按规则指派服务经理（与自动创建一致：多省份节点/单省份内规则）
- **AND** V1由具备指派权限的服务经理或工程管理部授权人员选择合法在职人员完成项目经理初始指派；服务经理和项目经理均存在有效责任区间后，项目`assignment_status`方可更新为`ASSIGNED`并满足S0准出
- **WHEN** 接口恢复后将手工创建项目与CRM执行单/ERP销售订单关联补录
- **THEN** 平台按项目编码或合同号匹配源数据，建立来源关联并补全CRM执行单号、ERP销售订单号、设备清单等字段
- **AND** 补录仅补空字段、不覆盖已人工录入字段并记录补录日志；发现同一来源已关联其他项目时转人工核对，禁止自动覆盖或重复创建
- **WHEN** 同一销售订单关联多个实施地点或多个办事处
- **THEN** 系统按多省份节点规则指派服务经理（办事处级语境）（下单办事处）与服务经理（本地实施级语境）（各实施地点）
- **AND** 单省份内场景指派实施地点服务经理
- **WHEN** CRM/ERP接口同步失败或数据校验不通过
- **THEN** 系统记录失败原因（字段缺失、接口超时、数据冲突等）并触发告警通知工程管理部，支持人工补录与接口重试或转手动创建
- **AND** 同步记录保持"失败/待补偿"，不生成信息不完整的正式项目；失败记录可在"同步异常列表"中查询与处理

**涉及数据字段：**
项目编码（CRM沿用/平台生成）、CRM项目编码、CRM执行单号、ERP销售订单号、合同号、客户名称、办事处（department_id/department_code/department_name）、实施地点（site_id/site_location_id、地点解析状态）、市场部、系统部、拓展部、子行业、签约方式、项目类别、项目实施方式、CRM重大项目级别、各字段来源系统/来源业务键/来源原值/映射码/同步版本/生效时间、项目模板编码及版本、BPM流程定义key、审批实例实际processDefinitionId、模板加载方式、模板匹配候选结果与决策方式、服务经理、项目经理、当前阶段、生命周期状态、指派状态、创建方式、创建原因、创建人和时间、幂等标识、补录日志

**权限与数据范围：**
工程管理部可手动创建项目、选择模板、补录来源映射并在V1确认服务经理；具备项目指派权限的服务经理或工程管理部授权人员可在授权范围完成人工项目经理初始指派。服务经理可查看被指派项目及确认本人承担的服务范围，但不得修改CRM/ERP权威来源字段或替换创建基线。普通项目成员只能在获分配项目后查看项目实例，无权创建项目。所有查询按租户和办事处（部门）授权过滤；多省份项目的服务经理（本地实施级语境）只查看本人负责实施地点及其项目节点，服务经理（办事处级语境）按授权查看项目汇总。

**异常、降级及留痕要求：**
CRM/ERP接口不可用或关键字段缺失时，同步记录保持"失败/待补偿"，不得生成信息不完整的正式项目；工程管理部可按手动创建方式建立项目并标记"来源待关联"。没有可用模板、同优先级多匹配未显式选择、模板引用失效或必填校验失败时拒绝创建并返回可修正项，不持久化项目或任何创建草稿（修订001）。组织数据无法确定服务经理或尚未形成有效项目经理责任区间时不阻断项目创建，但项目指派状态保持"待指派"，由工程管理部/服务经理按PM-08及PM-01人工补齐；未同时完成两类主责指派不得准出S0。接口恢复后按来源业务单号重试并关联现有项目，发现同一来源已关联其他项目时转人工核对，禁止自动覆盖或重复创建。平台记录来源报文标识、创建方式、失败原因、重试记录、人工补录内容、模板版本、指派前后值、幂等键和操作人时间。

**依赖关系：**
- 前置依赖：INT-01 CRM/ERP项目同步接口、部门主数据（办事处承载为部门）、AST地点主数据
- 后续依赖：PM-03项目模板加载、PM-07项目业务属性识别与分类、PM-08服务经理指派规则
## PM-07 项目业务属性识别与分类

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-07 |
| 所属阶段 | S0 |
| 用户角色 | 工程管理部 |
| 优先级 | P1 |
| 目标版本 | V1 |
| 复杂度 | 中 |
| 依赖条件 | 内部强前置：PM-03项目模板 |

**业务场景与需求描述：**
当项目创建时，系统应分别确定签约方式、项目类别、项目实施方式和CRM重大项目级别四个独立维度（项目四维业务属性），作为模板匹配的四项确定输入，并以模板匹配决策历史承接创建时匹配决策与创建后属性变化的影响追溯（修订003）。签约方式表达直签/非直签关系；项目类别表达普通/工程管控分类；实施方式表达原厂直服、原厂督导或代理商自服；重大项目级别只承接CRM权威属性及历史。四个维度可作为模板匹配或人工选模依据，但不得合并为一个"项目级别"或互相反向推导。

**核心业务规则：**
1. 四维属性当前值复用项目既有字段分别保存稳定编码与显示值，不新增同义当前值字段（修订003）；项目类别由平台维护，其他CRM来源属性按来源业务键、原值、映射码和生效时间留痕，字段Owner与来源版本由模板匹配决策历史的前后快照承接（修订003）。
2. CRM命中重大项目级别时，平台默认把项目类别初始化为工程类，但重大项目级别仍保存CRM原属性、项目类别仍保存平台字段，两个字段分别保存；后续受控调整项目类别不得反写CRM或覆盖来源历史。
3. 无CRM来源的手工项目，CRM重大项目级别保存为NULL并在界面显示"不适用"，不新增或臆造`NOT_APPLICABLE`字典码（修订003）；工程管理部可选择签约方式、项目类别和实施方式，不得人工伪造CRM重大项目级别。
4. 模板候选结果与决策方式分开管理（修订003）：候选结果为`NO_MATCH/UNIQUE/MULTIPLE_MATCHES`，决策方式为`AUTO_UNIQUE/EXPLICIT_SELECTION`；无匹配拒绝创建，唯一候选自动决定，多个合法候选仅在授权用户显式选择其中一个模板修订后允许创建。
5. 所有成功的正式项目创建路径在同一事务写入项目、`INITIAL_CREATE`模板匹配决策历史、模板冻结和全部实例要素；任一步失败整体回滚，不持久化项目或项目草稿（修订003）。
6. 创建后发生来源修正或人工调整时，更新既有当前值字段，调用相同匹配器只读判定影响，并分别追加`SOURCE_CORRECTION`或`MANUAL_ADJUSTMENT`决策历史；不得自动更换冻结模板或重新实例化已生成的阶段、任务、审批和交付件（修订003）。
7. 模板匹配决策历史永久、只追加保存四属性前后快照、属性Owner、来源与映射版本、匹配器版本、候选结果、决策方式、最终模板修订、影响结论、操作者、原因、时间、请求摘要、幂等标识和稳定`operationId`（修订003）；影响结论为只读保存，不产生独立影响表、CHG事件或工单，未来CHG-01从历史派生工单时独立立项。

**用户故事：**
作为工程管理部，我希望分别查看和维护四个有明确Owner的项目业务属性，以便模板匹配准确且不会把CRM重大项目级别与平台工程分类混淆。

**验收标准：**
- **WHEN** CRM同步签约方式、实施方式和重大项目级别且来源映射有效
- **THEN** 平台分别保存三个来源属性、来源版本与历史（由模板匹配决策历史前后快照承接）；重大项目命中时另行初始化平台项目类别为工程类，两个字段分别保存
- **WHEN** 工程管理部创建无CRM来源项目
- **THEN** 平台允许选择签约方式、项目类别和实施方式，重大项目级别保存NULL并显示"不适用"，记录手工创建依据
- **WHEN** 来源属性未映射、项目类别缺失或模板无匹配
- **THEN** 平台拒绝创建并返回具体冲突字段，不静默选模、不实例化模板；存在多个合法候选时须授权用户显式选择模板修订后方可创建，选择结果记入决策历史
- **WHEN** CRM后续修正重大项目级别或平台受控调整项目类别
- **THEN** 平台分别记录并追加SOURCE_CORRECTION或MANUAL_ADJUSTMENT决策历史（含四属性前后快照与只读影响结论），不互相覆盖，不自动更换冻结模板或重新实例化交付事实；未来CHG-01从决策历史派生工单时独立承接

**涉及数据字段：**
签约方式、项目类别、项目实施方式、CRM重大项目级别、字段Owner、来源系统、来源业务键、来源原值、映射码、同步版本、生效时间、分类依据、模板匹配决策历史（候选结果NO_MATCH/UNIQUE/MULTIPLE_MATCHES、决策方式AUTO_UNIQUE/EXPLICIT_SELECTION、匹配器版本、最终模板修订、影响结论、操作者、原因、时间、请求摘要、幂等标识、operationId）

**权限与数据范围：**
工程管理部可在授权范围维护平台项目类别和手工项目的签约/实施方式；CRM来源属性只允许同步服务按来源版本写入，业务人员不能覆盖。项目经理和服务经理可查看四维属性及模板匹配依据。字典与映射配置仅限授权配置岗，跨租户不可见。

**异常、降级及留痕要求：**
CRM字段同步失败、来源映射异常或模板匹配失败时，创建路径直接拒绝并返回冲突项，不以默认值覆盖来源；创建后的调整失败时不改变当前值与冻结模板。工程管理部可补齐平台项目类别后重新提交，但不能伪造CRM重大项目级别。平台记录来源字段快照、映射版本、人工选择、四维变更、模板差异、失败原因和处理人时间，由模板匹配决策历史与操作留痕承接。

**依赖关系：**
- 前置依赖：PM-03项目模板（差异化里程碑配置）
- 关联依赖：PM-01项目创建、识别规则配置、合同与设备数据；INT-01/INT-03（来源同步与对账）、CHG-01（变更工单，未来从决策历史派生时独立承接）
## PM-03 项目模板与阶段门禁

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-03 |
| 所属阶段 | 全局（S0阶段初始化，贯穿全生命周期） |
| 用户角色 | 工程管理部、管理员 |
| 优先级 | P0 |
| 目标版本 | V1 |
| 复杂度 | 高 |
| 依赖条件 | 无（平台核心骨架，首批建设） |

**业务场景与需求描述：**
平台应建设统一、版本化的项目交付流程配置中心。项目模板、阶段定义、任务定义、阶段前置/后置关系、阶段/任务交付件要求、里程碑、准入准出门禁、权限策略、完成规则和工作绑定均作为通用配置能力提供。不同交付场景通过模板引用已发布配置组成项目流程，不在项目状态机、页面或领域模块中硬编码固定阶段、固定任务和固定交付件。

模板运行结构统一为：

```text
ProjectTemplateVersion
  ├── TemplateStageDefinition[]
  │     ├── StageWorkBinding
  │     ├── StageDeliverableRequirement[]
  │     └── TemplateTaskDefinition[]
  │           ├── TaskWorkBinding
  │           └── TaskDeliverableRequirement[]
  └── StageTransitionDefinition[]（前置阶段 → 后置阶段）
```

ProjectStage和ProjectTask均可绑定特定业务实体。`WorkBinding`选择已发布的业务实体类型、实例解析策略和`BusinessViewKey`，项目工作区据此装载不同业务操作界面；同一业务实体类型可以注册多个受控视图。模板只能引用平台注册表中的实体、组件、动态表单、审批或组合视图，不得保存任意前端代码、脚本或未经注册的外部URL。绑定实体的数据、状态和业务命令仍由对应领域Owner管理，项目交付域只保存稳定引用、配置快照和完成判定结果。

模板不重复保存BPM审批节点清单，而是引用已发布BPM流程定义key。项目初始化冻结模板版本和key引用；实际发起审批时默认按key选择最新生效定义，授权发起人也可显式选择同key历史`processDefinitionId`。流程实例保存实际`processDefinitionId`和完整`taskDefinitionKey`，PMS不得解析、拼装或维护独立流程版本号。

**核心业务规则：**
1. 通用配置中心至少提供项目模板、阶段、任务、阶段转移、阶段/任务交付件要求、里程碑、工作绑定、权限策略、完成规则和门禁引用的创建、复制、校验、发布、停用及版本查询能力。已发布定义通过稳定编码和版本唯一标识，可被多个模板复用。
2. 模板版本按签约方式、项目类别、实施方式和重大项目级别等独立条件匹配。模板通过`TemplateStageDefinition`和`TemplateTaskDefinition`引用已发布阶段/任务定义，并可在允许范围内覆盖责任角色、计划工期、交付件要求、门禁和绑定参数；发布时把精确引用版本解析为不可变模板快照。
3. 阶段前置和后置关系统一由`StageTransitionDefinition`表达，至少保存来源阶段、目标阶段、转移条件、优先级、默认分支和版本。配置界面可以按阶段维护前置或后置关系，但底层不得分别保存两套可独立修改的前置列表和后置列表；两种视图必须从同一组转移关系派生。
4. 模板发布前必须校验：存在唯一开始阶段和可达的正常收口阶段；阶段编码在模板内唯一；所有阶段均可从开始阶段到达；不存在循环、悬空关系和不可达节点；每个非收口阶段至少有一条后置关系。当前状态模型只允许一个`current_stage`，因此并行激活多个项目阶段不属于当前能力；存在多条后置分支时，条件、优先级和默认分支必须保证每次推进唯一解析出一个目标阶段，否则模板不得发布或项目不得推进。
5. `DeliverableRequirementDefinition`统一支持`STAGE`和`TASK`两种归属范围，至少配置交付件类型、必传/选传、数量、允许来源、文件或业务产出类型、确认/审批要求、完成规则和版本。阶段级要求与任务级要求分别实例化；同一来源业务记录或文件版本可以通过满足关系同时满足多个要求，不得复制文件或伪造第二份交付事实。
6. 已发布模板、阶段、任务、转移关系、交付件要求、绑定和规则不得原位修改；调整必须形成新版本。新版本默认只影响其生效后创建的项目，既有项目继续使用创建时冻结快照；既有项目确需换模或调整流程图时必须完成影响评估、审批、数据迁移和回退准备。
7. 自动创建按四维条件和优先级匹配默认生效模板；手动创建只能选择当前业务属性适用的生效模板。无匹配时拒绝创建；多个同优先级合法候选须由授权用户显式选择。项目、模板匹配决策历史、阶段实例、转移关系、任务、里程碑、阶段/任务交付件要求、门禁和工作绑定必须在同一创建事务写入，任一步失败整体回滚。
8. 阶段推进统一执行：重新校验当前阶段`CompletionRule`和准出门禁；读取当前阶段有效出向转移；按冻结条件、优先级和默认分支唯一解析目标阶段；校验目标阶段准入条件；最后在同一受控动作中关闭当前阶段并更新`current_stage`。任何一步失败均保持原阶段，返回具体未满足项、候选分支或冲突关系，不得以按钮点击、待办完成或消息发送代替推进事实。
9. 每个可执行ProjectStage和ProjectTask必须且只能有一个当前有效主`WorkBinding`。阶段缺省使用`STAGE_NATIVE`，任务缺省使用`TASK_NATIVE`；需要组合多个业务实体时使用`COMPOSITE`主绑定引用多个已发布子视图，不允许为同一节点配置多个相互竞争的主执行入口。
10. `WorkBinding`至少支持原生、业务实体、业务组件、动态表单、审批和组合六类，并保存绑定归属（阶段/任务）、业务实体类型、实例解析策略（引用既有、进入时幂等创建、首次操作时幂等创建或只读聚合）、稳定目标引用映射、`BusinessViewKey`、组件/表单版本和上下文参数。创建实例必须使用项目、阶段/任务及业务范围生成稳定幂等键。
11. 业务实体及操作界面由对应领域通过受控注册表发布。注册信息至少包括实体类型编码、Owner领域、视图键、组件版本、上下文契约、支持操作模式、状态查询与命令接口、权限校验接口和可用状态。模板只能选择已发布且兼容的实体/视图组合；实体或视图停用只禁止新模板使用，不得破坏既有项目冻结版本的历史解释和只读访问。
12. `PermissionPolicy`只声明工作台所需操作策略，最终权限由服务端按用户、租户、项目树、阶段/任务、目标业务实体、字段/文件范围和当前状态重新计算。前端组件是否显示不得作为授权依据，模板配置不得扩大领域对象权限。
13. `CompletionRule`同时适用于阶段和任务。原生绑定校验节点自身状态和必填数据；其他绑定校验目标业务实体事实、审批结果、动态表单版本、子任务、交付件满足关系或门禁快照。非原生绑定不得通过通用“完成”操作绕过目标业务事实；业务事实换版或失效时按冻结规则重新计算完成状态。
14. 项目工作区导航只由ProjectStage和ProjectTask实例投影，不建立第二套菜单或导航配置。点击阶段时加载阶段通用信息、前后置关系、阶段交付件及阶段`WorkBinding`视图；点击任务时加载任务通用信息、任务交付件及任务`WorkBinding`视图。项目概览继续作为独立项目级入口。
15. BPM只承载模板中需要审批的子流程，不替代项目阶段关系、阶段/任务状态机和门禁。工程类二线审核与重大项目总部复审是否出现、顺序和候选人由实际启动的BPM流程定义决定，流程定义身份继续遵循key、实际`processDefinitionId`和完整`taskDefinitionKey`规则。
16. 基线可预置直签/非直签×工程/普通、售前测试等模板。售前预置模板配置`S0→S4`及EXE-03/EXE-04，不配置终验依赖；未纳入关系图的阶段不实例化。典型交付模板可配置初验、终验及相应交付件，但不得在模板发布或运行中因S5、签约方式或固定任务码强制添加业务规则。
17. 模板编排中的业务强制校验统一作为可版本化配置：适用范围、前置、必做任务、必传交付件、完成条件、审批条件、触发时点、阈值与条件组合均由配置声明。引擎只解释已注册的事实谓词和Owner命令；配置明确未要求某项业务事实时不强制补造，已配置规则引用缺失、无权、失效或无法判定时不能按未配置放行。租户隔离、服务端授权、引用/图结构有效性、状态受控迁移、事务与不可变历史仍是不可关闭的完整性约束。
18. 受控触发动作与只读条件判定分离。WorkBinding优先复用引用既有、进入时幂等创建、首次操作时幂等创建和只读聚合策略；审批或其他Owner事实触发须明确来源事实及版本、适用条件、目标解析、调用身份和稳定幂等键。条件查询、readiness预览和视图渲染不创建实体；创建命令成功不等于实体完成。不得配置“实体完成才允许进入节点”同时又只在“进入成功后创建该实体”的相互等待。
19. 终验由ACC拥有业务身份、报告、状态与结果，保留项目、业务范围和来源上下文；不以特定ProjectTask或S5为存在前提。节点需办理终验时配置WorkBinding及业务视图；节点只消费终验结果时配置CompletionRule/Gate引用，无需改变其主绑定。多个规则可引用同一明确终验身份；同一触发重放不重复创建，不把不同业务意图仅因同项目而合并。验收范围由PROJ/COM/ACC公开契约锁定真实范围及版本，不伪造阶段或由报告上传反推范围。

**售前测试边界（修订016）：** 售前模板仅实例化S0/S4，S4仅包含EXE-03/04及其真实交付要求；不实例化EXE-01/02、S5或S6。满足冻结完成规则后从S4使用CLO-01/02正常闭环，保留`closed_from_stage=S4`。该裁剪不取消设备、项目、凭证、命令和文件授权。

**用户故事：**
作为模板负责人，我希望在统一配置中心复用阶段、任务、交付件、前后置关系、门禁和业务界面绑定来组装项目模板，以便不同项目场景灵活形成交付流程而无需修改代码。

作为领域配置负责人，我希望发布受控的业务实体类型和业务视图契约，供阶段或任务选择绑定，以便领域操作界面能在项目工作区复用且不丢失领域Owner和权限边界。

作为项目成员，我希望点击项目阶段或任务即可进入与其业务实体匹配的操作界面，并由实际业务结果自动判定完成和阶段推进，以便在统一上下文完成交付工作。

**验收标准：**
- **WHEN** 授权模板负责人维护通用配置
- **THEN** 平台支持创建和版本化发布阶段定义、任务定义、阶段/任务交付件要求、里程碑、权限策略、完成规则、门禁和工作绑定，并可被多个模板按精确版本引用
- **WHEN** 模板负责人组装项目模板
- **THEN** 可选择阶段节点并配置前置/后置关系、转移条件、优先级和默认分支；可在每个阶段下配置任务树、阶段级及任务级交付件要求、准入准出条件和BPM流程定义key
- **AND** 配置界面的前置阶段和后置阶段均由同一`StageTransitionDefinition`生成，修改任一视图后另一视图立即一致，不产生双向冲突数据
- **WHEN** 为阶段或任务配置业务执行入口
- **THEN** 可选择已发布业务实体类型、实例解析策略和`BusinessViewKey`，并配置权限策略、完成规则及上下文映射；需要多个界面时使用组合视图
- **WHEN** 模板存在循环、不可达阶段、悬空转移、无正常收口、多个分支无法唯一解析，或引用未发布/不兼容的实体、视图、交付件规则和门禁
- **THEN** 平台拒绝发布，逐项返回阶段、关系、绑定或规则冲突，不生成可供项目选择的模板版本
- **WHEN** 自动或手动项目创建成功
- **THEN** 平台冻结模板及全部引用版本，在同一事务实例化阶段关系图、任务树、阶段/任务交付件要求、绑定和门禁；项目导航及操作入口从实例直接投影
- **WHEN** 用户点击绑定业务实体的阶段或任务
- **THEN** 平台按冻结`BusinessViewKey`和上下文契约加载对应业务操作界面，服务端返回允许操作模式；用户不需要离开项目上下文再次查找业务入口
- **WHEN** 当前阶段达到准出条件
- **THEN** 平台唯一解析后置目标，校验目标阶段准入条件并原子推进；解析结果为零个或多个、目标准入失败时保持当前阶段并列出原因
- **WHEN** 绑定业务实体状态、交付件版本或审批结果变化
- **THEN** 平台按冻结完成规则重新计算任务/阶段完成与门禁，不复制或直接修改领域业务事实
- **WHEN** 模板或通用配置发布新版本
- **THEN** 新项目使用新版本，既有项目继续使用冻结版本；停用实体视图或配置定义不得使既有项目失去历史解释和只读访问能力
- **WHEN** 模板含S5但未配置终验依赖
- **THEN** 平台不因S5强制创建终验任务、报告或门禁；只判定该模板明确配置的业务条件
- **WHEN** 模板不含S5但通过绑定或受控业务事件配置终验
- **THEN** 可创建或关联ACC终验实体并按配置办理；终验事实只影响明确引用它的任务、阶段、审批或闭环条件
- **WHEN** 同一受控触发重放或用户重复查看readiness
- **THEN** 同一触发最多创建一个目标实例；readiness查询不创建实体、不写业务完成事实

- **WHEN** 创建售前测试项目并完成EXE-03/04的适用要求
- **THEN** 平台仅生成S0/S4阶段实例，保存EXE-03/04真实完成证据和closed_from_stage=S4；不要求安装完成事实、终验或S6，按CLO-01/02闭环且不生成虚假任务。

**涉及数据字段：**
模板编码/版本/状态、四维匹配条件、阶段定义编码及版本、模板阶段ID、开始/收口标识、StageTransitionDefinition（来源阶段/目标阶段/条件/优先级/默认分支/版本）、任务定义及父任务、阶段/任务交付件要求（归属范围/类型/必传标识/数量/来源/确认审批要求/版本）、WorkBinding归属、绑定类型、业务实体类型、实例解析策略、目标引用映射、BusinessViewKey、组件/表单版本、上下文参数、PermissionPolicy、CompletionRule、GateRef、里程碑、BPM流程定义key、模板冻结快照、发布/停用人与时间

**权限与数据范围：**
平台管理员维护通用配置的技术发布和实体/视图注册权限；工程管理部中获授权的模板负责人组装模板并提交发布；对应领域配置负责人只能发布本领域实体与视图契约，不能扩大项目或领域数据权限。项目创建人员只能查看和选择本租户及当前业务条件可用的生效模板；项目经理只读查看项目冻结配置，无权修改公共模板或在运行中改变阶段关系。基础保留编码不得删除、复用或改义。

**异常、降级及留痕要求：**
模板、阶段关系、交付件要求、绑定或注册视图校验失败时保持草稿并返回精确错误；项目实例化任一节点失败时项目创建事务整体回滚。业务视图组件暂不可用时保留节点、绑定和业务对象原状态，展示可恢复原因，不得降级为任意通用表单完成专项业务。阶段推进并发或事实版本冲突时拒绝过期操作并重新计算。平台保存配置版本、引用关系、图校验结果、绑定兼容性结果、发布/停用、项目冻结快照、阶段转移判定、界面装载失败、补偿和操作人时间；任何已被项目引用的版本不得物理删除。

**依赖关系：**
- 前置依赖：无（平台核心骨架，首批建设）
- 后续依赖：PM-01项目创建加载模板、PM-02子项目继承模板、PM-07项目业务属性分类、PM-11项目阶段/任务运行时、各领域业务实体与视图注册
## PM-08 服务经理自动指派

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-08 |
| 所属阶段 | S0 |
| 用户角色 | 工程管理部 |
| 优先级 | P1 |
| 目标版本 | V1手动指派；V2自动指派 |
| 复杂度 | 低 |
| 依赖条件 | 内部强前置：部门主数据（办事处承载为部门）及行政区划—部门映射、AST地点主数据 |

**业务场景与需求描述：**
项目创建时根据项目所属办事处确定服务经理。多省份节点场景下，服务经理（办事处级语境）为下单办事处服务经理，服务经理（本地实施级语境）为各实施地点服务经理；单省份内场景下，服务经理为实施地点服务经理。本需求属于效率提升功能：V1优先建立行政区划—部门映射配置及人工指派，映射只提供候选过滤与提示，最终由授权人员人工确认；V2在同一已发布规则基础上实现唯一匹配自动指派（修订002/007）。

**核心业务规则：**
1. V1仅支持工程管理部人工指派，必须显示下单办事处、实施地点及候选服务经理，不以"自动指派"名义后台决定人员；行政区划与服务办事处映射（`area_code + area_level → department_code`）的维护、启停、版本和候选过滤属于V1首批配置基础，最终服务经理由授权人员人工确认（修订002/007）。V2按项目冻结的已发布匹配规则计算候选，只有结果唯一时才自动形成并生效主责指派；无匹配或多匹配时不得自动选定，项目保持“待指派”并转有权人员人工处理。
2. 多省份项目的根/统筹项目指派下单办事处服务经理，各实施地点对应的子项目或任务指派实施地点服务经理（服务经理（本地实施级语境））；单省项目以实施地点所属办事处服务经理为主责。项目层级不固定，角色挂接到实际项目节点而非固定一级、二级。
3. 一个项目节点同一时点只能有一名主责服务经理，可配置协同服务经理；改派必须指定生效时间和原因，历史责任区间不得覆盖。
4. 办事处、地点或人员状态不完整时，项目进入"待指派"，不得随意选择跨办事处人员作为系统默认值；行政区划—部门映射缺失、停用、冲突或无效不阻断项目创建，但须展示配置缺口，未解析文本地点不得参与办事处解析与服务经理指派（修订002/007）。

5. PM-08只拥有服务经理责任关系；项目`assignment_status`由服务经理和项目经理两类有效责任区间共同判定。完成服务经理指派但项目经理仍缺失时保持`UNASSIGNED`，不得提前满足S0准出。

**用户故事：**
作为工程管理部，我希望在V1根据办事处和实施地点明确选择各项目节点的主责服务经理，以便责任范围清晰，并为V2自动指派规则提供准确基础数据。

**验收标准：**
- **WHEN** V1工程管理部为项目节点选择在职且属于适用办事处的服务经理
- **THEN** 平台保存主责服务经理、责任项目节点和生效时间；仅当PM-01已存在有效项目经理责任区间时才将项目`assignment_status`更新为`ASSIGNED`，否则保持`UNASSIGNED`并提示待完成项目经理指派；`current_stage`和`lifecycle_status`保持不变，并向被指派人发送通知
- **WHEN** 项目涉及多个省份和多个实际项目节点
- **THEN** 平台允许分别为统筹节点和实施节点人工指派主责人，不要求节点位于固定结构深度，并可从根项目查看各节点责任分布
- **WHEN** 候选人已离职、办事处不匹配、实施地点缺失或项目节点已有生效中的主责人
- **THEN** 平台拒绝直接覆盖，保持原责任关系或"待指派"状态，并提示补齐地点或执行改派流程
- **WHEN** V1有权管理员新增、调整、启停行政区划—部门映射并发布新版本
- **THEN** 新的候选查询使用当前已发布映射版本，既有指派责任区间不被追溯改写；缺失或冲突映射显示配置缺口并保持人工处理
- **WHEN** V2项目节点按冻结的已发布规则只能匹配一名合法候选服务经理
- **THEN** 平台自动建立主责服务经理责任区间并保存规则版本、输入快照和唯一候选依据；仅当项目经理责任区间也有效时把`assignment_status`更新为`ASSIGNED`，否则保持`UNASSIGNED`
- **WHEN** V2匹配结果为0名或多名候选，或地点、组织、人员事实无法唯一确认
- **THEN** 平台不得静默选择人员，项目保持“待指派”并生成可处理的匹配缺口，由有权人员按V1人工指派流程处理

**涉及数据字段：**
办事处、实施地点（结构化站点引用及地点解析状态）、省份、行政区划—部门映射（area_code/area_level→department_code）、映射版本/状态、服务经理、项目经理有效指派校验、指派规则标识/版本、匹配输入与候选依据、指派方式（手动/自动）、指派时间

**权限与数据范围：**
工程管理部指派岗可为授权项目节点人工指派、改派主责和协同服务经理；具备组织映射维护权限的管理员可维护并发布行政区划—部门映射，但不能借此绕过项目指派权限。服务经理可查看本人责任项目及被授权后代项目，不能自行把责任转给他人。项目经理仅查看本项目责任人。候选人员列表按在职状态和办事处范围过滤，跨租户人员不能被选择。

**异常、降级及留痕要求：**
组织或人员主数据不可用时，禁止创建未经校验的新指派，项目保持"待指派"，已有生效责任关系继续展示并标记人员校验待恢复。通知发送失败不回滚已成功指派，但生成待重试通知；改派保存失败时原责任人继续生效。平台记录候选依据、指派/改派前后责任人、责任区间、原因、通知结果和操作人时间。

**依赖关系：**
- 前置依赖：部门主数据（办事处承载为部门）、PM-01项目创建
- 版本规划：V1首批完成区划—部门映射配置、候选过滤和人工指派；V2仅在唯一合法匹配时自动生效，异常继续复用V1人工流程
## PM-09 人员批量变更

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-09 |
| 所属阶段 | S0 |
| 用户角色 | 工程管理部、服务经理 |
| 优先级 | P1 |
| 目标版本 | V2 |
| 复杂度 | 低 |
| 依赖条件 | 内部强前置：组织权限 |

**业务场景与需求描述：**
支持人员批量变更操作：工程管理部可批量变更多个项目的服务经理，服务经理可批量变更其负责项目下的项目经理。变更操作记录变更历史，支持按项目、按办事处、按人员维度筛选批量操作。本需求只承载V2人员批量变更，不作为V1项目经理初始指派的前置；V1项目经理初始指派由PM-01承载。团队成员的后续指派或批量调整按本需求授权范围执行。

**核心业务规则：**
1. 工程管理部可批量变更服务经理，服务经理可批量变更本人责任项目的项目经理；角色类型不可混用，目标人员必须在职并满足办事处、岗位和项目范围要求。
2. 提交前平台逐项目预检当前责任人、目标人员、在途审批和数据权限，生成可变更/不可变更清单；用户确认的是冻结清单，清单过期或责任人变化后必须重新预检。
3. 批次按项目逐项执行，允许有效项目成功、无效项目失败；每项返回明确结果，批次状态为全部成功、部分成功或全部失败。失败项目不得改变原责任人。
4. 每个项目同一时点只有一个生效主责人，批量变更结束原责任区间并创建新责任区间；协同角色不随主责批量变更自动删除。
5. 在途审批继续由原审批实例确定的审批人处理，除非对应业务流程明确支持转办；人员变更不静默改写历史审批人与任务执行人。

**用户故事：**
作为工程管理部，我希望先预检再批量改派多个项目的服务经理，并获得逐项目结果，以便组织调整高效且不会覆盖不符合条件的责任关系。

**验收标准：**
- **WHEN** 有权人员筛选项目、选择合法目标人员并完成批量预检确认
- **THEN** 平台生成批次并逐项目结束原责任区间、建立新责任区间，返回成功数、失败数和每项处理结果
- **WHEN** 批次中部分项目已改派、无权、责任人已变化或目标人员不适用
- **THEN** 平台仅执行仍满足冻结条件的项目，失败项目保留原责任人，批次标记"部分成功"并提供失败明细
- **WHEN** 用户以过期预检清单提交或尝试把服务经理批量操作用于项目经理角色
- **THEN** 平台拒绝批次执行并要求重新预检，所有项目保持原责任关系且不产生变更记录

**涉及数据字段：**
原服务经理、新服务经理、原项目经理、新项目经理、变更项目范围、变更时间、变更人、变更原因

**权限与数据范围：**
工程管理部变更岗可批量改派授权办事处项目的服务经理；服务经理只能批量改派本人主责且有管理权项目的项目经理。目标人员候选列表按在职、岗位和组织过滤。操作者不能处理未授权平级、其他办事处或跨租户项目；批次结果导出沿用项目数据权限。

**异常、降级及留痕要求：**
组织数据不可用时禁止生成新预检；执行期间单项并发冲突只失败该项目并保留原责任人，不回滚已成功项目。通知失败不回滚责任变更，生成待重试消息；失败项目可在修正后形成新批次重试。平台记录筛选条件、预检快照、批次号、项目级前后责任人、责任区间、失败原因、通知和操作人时间。

**依赖关系：**
- 前置依赖：组织权限体系、PM-01项目创建、PM-08服务经理指派
- 关联依赖：组织主数据、权限矩阵
## PM-02 主子项目管理与进度汇总

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-02 |
| 所属阶段 | S0（贯穿全生命周期） |
| 用户角色 | 工程管理部、服务经理、项目经理 |
| 优先级 | P0 |
| 目标版本 | V1 |
| 复杂度 | 高 |
| 依赖条件 | 内部强前置：项目模板（PM-03）、权限体系 |

**业务场景与需求描述：**
支持主子项目按业务需要持续拆分，实现大型项目多办事处、多节点分别交付的统一管理。系统不设置固定层级深度上限；创建或拆分项目时可指定直接父项目，结构层级深度由父子关系自动计算，并可选择模板预设或人工指定的业务层级标签。子项目可独立交付与闭环，父项目按直接子项目权重逐级汇总全部后代项目进度；父项目闭环以全部后代项目闭环为前提条件。拆分维度支持自由组合（不限定订单行、数量、办事处、序列号维度的组合）。层级查询支持直接下级、全部后代、完整上级链和指定业务层级，并采用按需加载，避免一次性展开完整项目树。性能验证数据规模取实际迁移项目量两倍与最低20万项目的较大值，同时覆盖单项目树1万个节点、单节点直接子项目2000个和测试深度30；测试深度仅用于验收，不构成业务层级上限。

**核心业务规则：**
1. 项目树只由"直接父项目"关系形成；除根项目外，每个项目同一时点只能有一个直接父项目。系统不设置业务层级深度上限，但禁止项目以自身或任一后代作为父项目，禁止形成循环关系。
2. 拆分可组合订单行、数量、办事处、序列号等维度；子项目具有独立项目编码、负责人、阶段状态和闭环结果，并继承拆分时指定的模板版本及可继承主数据。拆分范围之和不得超过父项目可分配范围。
3. 父项目进度按直接子项目逐级计算：父项目进度=Σ（直接子项目进度×子项目权重）。同一父项目下全部生效直接子项目权重之和必须为100%；模板未预设且用户未调整时按直接子项目等权分配。
4. 设备数量仅作为统计指标和人工调整权重的参考，不自动决定权重。权重或汇总口径变更必须审批并生成新版本，保存变更前后值、生效时间和批准人；历史进度快照按当时版本解释，不追溯重算。
5. 父项目闭环必须以全部后代项目闭环为前提；任一后代仍在执行、暂停或关闭审批中时，父项目不得闭环。子项目关闭、撤销等状态是否进入进度分母，按生效的汇总口径版本处理。
6. 树查询必须支持直接下级、全部后代、完整上级链和指定业务层级，并先执行租户及项目授权过滤再返回结果；默认按需加载直接下级，测试深度30只是性能验收规模，不是业务层级上限。

**用户故事：**
作为工程管理部，我希望能够将大型项目按订单行/数量/办事处/序列号等维度自由拆分为任意层级的主子项目，并指定父项目和业务层级，使子项目独立交付、父项目逐级汇总进度、父项目闭环以全部后代项目闭环为前提，以便统一管理大型多节点项目并保证交付完整性。

作为项目经理，我希望按直接下级、全部后代、完整上级链或指定业务层级查询项目并仅按需展开当前节点，以便在项目层级较深、节点较多时快速定位目标项目。

**验收标准：**
- **WHEN** 服务经理或工程管理部对主项目发起拆分操作
- **THEN** 系统支持按订单行、数量、办事处、序列号维度自由组合选择拆分维度
- **AND** 用户选择维度后系统自动生成拆分方案预览（子项目数量、各子项目包含的设备/订单行），用户确认后批量创建子项目
- **AND** 子项目继承主项目模板与关键属性，保持独立项目编码
- **AND** 系统不限制固定层级深度，用户可指定直接父项目及业务层级标签；结构层级深度由父子关系自动计算，不允许人工设置与父子关系冲突的深度
- **AND** 系统校验父子关系合法且不得形成循环引用
- **WHEN** 用户查看多级项目结构
- **THEN** 系统支持查询直接下级、全部后代、完整上级链和指定业务层级；项目树首次加载根节点或当前节点的直接下级，用户展开时再加载下一层，查询结果保留目标项目的层级路径
- **AND** 在实际迁移项目量两倍与20万项目取较大值、单项目树1万个节点、直接子项目2000个、测试深度30的规模下，权限过滤后的上述查询与树形页面响应时间满足≤2秒（P95）
- **AND** 测试深度30只作为性能验收数据，不限制业务继续创建更深层级；超过测试深度时仍须保证关系正确并采用按需加载
- **WHEN** 子项目进度状态发生变更（如阶段推进、任务闭环）
- **THEN** 主项目按"Σ（直接子项目进度×子项目权重）"逐级汇总；同一父项目直接子项目权重合计必须为100%，未配置时按直接子项目等权分配
- **AND** 主项目详情页展示各子项目进度列表与整体进度
- **AND** 设备数量只作为可选统计指标，不自动改变权重；权重或汇总口径变更必须审批并形成版本，历史进度快照按当时版本解释且不追溯重算
- **WHEN** 主项目发起闭环申请（配合CLO-02闭环审批）
- **THEN** 系统校验所有层级的后代项目均已闭环，存在未闭环子项目时驳回主项目闭环申请并列出未闭环子项目清单
- **AND** 全部子项目闭环后主项目方可进入闭环审批流程
- **WHEN** 指定父项目不存在、跨租户、属于当前项目的后代，或拆分范围超过父项目可分配范围
- **THEN** 拆分申请保持草稿状态，不生成子项目和父子关系，并记录未通过的校验项

**涉及数据字段：**
根项目ID、父项目ID、项目层级深度、业务层级编码/名称、项目层级路径、主项目编码、子项目编码、拆分维度、子项目权重、权重来源（模板预设/人工调整/默认等权）、权重版本、权重生效时间、权重审批记录、进度状态、进度快照、闭环状态、所属办事处、服务经理、项目经理、直接子节点数、后代节点数、查询范围、查询耗时

**权限与数据范围：**
工程管理部和获授权服务经理可发起项目拆分、指定直接父项目和设置初始权重；项目经理可维护本人负责项目节点的进度与交付数据，但无权自行改变父子关系或已生效权重。权重及汇总口径调整由配置的审批角色批准。项目经理可查询本办事处项目概况；子项目的项目经理和服务经理可查看同根项目树其他节点的名称、状态、阶段、里程碑进度、交付件目录和齐套状态，但不能操作其他节点，也不能查看任务明细、人员、设备凭证、审批详情、敏感商务数据或交付件正文。设备、领域任务和交付件沿所属项目节点执行同一数据范围过滤。

**异常、降级及留痕要求：**
指定父项目不存在、跨租户、形成循环或拆分范围超出父项目可分配范围时，拆分申请保持草稿且不生成子项目。直接子项目权重合计不为100%时不得生效；用户修正后可重新提交。任一直接子项目缺少有效进度时，父项目汇总标记为"待计算"并禁止据此闭环，不得用0或上次结果静默替代。平台保存父子关系版本、拆分维度与数量、权重校验结果、审批意见、进度汇总版本、查询范围及操作前后值；关系撤销不得删除历史快照。

**依赖关系：**
- 前置依赖：PM-03项目模板（子项目继承模板）、PM-04主子项目权限与拆分（权限隔离）
- 关联依赖：权限体系（上级按授权范围查看后代项目、平级项目默认互不可见）
## PM-04 主子项目权限与拆分

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-04 |
| 所属阶段 | S0 |
| 用户角色 | 服务经理、项目经理 |
| 优先级 | P1 |
| 目标版本 | V1 |
| 复杂度 | 高 |
| 依赖条件 | 内部强前置：权限体系 |

**业务场景与需求描述：**
配合PM-02主子项目拆分，实现不限固定层级深度的项目权限隔离。上级项目角色可按授权范围查看全部后代项目数据；同一父项目下的平级项目默认互不可见；非根项目仅可查看自身及权限范围内的后代项目。权限判断适用于任意层级，并覆盖项目数据、进度、交付件、任务、设备、割接、巡检等所有业务对象，确保多办事处联合交付场景下的数据安全。

**核心业务规则：**
1. 项目层级由"根项目ID、直接父项目ID、层级路径"表达，不设置业务层级上限；创建、移动项目时必须阻止把项目挂到自身或任一后代节点下，也不得跨租户挂接。
2. 结构层级与业务层级分离：结构层级由父子关系计算，业务层级由可配置字典标记。一个业务层级标签可被不同结构深度使用，查询和统计不得假设"一级/二级/三级"等固定深度。
3. 项目授权可授予当前项目、当前项目及全部后代两种范围。上级角色只有取得"含后代"授权后才能穿透查看；授权当前项目不自动获得父项目、祖先项目或平级项目明细。
4. 平级项目默认互不可见；跨办事处协作必须通过项目角色或专项数据授权显式授予，并记录授权人、范围、有效期。项目移动后重新计算路径和继承授权，原父项目继承权限不得继续生效。
5. 项目汇总以设备、任务、交付件等业务对象的唯一ID去重；父项目可汇总授权范围内后代项目，不得把同一设备在父子项目中的引用关系重复计数。

**用户故事：**
作为服务经理，我希望按实际交付组织拆分任意深度的项目，并将查看或管理权限授予明确的项目子树，以便联合交付时既能汇总整体进展，又不暴露未授权平级项目数据。

**验收标准：**
- **WHEN** 服务经理在有管理权限的项目下创建第31层后代项目，或把项目移动到另一个有权父项目下
- **THEN** 平台保存直接父项目、根项目、完整层级路径和实际结构深度，查询结果可返回完整上级链且不以测试深度限制业务创建
- **WHEN** 无权访问平级项目B的项目A经理发起查询，或仅有"当前项目"权限的人员越权查询其子项目
- **THEN** 平台返回无权结果且不返回项目名称、进度、任务、设备和交付件明细；被授予"当前项目及全部后代"后才按授权项目子树返回数据
- **WHEN** 用户尝试把项目挂到自身、任一后代节点或其他租户项目下
- **THEN** 平台拒绝移动并保持原父子关系、路径和授权结果不变，同时记录拒绝原因
- **WHEN** 父项目汇总包含被多个层级引用的同一设备或交付对象
- **THEN** 平台按业务对象唯一ID去重，并同时展示授权范围内的明细数量与去重汇总数量

**涉及数据字段：**
根项目ID、父项目ID、项目层级深度、业务层级编码/名称、项目层级路径、所属办事处、可见范围、数据权限标识、角色权限

**权限与数据范围：**
服务经理可在本人管理的项目子树内创建、移动项目并授予项目角色；项目经理只能管理已授权项目及授权范围内后代，不能修改父项目或平级项目结构。普通项目成员仅查看被授予的当前项目数据。跨办事处、跨项目子树访问必须取得显式授权，跨租户一律禁止；设备、任务、割接、巡检、交付件和汇总查询必须复用同一项目范围过滤结果。

**异常、降级及留痕要求：**
层级路径重算或授权刷新失败时，项目移动整体回滚，原父子关系和原授权继续有效，不允许出现"结构已移动但权限未更新"的部分成功。大项目树查询超时可降级为按需加载直接下级，但不得绕过权限过滤；汇总计算失败时标记"汇总待重算"，不展示不完整结果为正式统计。平台记录创建、移动、授权、撤权、失败重试的操作人、时间、原父项目、新父项目、授权前后值和失败原因。

**依赖关系：**
- 前置依赖：权限体系、PM-02主子项目管理与进度汇总
- 关联依赖：与PM-02共同实现主子项目拆分与权限隔离
## PM-11 项目任务管理

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-11 |
| 所属阶段 | 全局（S0阶段初始化任务，贯穿全生命周期） |
| 用户角色 | 项目经理、服务经理、团队成员/工程师、管理员 |
| 优先级 | P1 |
| 目标版本 | V1任务层级模型、阶段/任务WorkBinding运行时与基础查询；V2甘特展示与受控任务依赖维护 |
| 复杂度 | 中 |
| 依赖条件 | 内部强前置：PM-03项目模板与交付流程通用配置 |

**业务场景与需求描述：**
项目创建时，平台根据PM-03冻结模板实例化ProjectStage关系图、ProjectTask树、阶段/任务交付件要求、工作绑定和完成规则。ProjectTask支持任务指派、进度跟踪、闭环管理及任意深度子任务；创建或拆分任务时可指定直接父任务，结构层级由父子关系自动计算，并可选择模板预设或人工指定的业务层级标签。V2在V1同一任务事实之上提供甘特展示和受控任务依赖维护，不建立第二套任务状态或进度事实。

项目工作区以ProjectStage作为一级业务导航、ProjectTask作为二级及可展开的深层任务导航。点击阶段时，右侧阶段工作台展示阶段通用信息、前后置关系、准入准出状态、阶段交付件和阶段`WorkBinding`执行区；点击任务时，右侧任务工作台展示任务通用信息、任务交付件和任务`WorkBinding`执行区。`STAGE_NATIVE`/`TASK_NATIVE`使用通用界面，其他绑定按业务实体类型、稳定目标引用和`BusinessViewKey`装载领域业务界面，可进入查看、创建、编辑、填写或审批模式。业务界面不形成第二套导航、权限或完成事实。

性能验证数据规模取实际迁移任务量两倍与最低200万任务的较大值，同时覆盖单任务树5万个节点、单节点直接子任务2000个和测试深度30；测试深度仅用于验收，不构成业务层级上限。

**核心业务规则：**
1. 任务以根任务ID、直接父任务ID和层级路径形成任意深度任务树；创建、移动时自动计算结构深度，并拒绝循环引用、跨租户挂接以及未明确授权的跨项目挂接。
2. 结构层级与业务层级标签分离。业务层级由可配置字典或项目模板给出，可跨不同结构深度复用；查询不得把业务层级名称解释为固定第N层。
3. 项目模板初始化ProjectStage、ProjectTask和配置引用快照。公共模板、阶段、任务、交付件或绑定版本更新不自动覆盖已执行项目；既有项目调整必须按PM-03受控换模和影响评估处理，项目经理只能在授权范围继续拆分运行时任务。
4. 查询支持任务直接下级、全部后代、完整上级链和指定业务层级；界面默认按需加载直接下级。搜索命中深层节点时返回完整路径，但路径中无权节点只显示必要结构占位，不泄露业务明细。
5. 任务移动后重新计算该节点及全部后代路径；存在进行中审批、已完成里程碑或会造成依赖循环时禁止移动。V1提供树形查询和模板冻结的基础依赖；V2复用同一任务、计划和进度事实提供甘特展示，并允许有权用户受控新增、更新或删除任务依赖。依赖变更必须拒绝循环、非法跨项目关系、越权对象和版本冲突。
6. 任务状态采用“初始化可扩展状态定义+受控状态机”。基础平台保存稳定编码、名称、显示样式、标准状态映射、合法迁移、适用角色及进入/退出条件；核心状态不得删除、复用或改义，扩展状态不得绕过完成、关闭和门禁。同一任务同一时点只有一个有效负责人，转派保留责任区间。
7. 每个ProjectStage和ProjectTask实例均保存一个当前有效主`WorkBinding`、`PermissionPolicy`和`CompletionRule`快照。阶段缺省`STAGE_NATIVE`，任务缺省`TASK_NATIVE`；非原生绑定额外保存业务实体类型、实例解析策略、稳定目标引用、`BusinessViewKey`、组件/表单版本和上下文参数。绑定目标的数据和状态仍由Owner领域管理。
8. 项目导航只从ProjectStage关系图和ProjectTask树投影。阶段导航可展示前置/后置关系和当前可达状态；任务导航可按需展开任意深度。项目概览继续独立提供基本信息、项目树、团队成员、项目任务、设备清单和实施范围，不作为阶段或任务重复配置。
9. 用户点击阶段时，平台先返回阶段通用摘要、阶段级交付件要求及满足状态，再按Stage WorkBinding装载业务执行区；用户点击任务时，平台返回任务通用详情、任务级交付件要求及满足状态，再按Task WorkBinding装载业务执行区。组合视图可以聚合多个已注册子视图，但不得绕过各子对象服务端权限。
10. 业务实体视图只通过PM-03受控注册表解析。前端依据`BusinessViewKey`选择已发布组件并传入受控上下文，所有查询和命令继续调用实体Owner接口；模板或前端不得直接修改其他领域表、拼接任意URL或根据组件可见性判定权限。
11. 阶段/任务交付件要求按冻结定义生成实例。实际交付件必须关联来源业务记录、文件身份和版本；同一文件或业务产出可满足多个要求但只保存一份权威事实。缺失、换版、失效或审批撤销时重新计算对应任务、阶段和后续门禁。
12. 任务完成由Task `CompletionRule`判定；阶段完成由Stage `CompletionRule`聚合阶段绑定事实、必做任务、里程碑、阶段/任务交付件和审批结果。非原生绑定不得通过通用“完成”操作绕过目标事实；完成或推进命令必须携带节点版本、绑定版本、规则版本和事实版本并重新校验。
13. 绑定目标不存在、无权、上下文映射失败、实体/视图版本失效或组件暂不可用时，阶段/任务保持原状态并展示可恢复原因。通用摘要仍可按权限查看，但不得用通用内容替代、伪造或完成绑定业务。
14. 阶段关系和阶段推进由PM-03拥有。PM-11只提供阶段/任务运行时投影、工作台和任务管理，不允许项目成员在运行中直接编辑模板阶段前后置关系；当前阶段准出后由统一推进服务解析后置目标和目标阶段准入条件。

**用户故事：**
作为项目经理，我希望任务可以按实际工作持续拆分、指派并查询任意上下级关系，以便在复杂项目中组织责任和跟踪进度。

作为项目成员，我希望点击阶段或任务即可看到其通用信息、交付件要求和与业务实体匹配的操作界面，以便在项目上下文直接完成真实业务工作。

作为服务经理，我希望阶段完成和推进来自阶段绑定事实、任务、交付件、里程碑和审批的统一判定，以便项目流程灵活配置但执行结果仍可审计。

**验收标准：**
- **WHEN** 用户创建或移动任务节点
- **THEN** 可指定直接父任务和业务层级标签，系统自动计算结构层级深度，并拒绝循环引用及跨项目非法挂接
- **WHEN** 用户查看或搜索任务树
- **THEN** 支持直接下级、全部后代、完整上级链和指定业务层级查询，默认按需加载；性能验收按迁移任务量两倍与200万任务取较大值，并覆盖单树5万个节点、直接子任务2000个和深度30，页面响应≤2秒（P95）
- **WHEN** 用户进入项目工作区
- **THEN** 一级导航来自ProjectStage关系图，二级及深层导航来自ProjectTask树；项目概览不作为任务节点重复配置
- **WHEN** 用户点击`STAGE_NATIVE`阶段
- **THEN** 阶段工作台展示阶段状态、前后置关系、准入准出条件、阶段交付件和任务树，并提供获权的通用阶段操作
- **WHEN** 用户点击绑定业务实体或组合视图的阶段
- **THEN** 平台在阶段通用摘要下按冻结`BusinessViewKey`加载对应领域操作界面，并按服务端权限与实体状态返回查看、创建、编辑、填写或审批模式
- **WHEN** 用户点击`TASK_NATIVE`任务
- **THEN** 任务工作台展示通用基础信息、任务交付件和本人获权操作，按ProjectTask自身状态机及完成规则执行
- **WHEN** 用户点击绑定业务实体、业务组件、动态表单、审批或组合视图的任务
- **THEN** 平台在任务上下文内加载相应真实业务界面，不要求用户再次查找入口，也不复制目标业务正文
- **WHEN** 阶段或任务绑定要求创建业务实体实例
- **THEN** 平台按项目、阶段/任务、业务范围和绑定版本生成稳定幂等键，重复打开或重试返回同一实例，不重复创建业务对象
- **WHEN** 阶段级或任务级交付件形成、换版、失效或审批撤销
- **THEN** 平台更新要求满足关系并重新计算任务完成、阶段完成及后续门禁，原文件和历史版本保持不变
- **WHEN** 完成规则未满足或绑定视图不可用
- **THEN** 节点保持原状态并返回具体缺口/恢复原因，非原生节点不能由通用完成操作直接绕过
- **WHEN** V2用户查看授权项目甘特图或维护任务依赖
- **THEN** 平台复用V1任务、计划、进度和依赖事实展示并受控保存；循环、越权、跨项目非法关系和版本冲突均被拒绝

**涉及数据字段：**
阶段实例ID、阶段定义/模板阶段版本、阶段前后置关系引用、阶段状态、阶段WorkBinding/BusinessEntityType/BusinessViewKey、阶段交付件要求及满足快照、阶段PermissionPolicy/CompletionRule/GateRef；任务编码/名称、根任务ID、父任务ID、层级路径、业务层级、所属阶段/里程碑、责任人/执行人、计划及实际时间、任务状态和状态机版本、Task WorkBinding类型、业务实体类型/目标引用、实例解析策略、BusinessViewKey、组件/表单/审批引用、上下文参数、任务交付件要求及满足关系、PermissionPolicy、CompletionRule、GateRef、业务事实版本、完成判定快照、跨区域标识、甘特排序、任务依赖、查询范围和耗时

**权限与数据范围：**
项目经理可在本人负责项目内创建、拆分、移动和指派任务；服务经理可查看授权项目子树并调整跨区域责任；团队成员/工程师只能查看获授权阶段/任务并操作绑定实体允许的动作，不能修改模板阶段关系、他人任务或领域Owner事实。配置管理员不能因维护组件注册或状态定义取得项目业务数据。阶段、任务、业务视图、交付件、搜索、统计和导出均执行一致的租户、项目树、对象、字段和文件权限。

**异常、降级及留痕要求：**
任务路径重算失败时移动事务整体回滚；大树完整展开超时降级为按需加载和分页搜索。模板实例化部分失败时项目创建整体回滚，不允许缺少阶段、任务、交付件或绑定的半成品项目。业务视图装载失败保留绑定和业务对象状态，支持组件恢复后重试；实体创建超时按幂等键查询后再决定重试。平台记录阶段/任务配置版本、绑定与上下文快照、业务实体引用、交付件满足关系、任务移动/转派、状态变化、完成判定、界面装载失败和补偿结果。

**依赖关系：**
- 前置依赖：PM-03项目模板、阶段关系、阶段/任务交付件和工作绑定配置
- 关联依赖：PM-02主子项目、权限体系、PLT-02文件版本、各领域业务实体与视图注册
## PM-10 项目回退与异常关闭

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-10 |
| 所属阶段 | S0及项目活动期 |
| 用户角色 | 服务经理、工程管理部 |
| 优先级 | P1 |
| 目标版本 | V1 |
| 复杂度 | 低 |
| 依赖条件 | 内部强前置：项目状态机、在途对象检查和归档规则 |

**业务场景与需求描述：**
PM-10只承载项目责任回退和异常关闭。回退用于将无法继续由当前责任主体承接的活动项目退回重新指派；异常关闭用于项目取消、失效或其他不再实施的异常退出。代理商自服“不予跟踪闭环”属于CLO-01/02管理的合法业务闭环，不得通过PM-10写成异常关闭。

**核心业务规则：**
1. 回退不改变`lifecycle_status=ACTIVE`，按受控规则结束原责任区间、将`assignment_status`置为`UNASSIGNED`并回到允许重新指派的阶段；已有业务事实保持只读可追溯。
2. 异常关闭必须填写原因、业务依据及遗留事项，并在所有在途审批、采集、割接、巡检、转包和其他领域任务按各自Owner规则完成、撤回、终止或明确移交后，才可写入`EXCEPTION_CLOSED`。
3. PM-10不得产生`NORMAL_CLOSED`或`NO_TRACKING_CLOSED`，也不得把“代理商自服”自动解释为异常关闭或不予跟踪闭环。
4. 主项目异常关闭前必须检查全部后代项目；存在未处置后代时禁止关闭。关闭子项目不自动关闭父项目或平级项目。
5. 异常关闭后禁止新增或修改业务数据，仅允许有权人员查看、导出和执行受控重开。PM-10只允许重开`EXCEPTION_CLOSED`；正常闭环和不予跟踪闭环如需继续承载业务，必须使用另行批准的闭环后变更机制或新建项目。

**用户故事：**
作为服务经理，我希望对无法继续由当前责任人承接的项目发起回退，并对取消或失效项目执行异常关闭，使责任调整、业务闭环和异常退出保持清晰边界。

**验收标准：**
- **WHEN** 服务经理对符合条件的活动项目填写原因并确认回退
- **THEN** 平台保持`lifecycle_status=ACTIVE`，结束原责任区间、将`assignment_status`置为`UNASSIGNED`并保留全部历史事实
- **WHEN** 工程管理部对不存在未处置在途对象和后代项目的异常项目确认关闭
- **THEN** 平台保存依据及遗留事项快照，写入`lifecycle_status=EXCEPTION_CLOSED`、`closure_type=EXCEPTION`和`closed_from_stage`，且不计入业务闭环数量
- **WHEN** 项目实施方式为代理商自服且用户希望不再跟踪
- **THEN** PM-10保持项目ACTIVE并返回CLO-01/02不予跟踪闭环入口与资格说明，记录本次拒绝，不直接写入终态
- **WHEN** 有权人员通过PM-10重开项目
- **THEN** 仅`EXCEPTION_CLOSED`可以更新为ACTIVE并记录重开依据；`NORMAL_CLOSED`和`NO_TRACKING_CLOSED`保持原状态，重开请求被拒绝且留痕

**涉及数据字段：**
当前阶段、生命周期状态、指派状态、闭环类型、闭环来源阶段、归档标识、操作人、操作时间、回退原因、异常关闭原因、业务依据、遗留事项及在途对象处置快照

**权限与数据范围：**
服务经理可对本人主责项目发起回退；工程管理部异常关闭岗可在授权范围确认异常关闭或重开。项目经理和普通成员只读查看状态及原因摘要。跨租户和未授权平级项目不可见。

**异常、降级及留痕要求：**
关联对象检查失败或状态更新部分失败时事务整体回滚。外部任务终止回调超时则标记“异常关闭受阻”，不得先归档。记录阻断对象、状态前后值、原因、依据、执行人、时间和重试结果。

**依赖关系：**
- 前置依赖：PM-01项目创建、PM-08服务经理指派、项目状态机
- 关联依赖：CLO-01/02业务闭环、各领域在途对象处置规则、归档规则
## PROJ-12 项目组合管理

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PROJ-12 |
| 所属阶段 | S0-S5 |
| 用户角色 | 管理层、工程管理部、区域负责人 |
| 优先级 | P1 |
| 目标版本 | V2 |
| 复杂度 | 高 |
| 依赖条件 | 内部强前置：项目层级模型、组织权限、项目指标 |
| 来源追溯 | FR-PROJ-024 |

**业务场景与需求描述：**
对同一客户、合同、区域或交付计划下的多个项目建立可配置组合，支持组合级进度、风险、资源和经营统计；项目和任务均不限制固定层级，可由管理员配置层级类型和父子关系。

**核心业务规则：**
1. 组合是统计与治理对象，不替代项目；一个项目可加入多个组合，但必须指定一个主组合用于默认统计。
2. 组合层级、项目关系类型和统计口径由字典配置；父子项目进度按配置的汇总规则计算，不能静默覆盖项目原始数据。
3. 组合变更须校验循环关系、权限和已发布报表快照，生效前保留版本和审批记录。

**用户故事：**
作为区域负责人，我希望按客户或合同查看多级项目组合的交付健康度，并下钻到具体项目和任务。

**验收标准：**
- **WHEN** 管理员创建组合并关联多个项目
- **THEN** 系统校验权限和循环关系，保存主组合及关系类型
- **WHEN** 查询组合进度
- **THEN** 按配置规则汇总子项目里程碑、任务、风险和成本，并可下钻到原始项目
- **WHEN** 修改组合关系
- **THEN** 新统计从生效时间起采用新关系，历史快照保持不变

**涉及数据字段：**
组合ID、组合类型、组合层级、主组合标识、项目ID、关系类型、统计规则、有效期、版本、审批记录

**权限与数据范围：**
组合管理员维护授权组织范围；成员只能查看同时具备项目权限的项目明细；管理层可查看授权组合汇总但不能越权查看项目敏感字段。

**异常、降级及留痕要求：**
发现循环、孤儿项目或指标缺失时阻止发布并列出问题；汇总任务失败保留上次成功快照并标记数据时点；记录关系变更前后值和操作人。

**依赖关系：**
- 前置依赖：PM-02、PM-04和基础平台字典
- 后续依赖：ANA-01及组合级报表使用
## PM-05 借货项目转销管理

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-05 |
| 所属阶段 | S0 |
| 用户角色 | 工程管理部、项目经理 |
| 优先级 | P1 |
| 目标版本 | V2 |
| 复杂度 | 高 |
| 依赖条件 | 内部强前置：项目关联关系模型 |

**业务场景与需求描述：**
支持借货（售前测试）项目转销为正式销售项目的全流程管理。借货项目生成临时项目进行实施，转销后转为正式项目，原临时项目归档；所有实施过程记录（工勘、方案、配置、割接、领域任务等）与新项目建立关联（主）或直接建立副本（辅），避免重复录入、保证实施连续性。拷贝范围为全部实施过程记录。

**核心业务规则：**
1. 借货临时项目与转销正式项目是两个独立项目记录，以"转销来源/转销目标"关系关联；正式项目编码和合同订单信息必须来自CRM/ERP有效销售业务，不把临时项目原地改号为正式项目。
2. 工勘、方案、配置、割接、领域任务和附件等事实记录默认建立跨项目只读引用，保留原项目、原版本和原责任人；只有需继续编辑的派生草稿才允许复制，副本必须记录来源对象ID和来源版本。
3. 同一临时项目只能存在一个生效中的转销目标；转销以来源项目ID和正式销售业务ID幂等，重复触发返回既有关系。正式项目未创建成功前不得归档临时项目。
4. 设备在同一时点只能有一个直接项目归属。转销时逐台确认继续借测、转入正式项目或已归还，变更归属必须记录生效时间；父项目统计按设备ID去重。
5. 所有关联对象校验完成后转销状态才可由"处理中"变为"已完成"，临时项目转为只读归档；存在失败对象时保持"部分失败/待处理"，不得宣称全部转销完成。

**用户故事：**
作为工程管理部，我希望把已形成正式销售业务的借货项目受控转销，并复用可追溯的实施成果，以便延续交付而不篡改临时项目历史。

**验收标准：**
- **WHEN** 有权人员选择借货项目和已生效的正式销售项目并确认设备处置及成果复用清单
- **THEN** 平台创建唯一转销关系，逐项生成只读引用或带来源的派生草稿；全部成功后归档临时项目并展示对象处理汇总
- **WHEN** 转销中部分附件引用或设备归属调整失败
- **THEN** 平台把转销标记为"部分失败"，列出成功与失败对象，临时项目保持可处理状态且正式项目不把失败对象展示为已接收
- **WHEN** 正式项目不存在、临时项目已有生效转销目标、设备归属冲突或用户无源/目标项目权限
- **THEN** 平台拒绝发起或重复转销，保持两个项目及其业务对象原状态不变

**涉及数据字段：**
转销前后业务身份（借货测试/正式）、原项目编码、新项目编码、关联关系类型、实施记录副本标识、归档状态、转销时间

**权限与数据范围：**
工程管理部转销岗须同时具备来源借货项目和目标正式项目管理权限，才可发起、重试和确认转销；项目经理可核对本人项目的复用清单和派生草稿，不能修改原项目历史事实。引用对象继续执行原敏感资料权限，跨租户项目禁止建立转销关系。

**异常、降级及留痕要求：**
CRM/ERP正式业务校验失败、对象引用失败或设备归属并发冲突时，失败项保持待处理并可按原转销批次幂等重试；不得以复制文件兜底掩盖关联失败。无法自动处理的对象由工程管理部选择保留临时项目或人工修正映射。平台记录转销批次、源/目标项目、销售业务ID、对象级处理方式与结果、设备归属前后值、失败原因和操作人时间。

**依赖关系：**
- 前置依赖：项目关联关系模型、PM-01项目创建
- 关联依赖：实施方案、工勘、割接及其他领域任务的实施记录
## PM-06 项目合同与实施范围追加合并

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | PM-06 |
| 所属阶段 | S0及项目活动期范围变更 |
| 用户角色 | 服务经理、项目经理、合同管理员 |
| 优先级 | P1 |
| 目标版本 | V2 |
| 复杂度 | 中 |
| 依赖条件 | 内部强前置：COM-01项目订单行实施范围、项目范围版本、影响分析与审批；集成依赖：ERP合同/订单事实 |

**业务场景与需求描述：**
当扩容、续采、分期采购或补充合同仍由同一个既有项目统一实施时，平台应在该项目内追加合并新的合同、销售订单、订单行及对应实施范围。项目ID和项目主档保持不变；追加批准后形成新的当前项目范围版本，由同一个项目继续承载新增范围的计划、方案、实施、验收和闭环。该功能不是项目间关联、项目组合、多个项目实体物理合并或新建一期项目。

**核心业务规则：**
1. 目标项目必须为`ACTIVE`。`NORMAL_CLOSED`、`NO_TRACKING_CLOSED`和`EXCEPTION_CLOSED`项目不得直接追加合同或实施范围；确需继续承载时先走另行批准的闭环后变更/重开机制，否则新建立项。
2. 合同、销售订单、订单行、产品、数量和金额以ERP为权威事实。平台仅保存其稳定引用、必要快照、订单行分配数量、实施范围、生效区间和范围版本，不复制或改写ERP合同正文。
3. 一个项目可以承载多份原始或追加合同。每次批准的追加以`project_id + source_contract/order_line + change_request_id`幂等处理，并创建单调递增的`ProjectScopeVersion`；旧范围版本和历史绑定不可原位覆盖。
4. 追加前必须校验订单行可用数量。同一订单行在所有有效项目范围中的累计分配数量不得超过ERP有效数量；支持按数量拆分时，必须精确保存每个项目的分配量和有效期。
5. 追加申请必须明确新增合同、订单行、设备/产品、数量、地点、服务内容及实施边界，并保存基准范围版本、变更原因、影响分析、审批实例和追加前后差异。
6. 审批通过后，平台按新增范围和当前冻结配置生成或补充所需项目任务、交付件要求、`WorkBinding`及门禁引用；不得仅更新合同清单而遗漏可执行交付节点。
7. 原范围已经完成的工期、计划、方案、到货、安装、配置、联调、割接、终验或闭环前事实，不自动覆盖新增范围。系统必须逐阶段判断覆盖关系；需要补充时创建新的工期/计划/方案/实施/验收版本或任务，并重新计算阶段完成、准出及后续准入门禁。
8. 范围追加不自动把`current_stage`强制退回S0。旧范围完成事实继续保留，新范围显示其真实未完成状态；若影响要求正式阶段回退，必须使用受控回退规则并保存原因和影响范围。
9. 新增范围是否需要补充验收由冻结模板、本次范围变更审批及已有验收范围约束决定，与当前是否为S5无关。需要验收时，原终验不得自动覆盖新增范围，所有明确引用该验收事实的任务、阶段、审批及正常闭环条件重新计算；不需要终验时保存明确规则依据。
10. 本需求不创建项目群组、期次项目、项目组合或第二个项目编码；多期只是追加合同与实施范围的业务场景之一。

**补充验收承载（修订016）：** 需要补充验收时按ACC-03形成当前范围的总体验收新版本，保留旧范围报告和结论；不得建立第二个“当前终验”或把旧报告范围从A改成A+B。追加不减少既有适用验收义务，明确不适用的新范围须保留冻结规则与本次审批依据。范围切换与依赖它的当前门禁失效必须原子完成；旧历史快照不改写。

**用户故事：**
作为服务经理，我希望把后续新增合同和实施范围合并追加到原项目中，由同一项目统一计划、实施和验收，而不是创建关联项目或项目组合。

**验收标准：**
- **WHEN** 授权用户为活动项目选择新的ERP合同/订单行、填写新增实施范围并通过审批
- **THEN** 平台保持原`project_id`不变，不创建新项目或组合；创建新的有效项目范围版本，保存合同/订单行引用、分配数量和追加差异
- **AND** 平台为新增范围生成所需任务、交付件和业务绑定，并重新计算受影响阶段及闭环门禁
- **WHEN** 原范围A已完成而新增范围B尚未实施
- **THEN** 系统保留A的历史完成事实，但不得把B或项目总体状态误判为已完成
- **WHEN** 新增范围需要终验而原终验只覆盖旧范围
- **THEN** 平台生成补充验收要求并使所有显式依赖该终验的阶段、任务及正常闭环门禁重新计算
- **WHEN** 订单行累计分配超过ERP有效数量、来源合同无效、新增范围已被其他项目占用、项目处于任一闭环终态或审批未通过
- **THEN** 平台拒绝生效，保持原范围版本和阶段状态不变，并返回具体冲突对象

**涉及数据字段：**
项目ID、原始/追加合同ID及来源版本、销售订单ID、订单行ID、ERP有效数量、项目分配数量、基准范围版本、当前范围版本、变更类型`CONTRACT_SCOPE_APPEND`、新增设备/产品/地点/服务范围、变更原因、影响分析、审批实例、追加前后差异、生成任务/交付件/WorkBinding引用、生效时间和幂等键

**权限与数据范围：**
项目经理可创建本人负责项目的追加草稿和影响分析；服务经理/合同管理员按配置审批。用户必须同时具备目标项目和来源合同/订单行的最小查看权限；合同金额等敏感字段继续按ERP/合同权限控制。任何角色均不得借追加操作取得其他项目或合同的泛化访问权。

**异常、降级及留痕要求：**
ERP不可用时允许保存待核对草稿，但不得批准生效或占用最终可分配数量。任务实例化、范围版本切换或状态重算任一步失败时追加事务整体回滚；重复请求返回已有结果。平台记录来源版本、数量占用、审批、范围前后版本、逐阶段影响判断、生成对象、门禁失效/恢复和操作人时间。

**依赖关系：**
- 前置依赖：COM-01订单行范围分配、PM-01项目、PM-03模板/任务/交付件/绑定配置、审批引擎
- 后续依赖：PRE/PLN/SCH/EXE/ACC/CLO按新增范围重算
- 关联依赖：ERP合同订单事实、PLT-02文件与审计能力
## INT-01 CRM/ERP项目同步

| 属性 | 内容 |
| ---- | ---- |
| 需求编号 | INT-01 |
| 所属阶段 | S0 |
| 用户角色 | 工程管理部 |
| 优先级 | P0 |
| 目标版本 | V1 |
| 复杂度 | 低 |
| 依赖条件 | 集成依赖：CRM/ERP接口 |

**业务场景与需求描述：**

作为项目交付平台的数据源头集成，当CRM中产生项目、客户及销售执行信息或ERP中产生合同与销售订单信息时，系统应入向同步并支撑PM-01项目立项管理；本需求为P0核心需求，数据源头须首批建设。CRM同步项目编码、项目名称、客户、销售、办事处、行业四级值、签约方式、实施方式、重大项目级别、执行单号和商机信息等，CRM是上述来源字段的权威来源，项目类别由当前平台维护、CRM不得写入或覆盖；ERP同步合同号、销售订单号、产品明细、金额等合同执行信息，ERP是合同与订单信息的权威来源。平台按项目编码、合同号和销售订单号建立CRM与ERP映射，字段按权威来源分别维护，人工仅补充缺失值，冲突进入受控裁决并保留来源值与裁决记录。CRM/ERP数据实时或准实时同步，变更后平台在准实时窗口内完成校验和更新；接口不可用时允许手工创建项目，恢复后受控关联补录。

**核心业务规则：**

1. CRM拥有项目来源、客户、销售、办事处、行业、重大项目级别、执行单和商机字段；ERP拥有合同、销售订单、产品明细和金额字段；平台拥有项目类别、项目实施层级、交付状态、阶段、任务及平台扩展字段。任一来源不得覆盖其他系统或平台拥有的字段。
2. CRM项目按CRM项目ID幂等，ERP合同/订单按"ERP合同ID+销售订单ID"幂等；平台项目ID是同步落地后的内部标识。重复事件、轮询重放或失败重试只更新同一来源记录，不得重复创建项目、合同或订单。
3. CRM项目先以项目编码建立候选项目；ERP订单按合同号、销售订单号及CRM提供的执行关联匹配。一个来源对象匹配零个或多个平台项目时进入"待映射"，须由工程管理部确认，不得自动绑定到名称相似项目。
4. 同步采用来源变更事件优先、增量任务兜底的方式，任务保存增量游标、计划频率、开始/结束时间和处理数量；每日执行一次CRM、ERP与平台来源记录对账。具体准实时频率由集成配置发布并作为运行验收参数，不得使用无记录的人工临时频率。
5. 来源权威字段发生冲突时保存CRM值、ERP值、平台现值和差异类型，不自动覆盖；工程管理部只能在字段Owner规则允许时确认映射或选择平台补充值，不能把裁决写回并冒充来源值。
6. 接口不可用时允许创建"平台临时"项目以保障实施，但必须标记缺失来源、禁止填写伪造的CRM/ERP来源ID。恢复后通过受控匹配合并来源关系，保留临时项目ID和所有业务引用。
7. 项目类别、级别、行业、同步状态、差异类型和裁决状态等可变化字段使用基础平台可配置字典；来源编码映射需版本化并保留历史解释。

**用户故事：**

作为工程管理部用户，我希望平台能够从CRM同步项目、客户和销售执行信息，从ERP同步合同与销售订单信息，并按字段权威来源自动创建项目，以便避免重复录入和多源覆盖；接口不可用时支持手工创建，恢复后受控关联补录。

**验收标准：**

- **WHEN** CRM中新增或变更项目、客户或销售执行信息
- **THEN** 平台通过CRM接口在准实时窗口内同步至项目档案
- **AND** CRM权威字段按来源更新，平台扩展字段不被覆盖
- **WHEN** ERP系统中产生与项目关联的销售订单
- **THEN** 平台同步合同与销售订单数据，按项目编码、合同号或销售订单号建立CRM与ERP映射
- **AND** 来源冲突进入待裁决状态，未经审批不得覆盖权威字段
- **WHEN** CRM/ERP接口不可用
- **THEN** 平台允许手工创建项目并标记来源待补录
- **AND** 接口恢复后执行关联补录，仅补充空缺字段或经审批裁决的冲突字段，并保留补录记录
- **WHEN** 同一来源事件重复到达、来源业务键已映射，或一个来源对象匹配多个平台项目发生冲突
- **THEN** 平台对重复事件返回既有映射且不创建重复对象；对多匹配记录标记"待映射"并保留候选列表，不自动覆盖项目权威字段

**涉及数据字段：**

同步任务ID、来源系统、来源事件ID、增量游标、CRM项目ID/编码、项目名称、客户ID、销售、办事处、市场部、系统部、拓展部、子行业、签约方式、实施方式、重大项目级别、执行单号、商机ID；平台项目类别及变更依据；ERP合同ID/合同号、销售订单ID/订单号、产品明细、金额；平台项目ID、临时项目标识、来源映射关系、字段Owner、来源原值、平台现值、同步批次/时间/状态、处理数量、差异项、待映射候选、裁决状态/人/时间/记录、对账日期/结果

**权限与数据范围：**

CRM和ERP集成服务账号仅可写入各自Owner字段及同步日志；工程管理部可在所属组织范围内查看差异、确认映射、创建临时项目和处理允许裁决的字段，但不能修改来源原值或更改项目实施数据。项目经理只读查看本人项目的来源状态和脱敏差异，无权执行批次重试或跨项目映射。集成管理员可配置任务、重试和对账但不能裁决业务归属；跨租户来源记录与映射不可见。

**异常、降级及留痕要求：**

接口超时、鉴权失败或部分数据校验失败时，同步批次标记"失败/部分失败"，成功记录可提交，失败记录按来源业务键重试；增量游标只推进到已安全提交位置。来源键缺失、映射多义或字段Owner冲突时进入待处理队列，不创建错误关联。对账差异不自动删除平台项目，须由工程管理部处理。平台保存请求/响应摘要、批次、幂等命中、字段差异、映射裁决、临时项目合并、重试和对账留痕，不记录接口密钥。

**依赖关系：**

- 前置依赖：CRM接口、ERP接口
- 后续依赖：PM-01项目立项管理（项目创建基于CRM/ERP双源数据）、INT-03 CRM客户同步（复用CRM客户主数据）
- 关联依赖：权限体系（工程管理部同步权限）

---

## 4. V3演进范围

本领域暂无直接对应的V3演进项；后续新增方向必须先完成Owner、范围和验收边界评审。

## 5. OUT_OF_SCOPE追溯

| 追溯编号 | 排除主题 | 处理结论 |
| --- | --- | --- |
| FR-PROJ-023 | 项目日报与周报 | 结构化项目数据及看板聚合继续保留 |

## 6. 领域验收门禁

- 本领域正式需求全部直接来源于PRD需求块，业务验收标准以PRD为准。
- V1/V2需求不得以V3方向替代；V3不进入当前开发与验收。
- 领域状态必须遵循“初始化可扩展状态定义+受控状态机”；扩展状态不得绕过权限、责任、审批或证据门禁。
- 跨领域写入必须通过应用服务、接口或事件完成，不直接修改其他领域业务事实。
- 权限拒绝、版本冲突、重复提交、外部失败和人工降级必须保留可追溯记录。
