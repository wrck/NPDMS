# RES领域需求规格：设备、备件、公告与外协

> 文档状态：评审基线<br>
> 实现模块：pms-module-asset / pms-module-outsourcing<br>
> 技术约束：`appendices/module-boundary-and-naming.md`、`appendices/api-design-specification.md`<br>
> 详细需求：28项；V3演进：1项

## 1. 领域目标与边界

管理设备档案、备件、RMA、技术公告、服务商、转包、工时和付款协同。

本分册只定义本领域拥有的业务规则和数据；公共身份、权限、文件、工作流、审计和通知规则引用总体规格。

## 2. 需求清单

| 功能编号 | 来源 | 名称 | 版本 | 优先级 |
| --- | --- | --- | --- | --- |
| FR-RES-001 | REQ-017 | 设备与序列号档案 | V1 | P0 |
| FR-RES-002 | REQ-018 | 设备版本历史 | V1 | P0 |
| FR-RES-003 | REQ-019 | 设备配置Log档案 | V1 | P0 |
| FR-RES-004 | REQ-020 | 设备安装位置与照片 | V2 | P1 |
| FR-RES-005 | REQ-104 | 打卡与考勤同步 | V2 | P2 |
| FR-RES-006 | REQ-105 | 工作记录与项目关联 | V2 | P1 |
| FR-RES-007 | REQ-106 | 工时申报与审批 | V2 | P1 |
| FR-RES-008 | REQ-112 | 服务商档案与权限 | V2 | P1 |
| FR-RES-009 | REQ-113 | 转包申请与项目关联 | V2 | P1 |
| FR-RES-010 | REQ-114 | 可配置转包审批 | V2 | P1 |
| FR-RES-011 | REQ-115 | 合同与订单回写 | V2 | P2 |
| FR-RES-012 | REQ-116 | 转包付款条件校验 | V2 | P1 |
| FR-RES-013 | REQ-117 | 分阶段付款与余额 | V2 | P1 |
| FR-RES-014 | REQ-118 | 转包回访门禁 | V2 | P2 |
| FR-RES-015 | REQ-119 | RMA批量申请 | V2 | P2 |
| FR-RES-016 | REQ-120 | 备件库选择与发货策略 | V2 | P2 |
| FR-RES-017 | REQ-121 | 好坏件全流程管理 | V2 | P2 |
| FR-RES-018 | REQ-122 | 借用与补库申请 | V2 | P2 |
| FR-RES-019 | REQ-123 | 备件转移与交接 | V2 | P2 |
| FR-RES-020 | REQ-124 | 备件库存告警与预测 | V3 | P3 |
| FR-RES-021 | REQ-125 | RMA替换继承维保 | V2 | P1 |
| FR-RES-022 | REQ-126 | 技术公告模板与编制 | V2 | P2 |
| FR-RES-023 | REQ-127 | 影响版本规范与重叠校验 | V2 | P2 |
| FR-RES-024 | REQ-128 | 技术公告分产品线会签 | V2 | P2 |
| FR-RES-025 | REQ-129 | 技术公告检索 | V2 | P2 |
| FR-RES-026 | REQ-130 | 公告命中项目与设备 | V2 | P1 |
| FR-RES-027 | REQ-131 | 公告治理工单与优先级 | V2 | P2 |
| FR-RES-028 | REQ-132 | 公告与ITR双向关联 | V2 | P2 |
| FR-RES-029 | REQ-133 | 技术公告处置统计 | V2 | P2 |

## 3. 详细功能规格

## FR-RES-001 设备与序列号档案

**用例编号：** UC-RES-001<br>
**来源需求：** REQ-017<br>
**所属版本：** V1<br>
**优先级／复杂度：** P0／高<br>
**参与角色：** 项目团队/服务人员<br>
**业务场景：** 查看项目设备全生命周期<br>
**来源标识：** 原始归纳

### 业务目标

以序列号建立唯一设备身份档案，并分别关联产品编码型号、历次发货/退货事件、项目归属、安装位置、版本、维保、License、公告命中和服务记录。

### 前置条件

- 用户已登录且具有“设备资产”相应功能权限和项目数据权限。
- 依赖条件：REQ-009,REQ-014。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 项目团队进入“设备资产”功能，并选择对应项目或设备资产。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-009,REQ-014）。
1. 用户上传数据或触发接口/采集任务，系统创建唯一批次号和幂等键。
1. 系统进行格式、必填、编码、关联对象和权限校验，逐条输出校验结果。
1. 校验通过的数据写入设备资产；冲突数据按配置执行拒绝、覆盖或人工确认。
1. 系统生成处理摘要、失败明细和审计记录，并按需发送完成通知。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持设备资产原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 部分失败：成功记录可按批次提交，失败记录保留原因并支持修复后重试。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-011：** 设备资产只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-012：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-013：** “设备与序列号档案”必须满足依赖条件和当前状态门禁，不得通过前端绕过。
- **BR-RES-014：** 设备身份、物流事件和项目归属必须分别管理；一次发货、一次转移或一次RMA替换不得覆盖设备身份档案。
- **BR-RES-015：** 【建议】同一租户内一个有效SN对应一个设备身份；历史重复SN应先归并可证明属于同一设备的事件，无法证明的冲突进入数据治理清单。
- **BR-RES-016：** 同一设备可关联多次发货、退货、再发或RMA事件；每次事件均须保留发生时间、来源证据和关联业务对象。
- **BR-RES-017：** 设备与项目的归属关系应保存生效区间和转移历史，不以设备档案上的单一项目字段覆盖历史归属。
- **BR-RES-018：** 设备已能确认身份和项目归属但暂时无法定位订单行时，应保留设备与归属，将物流事件标记为待映射，不得因订单行缺失丢弃设备记录。

### 数据要求

- 设备资产标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。
- 物流事件：装箱/发货凭证、事件类型、业务动作、RMA号、发生时间、订单行映射状态和来源证据。
- 项目归属：项目、归属类型、生效时间、失效时间、转移批次和可选的订单行实施范围。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的设备资产、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-011：** Given 项目团队具备权限且前置条件满足，When 完成“设备与序列号档案”正常操作，Then 系统应保存正确结果、更新设备资产及关联状态并记录审计。
- **AC-RES-012：** Given 用户无数据权限或设备资产状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-013：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。
- **AC-RES-014：** Given 同一SN先后发生发货、退货和再次发货，When 用户查看设备档案，Then 系统应只展示一个设备身份，并按时间显示三条独立物流事件且可回溯来源。
- **AC-RES-015：** Given 设备身份和项目归属已确认但订单行无法唯一映射，When 同步设备资料，Then 系统应保留设备及归属、将相关事件标记待映射，不得重复创建设备或丢弃记录。

## FR-RES-002 设备版本历史

**用例编号：** UC-RES-002<br>
**来源需求：** REQ-018<br>
**所属版本：** V1<br>
**优先级／复杂度：** P0／高<br>
**参与角色：** 工程师/技术支持<br>
**业务场景：** 配置或维护后更新设备版本<br>
**来源标识：** 原始归纳

### 业务目标

维护软件、分支、CPLD、Conboot、官网版本和历史变更，保留最新值与变更轨迹。

### 前置条件

- 用户已登录且具有“设备资产”相应功能权限和项目数据权限。
- 依赖条件：REQ-017。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“设备资产”功能，并选择对应项目或设备资产。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-017）。
1. 用户创建或选择设备资产，录入“设备版本历史”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新设备资产、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持设备资产原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-021：** 设备资产只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-022：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-023：** “设备版本历史”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 设备资产标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的设备资产、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-021：** Given 工程师具备权限且前置条件满足，When 完成“设备版本历史”正常操作，Then 系统应保存正确结果、更新设备资产及关联状态并记录审计。
- **AC-RES-022：** Given 用户无数据权限或设备资产状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-023：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-003 设备配置Log档案

**用例编号：** UC-RES-003<br>
**来源需求：** REQ-019<br>
**所属版本：** V1<br>
**优先级／复杂度：** P0／极高<br>
**参与角色：** 工程师/项目经理<br>
**业务场景：** 配置调试、联调或巡检后归档配置<br>
**来源标识：** 原始归纳

### 业务目标

按设备或板卡序列号拆分并保存配置Log，记录来源、上传人、时间、版本和下载删除权限。

### 前置条件

- 用户已登录且具有“设备资产”相应功能权限和项目数据权限。
- 依赖条件：REQ-017,REQ-008。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“设备资产”功能，并选择对应项目或设备资产。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-017,REQ-008）。
1. 用户选择模板、文件或业务数据，系统校验文件类型、大小、版本和设备资产关联。
1. 系统生成或保存新版本，记录来源、创建人、时间、摘要和文件校验值。
1. 用户预览、下载、提交评审或归档；系统按状态控制编辑、替换和删除。
1. 系统更新关联业务记录、交付物完整性和审计日志。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持设备资产原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-031：** 设备资产只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-032：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-033：** “设备配置Log档案”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 设备资产标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 文件元数据：文件名、类型、大小、存储标识、校验值、版本、上传人和上传时间。
- 时间数据：计划开始/结束、实际开始/结束、时区、时效规则和超期原因。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的设备资产、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-031：** Given 工程师具备权限且前置条件满足，When 完成“设备配置Log档案”正常操作，Then 系统应保存正确结果、更新设备资产及关联状态并记录审计。
- **AC-RES-032：** Given 用户无数据权限或设备资产状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-033：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-004 设备安装位置与照片

**用例编号：** UC-RES-004<br>
**来源需求：** REQ-020<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／中<br>
**参与角色：** 现场工程师<br>
**业务场景：** 设备上架安装<br>
**来源标识：** 原始归纳

### 业务目标

按序列号记录机房、机柜、U位、定位信息和安装照片，并同步客户资产档案。

### 前置条件

- 用户已登录且具有“设备资产”相应功能权限和项目数据权限。
- 依赖条件：REQ-017。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 现场工程师进入“设备资产”功能，并选择对应项目或设备资产。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-017）。
1. 用户上传数据或触发接口/采集任务，系统创建唯一批次号和幂等键。
1. 系统进行格式、必填、编码、关联对象和权限校验，逐条输出校验结果。
1. 校验通过的数据写入设备资产；冲突数据按配置执行拒绝、覆盖或人工确认。
1. 系统生成处理摘要、失败明细和审计记录，并按需发送完成通知。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持设备资产原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 部分失败：成功记录可按批次提交，失败记录保留原因并支持修复后重试。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-041：** 设备资产只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-042：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-043：** “设备安装位置与照片”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 设备资产标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 文件元数据：文件名、类型、大小、存储标识、校验值、版本、上传人和上传时间。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的设备资产、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-041：** Given 现场工程师具备权限且前置条件满足，When 完成“设备安装位置与照片”正常操作，Then 系统应保存正确结果、更新设备资产及关联状态并记录审计。
- **AC-RES-042：** Given 用户无数据权限或设备资产状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-043：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-005 打卡与考勤同步

**用例编号：** UC-RES-005<br>
**来源需求：** REQ-104<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 工程师<br>
**业务场景：** 现场工作签到签退<br>
**来源标识：** 原始归纳

### 业务目标

同步钉钉和EHR打卡、位置及补卡，生成待补充的工作记录。

### 前置条件

- 用户已登录且具有“工作记录”相应功能权限和项目数据权限。
- 依赖条件：REQ-009。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“工作记录”功能，并选择对应项目或工作记录。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-009）。
1. 用户上传数据或触发接口/采集任务，系统创建唯一批次号和幂等键。
1. 系统进行格式、必填、编码、关联对象和权限校验，逐条输出校验结果。
1. 校验通过的数据写入工作记录；冲突数据按配置执行拒绝、覆盖或人工确认。
1. 系统生成处理摘要、失败明细和审计记录，并按需发送完成通知。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持工作记录原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 部分失败：成功记录可按批次提交，失败记录保留原因并支持修复后重试。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-051：** 工作记录只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-052：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-053：** “打卡与考勤同步”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 工作记录标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的工作记录、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-051：** Given 工程师具备权限且前置条件满足，When 完成“打卡与考勤同步”正常操作，Then 系统应保存正确结果、更新工作记录及关联状态并记录审计。
- **AC-RES-052：** Given 用户无数据权限或工作记录状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-053：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-006 工作记录与项目关联

**用例编号：** UC-RES-006<br>
**来源需求：** REQ-105<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 工程师<br>
**业务场景：** 记录项目实施、维护和售前工作<br>
**来源标识：** 原始归纳

### 业务目标

支持按打卡、项目、序列号或扫描设备创建记录，维护工作性质、耗时、进展和附件。

### 前置条件

- 用户已登录且具有“工作记录”相应功能权限和项目数据权限。
- 依赖条件：REQ-104,REQ-017,REQ-014。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“工作记录”功能，并选择对应项目或项目。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-104,REQ-017,REQ-014）。
1. 用户选择模板、文件或业务数据，系统校验文件类型、大小、版本和项目关联。
1. 系统生成或保存新版本，记录来源、创建人、时间、摘要和文件校验值。
1. 用户预览、下载、提交评审或归档；系统按状态控制编辑、替换和删除。
1. 系统更新关联业务记录、交付物完整性和审计日志。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持项目原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-061：** 项目只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-062：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-063：** “工作记录与项目关联”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 项目标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 文件元数据：文件名、类型、大小、存储标识、校验值、版本、上传人和上传时间。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的项目、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-061：** Given 工程师具备权限且前置条件满足，When 完成“工作记录与项目关联”正常操作，Then 系统应保存正确结果、更新项目及关联状态并记录审计。
- **AC-RES-062：** Given 用户无数据权限或项目状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-063：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-007 工时申报与审批

**用例编号：** UC-RES-007<br>
**来源需求：** REQ-106<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 工程师/审批人<br>
**业务场景：** 统计项目和服务投入<br>
**来源标识：** 原始归纳

### 业务目标

按业务类型配置工时审批，区分处理、在途耗时并支持项目、客户、人员维度统计。

### 前置条件

- 用户已登录且具有“工时管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-105,REQ-006。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“工时管理”功能，并选择对应项目或工时管理。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-105,REQ-006）。
1. 发起人完善工时管理内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后工时管理生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持工时管理原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-071：** 工时管理只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-072：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-073：** “工时申报与审批”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 工时管理标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 流程数据：流程实例、节点、审批人、意见、动作、到达时间和完成时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的工时管理、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-071：** Given 工程师具备权限且前置条件满足，When 完成“工时申报与审批”正常操作，Then 系统应保存正确结果、更新工时管理及关联状态并记录审计。
- **AC-RES-072：** Given 用户无数据权限或工时管理状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-073：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-008 服务商档案与权限

**用例编号：** UC-RES-008<br>
**来源需求：** REQ-112<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 服务经理/管理员<br>
**业务场景：** 选择外协服务商实施<br>
**来源标识：** 原始归纳

### 业务目标

维护服务商资质、账户和工程师，按项目授权其数据及操作范围。

### 前置条件

- 用户已登录且具有“转包管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-003,REQ-024。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 服务经理进入“转包管理”功能，并选择对应项目或转包任务。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-003,REQ-024）。
1. 用户创建或选择转包任务，录入“服务商档案与权限”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新转包任务、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持转包任务原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-081：** 转包任务只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-082：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-083：** “服务商档案与权限”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 转包任务标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的转包任务、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-081：** Given 服务经理具备权限且前置条件满足，When 完成“服务商档案与权限”正常操作，Then 系统应保存正确结果、更新转包任务及关联状态并记录审计。
- **AC-RES-082：** Given 用户无数据权限或转包任务状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-083：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-009 转包申请与项目关联

**用例编号：** UC-RES-009<br>
**来源需求：** REQ-113<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 服务经理<br>
**业务场景：** 项目全部或部分设备转包<br>
**来源标识：** 原始归纳

### 业务目标

按合同号创建转包单，支持多项目、部分序列号、类型、原因、金额和服务商带入。

### 前置条件

- 用户已登录且具有“转包管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-112,REQ-014,REQ-017。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 服务经理进入“转包管理”功能，并选择对应项目或项目。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-112,REQ-014,REQ-017）。
1. 用户创建或选择项目，录入“转包申请与项目关联”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新项目、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持项目原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-091：** 项目只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-092：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-093：** “转包申请与项目关联”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 项目标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的项目、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-091：** Given 服务经理具备权限且前置条件满足，When 完成“转包申请与项目关联”正常操作，Then 系统应保存正确结果、更新项目及关联状态并记录审计。
- **AC-RES-092：** Given 用户无数据权限或项目状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-093：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-010 可配置转包审批

**用例编号：** UC-RES-010<br>
**来源需求：** REQ-114<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／极高<br>
**参与角色：** 服务经理/工程管理/总监/主任<br>
**业务场景：** 审核转包必要性和金额<br>
**来源标识：** 原始归纳

### 业务目标

支持受益部门、工程管理、总监和主任等条件节点、提醒、驳回及任意节点终止。

### 前置条件

- 用户已登录且具有“转包管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-113,REQ-006。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 服务经理进入“转包管理”功能，并选择对应项目或转包任务。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-113,REQ-006）。
1. 发起人完善转包任务内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后转包任务生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持转包任务原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-101：** 转包任务只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-102：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-103：** “可配置转包审批”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 转包任务标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 流程数据：流程实例、节点、审批人、意见、动作、到达时间和完成时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的转包任务、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-101：** Given 服务经理具备权限且前置条件满足，When 完成“可配置转包审批”正常操作，Then 系统应保存正确结果、更新转包任务及关联状态并记录审计。
- **AC-RES-102：** Given 用户无数据权限或转包任务状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-103：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-011 合同与订单回写

**用例编号：** UC-RES-011<br>
**来源需求：** REQ-115<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／极高<br>
**参与角色：** 工程人员/系统<br>
**业务场景：** 转包审批通过后生成合同<br>
**来源标识：** 原始归纳

### 业务目标

录入TS合同并对接D365或财务系统生成PA订单，回写合同扫描件和状态。

### 前置条件

- 用户已登录且具有“转包管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-114,REQ-009。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程人员进入“转包管理”功能，并选择对应项目或转包任务。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-114,REQ-009）。
1. 用户上传数据或触发接口/采集任务，系统创建唯一批次号和幂等键。
1. 系统进行格式、必填、编码、关联对象和权限校验，逐条输出校验结果。
1. 校验通过的数据写入转包任务；冲突数据按配置执行拒绝、覆盖或人工确认。
1. 系统生成处理摘要、失败明细和审计记录，并按需发送完成通知。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持转包任务原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 部分失败：成功记录可按批次提交，失败记录保留原因并支持修复后重试。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-111：** 转包任务只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-112：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-113：** “合同与订单回写”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 转包任务标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的转包任务、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-111：** Given 工程人员具备权限且前置条件满足，When 完成“合同与订单回写”正常操作，Then 系统应保存正确结果、更新转包任务及关联状态并记录审计。
- **AC-RES-112：** Given 用户无数据权限或转包任务状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-113：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-012 转包付款条件校验

**用例编号：** UC-RES-012<br>
**来源需求：** REQ-116<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 服务经理/财务<br>
**业务场景：** 服务商满足阶段付款条件<br>
**来源标识：** 原始归纳

### 业务目标

按转包类型校验发票、验收、满意度、服务记录、总结、日报和考勤材料。

### 前置条件

- 用户已登录且具有“付款管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-113,REQ-098。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 服务经理进入“付款管理”功能，并选择对应项目或付款申请。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-113,REQ-098）。
1. 发起人完善付款申请内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后付款申请生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持付款申请原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-121：** 付款申请只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-122：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-123：** “转包付款条件校验”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 付款申请标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的付款申请、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-121：** Given 服务经理具备权限且前置条件满足，When 完成“转包付款条件校验”正常操作，Then 系统应保存正确结果、更新付款申请及关联状态并记录审计。
- **AC-RES-122：** Given 用户无数据权限或付款申请状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-123：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-013 分阶段付款与余额

**用例编号：** UC-RES-013<br>
**来源需求：** REQ-117<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 工程管理/财务<br>
**业务场景：** 多次付款或余款未清<br>
**来源标识：** 原始归纳

### 业务目标

维护申请金额、发票金额、已付未付和付款状态，支持余款续提及作废。

### 前置条件

- 用户已登录且具有“付款管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-116,REQ-009。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程管理进入“付款管理”功能，并选择对应项目或付款申请。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-116,REQ-009）。
1. 用户创建或选择付款申请，录入“分阶段付款与余额”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新付款申请、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持付款申请原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-131：** 付款申请只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-132：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-133：** “分阶段付款与余额”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 付款申请标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的付款申请、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-131：** Given 工程管理具备权限且前置条件满足，When 完成“分阶段付款与余额”正常操作，Then 系统应保存正确结果、更新付款申请及关联状态并记录审计。
- **AC-RES-132：** Given 用户无数据权限或付款申请状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-133：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-014 转包回访门禁

**用例编号：** UC-RES-014<br>
**来源需求：** REQ-118<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／中<br>
**参与角色：** 回访人员/财务<br>
**业务场景：** 维护类转包申请付款<br>
**来源标识：** 原始归纳

### 业务目标

匹配维护问卷，回访通过后才允许进入付款审批。

### 前置条件

- 用户已登录且具有“转包管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-096,REQ-116。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 回访人员进入“转包管理”功能，并选择对应项目或转包任务。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-096,REQ-116）。
1. 发起人完善转包任务内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后转包任务生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持转包任务原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-141：** 转包任务只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-142：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-143：** “转包回访门禁”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 转包任务标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 流程数据：流程实例、节点、审批人、意见、动作、到达时间和完成时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的转包任务、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-141：** Given 回访人员具备权限且前置条件满足，When 完成“转包回访门禁”正常操作，Then 系统应保存正确结果、更新转包任务及关联状态并记录审计。
- **AC-RES-142：** Given 用户无数据权限或转包任务状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-143：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-015 RMA批量申请

**用例编号：** UC-RES-015<br>
**来源需求：** REQ-119<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／极高<br>
**参与角色：** 工程师/备件人员<br>
**业务场景：** 设备故障需返修换件<br>
**来源标识：** 原始归纳

### 业务目标

支持扫码、导入、ITR问题单等方式批量申请并修改返回、维保、故障和版本属性。

### 前置条件

- 用户已登录且具有“备件管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-017,REQ-109。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“备件管理”功能，并选择对应项目或备件。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-017,REQ-109）。
1. 用户上传数据或触发接口/采集任务，系统创建唯一批次号和幂等键。
1. 系统进行格式、必填、编码、关联对象和权限校验，逐条输出校验结果。
1. 校验通过的数据写入备件；冲突数据按配置执行拒绝、覆盖或人工确认。
1. 系统生成处理摘要、失败明细和审计记录，并按需发送完成通知。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持备件原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 部分失败：成功记录可按批次提交，失败记录保留原因并支持修复后重试。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-151：** 备件只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-152：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-153：** “RMA批量申请”必须满足依赖条件和当前状态门禁，不得通过前端绕过。
- **BR-RES-154：** 批量RMA申请应逐条校验SN、故障信息、原设备关系和来源业务键，返回每条成功、失败或待确认结果；单条失败不得掩盖其他记录结果。
- **BR-RES-155：** RMA替换应显式关联原SN、新SN、RMA单号和生效时间，不得把新SN覆盖写入原设备档案。
- **BR-RES-156：** 【待确认】RMA、借转销、借转退、借用返还、再次发放等业务动作的正式字典及生效规则；字典未确认前，只能保留源动作和“未分类”状态，不得仅凭RMA号推断动作。

### 数据要求

- 备件标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的备件、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-151：** Given 工程师具备权限且前置条件满足，When 完成“RMA批量申请”正常操作，Then 系统应保存正确结果、更新备件及关联状态并记录审计。
- **AC-RES-152：** Given 用户无数据权限或备件状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-153：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-016 备件库选择与发货策略

**用例编号：** UC-RES-016<br>
**来源需求：** REQ-120<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／极高<br>
**参与角色：** 审批人/库管<br>
**业务场景：** RMA好件发货<br>
**来源标识：** 原始归纳

### 业务目标

按本地、就近、中央、库龄等优先级选择备件库，支持联合发货和差异化流程。

### 前置条件

- 用户已登录且具有“备件管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-119。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 审批人进入“备件管理”功能，并选择对应项目或备件。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-119）。
1. 用户创建或选择备件，录入“备件库选择与发货策略”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新备件、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持备件原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-161：** 备件只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-162：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-163：** “备件库选择与发货策略”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 备件标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的备件、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-161：** Given 审批人具备权限且前置条件满足，When 完成“备件库选择与发货策略”正常操作，Then 系统应保存正确结果、更新备件及关联状态并记录审计。
- **AC-RES-162：** Given 用户无数据权限或备件状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-163：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-017 好坏件全流程管理

**用例编号：** UC-RES-017<br>
**来源需求：** REQ-121<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 工程师/库管<br>
**业务场景：** 好件发出接收及坏件返回核销<br>
**来源标识：** 原始归纳

### 业务目标

支持分批次发货、接收、维保替换、坏件返回和核销。

### 前置条件

- 用户已登录且具有“备件管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-119。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师或库管选择好件发出、接收、坏件返回、核销、再次发放或RMA替换动作，并录入批次和设备明细。
1. 系统校验用户权限、项目数据范围、设备身份、当前有效归属和依赖条件（REQ-119）。
1. 系统为每次业务动作生成独立物流事件，关联装箱/发货凭证、订单行、RMA单号和来源证据。
1. 无法确认订单行或动作分类的记录进入待处理状态，但已确认设备身份和归属继续保留。
1. 用户提交后，系统更新好件/坏件处理状态、待办和操作日志，不覆盖此前物流事件。
1. 用户可按设备、批次、项目或RMA链路查看完整时间线并执行核销。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持备件原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-171：** 备件只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-172：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-173：** “好坏件全流程管理”必须满足依赖条件和当前状态门禁，不得通过前端绕过。
- **BR-RES-174：** 每次发货、接收、退货、再次发放、坏件返回或RMA处理均形成独立业务事件，并记录设备、时间、来源、装箱/发货凭证及可选订单行。
- **BR-RES-175：** 销售订单、退货订单和RMA退货数量应按来源类型分别解释；【待确认】退货是否采用负数数量、冲销与退货的关系及对完成量的影响。
- **BR-RES-176：** 物流事件无法关联订单行时应标记待映射，不得阻断设备身份、项目归属和物流来源证据的保存。
- **BR-RES-177：** 已生效物流事件不得通过普通编辑覆盖；纠错应新增更正、冲销或后续事件并保留与原事件的关系。

### 数据要求

- 备件标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的备件、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-171：** Given 工程师具备权限且前置条件满足，When 完成“好坏件全流程管理”正常操作，Then 系统应保存正确结果、更新备件及关联状态并记录审计。
- **AC-RES-172：** Given 用户无数据权限或备件状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-173：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。
- **AC-RES-174：** Given 同一设备经历好件发出、坏件返回和再次发放，When 用户查看全流程，Then 系统应按时间展示独立事件、当前处理状态及每次事件的来源凭证。
- **AC-RES-175：** Given 一条退货或RMA事件尚未确认数量符号或订单行映射，When 系统统计发货与完成量，Then 该事件应进入待确认统计且不得被无依据地计为正常发货完成量。

## FR-RES-018 借用与补库申请

**用例编号：** UC-RES-018<br>
**来源需求：** REQ-122<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 工程师/库管<br>
**业务场景：** 储备、补库或重点客户保障<br>
**来源标识：** 原始归纳

### 业务目标

配置申请类型和表单，支持批量产品、数量调整、审批和分批接收。

### 前置条件

- 用户已登录且具有“备件管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-006。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 工程师进入“备件管理”功能，并选择对应项目或备件。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-006）。
1. 发起人完善备件内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后备件生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持备件原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-181：** 备件只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-182：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-183：** “借用与补库申请”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 备件标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。
- 流程数据：流程实例、节点、审批人、意见、动作、到达时间和完成时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的备件、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-181：** Given 工程师具备权限且前置条件满足，When 完成“借用与补库申请”正常操作，Then 系统应保存正确结果、更新备件及关联状态并记录审计。
- **AC-RES-182：** Given 用户无数据权限或备件状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-183：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-019 备件转移与交接

**用例编号：** UC-RES-019<br>
**来源需求：** REQ-123<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 库管/责任人<br>
**业务场景：** 责任人、存放地或离职交接变化<br>
**来源标识：** 原始归纳

### 业务目标

支持批量转移、接收、审批和完整转移记录，离职交接不重置库龄。

### 前置条件

- 用户已登录且具有“备件管理”相应功能权限和项目数据权限。
- 依赖条件：REQ-122。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 库管进入“备件管理”功能，并选择对应项目或备件。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-122）。
1. 发起人完善备件内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后备件生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持备件原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-191：** 备件只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-192：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-193：** “备件转移与交接”必须满足依赖条件和当前状态门禁，不得通过前端绕过。
- **BR-RES-194：** 设备或备件转移应保存转出项目/责任人、转入项目/责任人、转移批次、生效时间、交接确认和原归属失效时间，形成连续归属历史。
- **BR-RES-195：** 【待确认】同一设备同一时点是否只允许一个有效项目实施归属；确认前系统应识别并报告重叠有效区间，不得按最后一条记录静默覆盖。
- **BR-RES-196：** 转移与离职交接只改变归属、责任人或存放地，不重建设备身份，不重置库龄、维保起点和既有物流历史。
- **BR-RES-197：** 转移记录与当前归属冲突、交接对象失效或项目范围不一致时，应阻断生效并保留待处理原因；不得在审批失败时留下部分归属变化。

### 数据要求

- 备件标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。
- 流程数据：流程实例、节点、审批人、意见、动作、到达时间和完成时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的备件、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-191：** Given 库管具备权限且前置条件满足，When 完成“备件转移与交接”正常操作，Then 系统应保存正确结果、更新备件及关联状态并记录审计。
- **AC-RES-192：** Given 用户无数据权限或备件状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-193：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。
- **AC-RES-194：** Given 一台设备从项目A转移到项目B且双方完成交接，When 转移生效，Then 项目A归属应按生效时间结束、项目B归属开始，设备身份、库龄、维保和此前事件保持不变。
- **AC-RES-195：** Given 新转移区间与既有有效项目归属重叠，When 用户提交转移，Then 系统应识别并展示重叠区间；唯一归属规则确认前进入冲突待处理，不得以新记录静默覆盖旧归属。

## FR-RES-021 RMA替换继承维保

**用例编号：** UC-RES-021<br>
**来源需求：** REQ-125<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／高<br>
**参与角色：** 系统/维保人员<br>
**业务场景：** 坏件替换为新序列号<br>
**来源标识：** 原始归纳

### 业务目标

新序列号继承原设备维保期限并保留RMA链路和维修报告。

### 前置条件

- 用户已登录且具有“设备资产”相应功能权限和项目数据权限。
- 依赖条件：REQ-017,REQ-119,REQ-103。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 维保人员从RMA申请或坏件记录选择原设备，并录入/同步替换设备SN、RMA单号、替换时间和维修报告。
1. 系统校验用户权限、项目数据范围、原设备与替换设备身份、RMA证据及当前状态。
1. 系统建立从原设备到替换设备的RMA替换关系，不修改或删除原设备身份和历史。
1. 系统按已确认维保规则计算替换设备的继承期限，保留原期限、计算依据和调整记录。
1. 若关系、自关联、重复来源或动作分类存在冲突，系统阻断正式生效并进入待处理清单。
1. 生效后，用户可从任一设备查看RMA链路、维修报告、维保期限及关联项目。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持设备资产原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-211：** 设备资产只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-212：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-213：** “RMA替换继承维保”必须满足依赖条件和当前状态门禁，不得通过前端绕过。
- **BR-RES-214：** RMA替换关系应显式记录原设备、新设备、RMA单号、生效时间和来源证据，并保持明确方向。
- **BR-RES-215：** 【建议】设备不得与自身建立替换关系；同一来源证据重复到达时应幂等处理，不重复生成替换链路。
- **BR-RES-216：** 替换设备继承原设备剩余维保期限；系统应保存原期限、继承后的期限、计算时间和人工调整依据。
- **BR-RES-217：** 建立替换关系不得覆盖原设备SN、历史项目归属、物流事件或维修记录；原设备和替换设备均可追溯完整RMA链路。
- **BR-RES-218：** 【待确认】RMA替换与返修后原机返回、再次替换、借用替换之间的正式关系类型和期限继承规则；未确认前保留源动作并标记未分类。

### 数据要求

- 设备资产标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。
- RMA关系：原设备、替换设备、RMA单号、关系类型、生效时间、来源证据、维修报告和维保继承依据。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的设备资产、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-211：** Given 系统具备权限且前置条件满足，When 完成“RMA替换继承维保”正常操作，Then 系统应保存正确结果、更新设备资产及关联状态并记录审计。
- **AC-RES-212：** Given 用户无数据权限或设备资产状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-213：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。
- **AC-RES-214：** Given 原设备处于有效维保期且存在已确认RMA替换证据，When 替换关系生效，Then 新设备应继承剩余维保期限，原设备身份与历史保持可查，并可从双方查看同一RMA链路。
- **AC-RES-215：** Given 替换设备与原设备相同、来源证据重复或关系类型无法确认，When 用户提交RMA替换，Then 系统应分别拒绝自关联、幂等返回既有关系或标记待确认，不得覆盖设备主档。

## FR-RES-022 技术公告模板与编制

**用例编号：** UC-RES-022<br>
**来源需求：** REQ-126<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 研发/管理员<br>
**业务场景：** 发布技术公告或漏洞通告<br>
**来源标识：** 原始归纳

### 业务目标

按模板维护现象、条件、判断、原因、影响、巡检、预防、恢复和解决方案。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-022,REQ-008。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 研发进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-022,REQ-008）。
1. 用户创建或选择技术公告，录入“技术公告模板与编制”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新技术公告、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-221：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-222：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-223：** “技术公告模板与编制”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 文件元数据：文件名、类型、大小、存储标识、校验值、版本、上传人和上传时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-221：** Given 研发具备权限且前置条件满足，When 完成“技术公告模板与编制”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-222：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-223：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-023 影响版本规范与重叠校验

**用例编号：** UC-RES-023<br>
**来源需求：** REQ-127<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／极高<br>
**参与角色：** 研发/审批人<br>
**业务场景：** 填写公告影响范围<br>
**来源标识：** 原始归纳

### 业务目标

规范产品和版本范围格式，校验同类公告重叠并阻止不合规提交。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-126,REQ-018。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 研发进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-126,REQ-018）。
1. 发起人完善技术公告内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后技术公告生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-231：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-232：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-233：** “影响版本规范与重叠校验”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-231：** Given 研发具备权限且前置条件满足，When 完成“影响版本规范与重叠校验”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-232：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-233：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-024 技术公告分产品线会签

**用例编号：** UC-RES-024<br>
**来源需求：** REQ-128<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／极高<br>
**参与角色：** 产品线审批人/研发<br>
**业务场景：** 公告涉及多个产品线<br>
**来源标识：** 原始归纳

### 业务目标

生成产品线子模块并签，支持指派、转单、驳回和整体发布。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-126,REQ-006。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 产品线审批人进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-126,REQ-006）。
1. 发起人完善技术公告内容并提交，系统冻结本次提交版本。
1. 系统按流程模板、项目类型、金额/风险等级和公司、部门及岗位关系计算审批路径并生成待办。
1. 审批人执行通过、驳回、退回、转办或加签；系统记录意见和节点时效。
1. 审批通过后技术公告生效并触发后续业务；未通过时恢复为可修订状态。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 审批超时或人员失效：按配置提醒、升级或转交，不自动越权通过。

### 状态流转

草稿 → 已提交 → 审批中 → 已通过/已驳回/已撤回/已终止。

### 业务规则

- **BR-RES-241：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-242：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-243：** “技术公告分产品线会签”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 流程数据：流程实例、节点、审批人、意见、动作、到达时间和完成时间。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-241：** Given 产品线审批人具备权限且前置条件满足，When 完成“技术公告分产品线会签”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-242：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-243：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-025 技术公告检索

**用例编号：** UC-RES-025<br>
**来源需求：** REQ-129<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 技术支持/项目人员<br>
**业务场景：** 按设备和问题查找风险<br>
**来源标识：** 原始归纳

### 业务目标

按编号、主题、类型、状态、设备、版本、问题单、bug和割接场景组合检索。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-126,REQ-127。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 技术支持进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-126,REQ-127）。
1. 用户设置公司、区域、项目层级、状态、时间或业务属性等查询条件。
1. 系统按数据权限执行查询和聚合，并明确统计口径、数据时间点和过滤条件。
1. 用户查看列表、树、指标或明细钻取；汇总结果可追溯到原始业务记录。
1. 用户按权限导出；大数据量导出转为异步任务并记录下载审计。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 聚合超时：转为异步计算并返回任务编号，不返回不完整且无标识的结果。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-251：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-252：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-253：** “技术公告检索”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-251：** Given 技术支持具备权限且前置条件满足，When 完成“技术公告检索”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-252：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-253：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-026 公告命中项目与设备

**用例编号：** UC-RES-026<br>
**来源需求：** REQ-130<br>
**所属版本：** V2<br>
**优先级／复杂度：** P1／极高<br>
**参与角色：** 产品线审批人/服务经理<br>
**业务场景：** 公告发布后筛选受影响对象<br>
**来源标识：** 原始归纳

### 业务目标

根据客户、设备型号和版本识别命中项目设备，并生成预警实施单。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-127,REQ-017,REQ-014。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 产品线审批人进入“技术公告”功能，并选择对应项目或项目。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-127,REQ-017,REQ-014）。
1. 用户选择模板、文件或业务数据，系统校验文件类型、大小、版本和项目关联。
1. 系统生成或保存新版本，记录来源、创建人、时间、摘要和文件校验值。
1. 用户预览、下载、提交评审或归档；系统按状态控制编辑、替换和删除。
1. 系统更新关联业务记录、交付物完整性和审计日志。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持项目原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-261：** 项目只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-262：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-263：** “公告命中项目与设备”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 项目标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 资产数据：设备/备件编号、序列号、型号、版本、位置、归属客户和维保状态。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的项目、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-261：** Given 产品线审批人具备权限且前置条件满足，When 完成“公告命中项目与设备”正常操作，Then 系统应保存正确结果、更新项目及关联状态并记录审计。
- **AC-RES-262：** Given 用户无数据权限或项目状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-263：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-027 公告治理工单与优先级

**用例编号：** UC-RES-027<br>
**来源需求：** REQ-131<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 一线/服务经理<br>
**业务场景：** 跟踪公告处置<br>
**来源标识：** 原始归纳

### 业务目标

记录默认及办事处调整的治理优先级、计划、反馈、挂起、完成和验证结果。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-130,REQ-107。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 一线进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-130,REQ-107）。
1. 用户创建或选择技术公告，录入“公告治理工单与优先级”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新技术公告、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-271：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-272：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-273：** “公告治理工单与优先级”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 时间数据：计划开始/结束、实际开始/结束、时区、时效规则和超期原因。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-271：** Given 一线具备权限且前置条件满足，When 完成“公告治理工单与优先级”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-272：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-273：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-028 公告与ITR双向关联

**用例编号：** UC-RES-028<br>
**来源需求：** REQ-132<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／高<br>
**参与角色：** 技术支持<br>
**业务场景：** 问题处理形成公告或公告处置形成问题<br>
**来源标识：** 原始归纳

### 业务目标

关联首发问题单、Bug和ITR处置状态，支持技术公告系统内闭环。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-109,REQ-126,REQ-009。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 技术支持进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-109,REQ-126,REQ-009）。
1. 用户创建或选择技术公告，录入“公告与ITR双向关联”所需业务数据和附件。
1. 系统执行必填、格式、唯一性、关联关系、状态和业务规则校验。
1. 用户保存或提交，系统更新技术公告、关联对象、待办和操作日志。
1. 用户可按权限查看、修改、停用或归档；已生效数据的关键变更需要保留版本。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-281：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-282：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-283：** “公告与ITR双向关联”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-281：** Given 技术支持具备权限且前置条件满足，When 完成“公告与ITR双向关联”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-282：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-283：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

## FR-RES-029 技术公告处置统计

**用例编号：** UC-RES-029<br>
**来源需求：** REQ-133<br>
**所属版本：** V2<br>
**优先级／复杂度：** P2／中<br>
**参与角色：** 管理层<br>
**业务场景：** 监控公告治理进度<br>
**来源标识：** 原始归纳

### 业务目标

统计命中、计划、未处置、完成、超期和不同优先级治理情况。

### 前置条件

- 用户已登录且具有“技术公告”相应功能权限和项目数据权限。
- 依赖条件：REQ-131。
- 所属项目、客户、公司和必要字典数据处于有效状态。

### 主流程

1. 管理层进入“技术公告”功能，并选择对应项目或技术公告。
1. 系统校验用户权限、项目数据范围、当前状态及依赖条件（REQ-131）。
1. 用户设置公司、区域、项目层级、状态、时间或业务属性等查询条件。
1. 系统按数据权限执行查询和聚合，并明确统计口径、数据时间点和过滤条件。
1. 用户查看列表、树、指标或明细钻取；汇总结果可追溯到原始业务记录。
1. 用户按权限导出；大数据量导出转为异步任务并记录下载审计。

### 分支与异常

- 权限不足：拒绝操作，不返回无权限对象的敏感信息，并记录安全审计。
- 状态不允许：保持技术公告原状态，返回明确的门禁条件和可执行动作。
- 并发冲突：检测版本号变化，拒绝静默覆盖并提示刷新或执行冲突合并。
- 聚合超时：转为异步计算并返回任务编号，不返回不完整且无标识的结果。

### 状态流转

该功能不单独定义状态时，沿用所属业务对象状态机和版本控制规则。

### 业务规则

- **BR-RES-291：** 技术公告只能由具备功能权限且处于数据授权范围内的用户操作。
- **BR-RES-292：** 关键字段或状态发生变化时必须记录变更前值、变更后值、操作者、时间和原因。
- **BR-RES-293：** “技术公告处置统计”必须满足依赖条件和当前状态门禁，不得通过前端绕过。

### 数据要求

- 技术公告标识：全局唯一，创建后不可复用。
- 项目关联：除平台主数据外，业务记录必须关联项目或明确记录无项目原因。
- 状态与版本：保存当前状态、版本号、生效时间和最后修改信息。
- 时间数据：计划开始/结束、实际开始/结束、时区、时效规则和超期原因。

### 权限、通知与审计

- 功能权限控制入口和动作；数据权限按公司、区域、项目层级、项目成员和任务归属共同计算。
- 影响责任人、审批人、阶段门禁或后续工作的操作应生成待办或通知，并执行事件去重。
- 创建、修改、状态变化、审批、导入导出和文件操作必须记录操作人、时间、来源、业务键和前后值。

### 输出与后置条件

- 输出：更新后的技术公告、关联记录、待办/通知以及审计日志。
- 后置条件：业务数据满足当前状态完整性要求；失败时不得残留无法识别的中间状态。

### 验收标准

- **AC-RES-291：** Given 管理层具备权限且前置条件满足，When 完成“技术公告处置统计”正常操作，Then 系统应保存正确结果、更新技术公告及关联状态并记录审计。
- **AC-RES-292：** Given 用户无数据权限或技术公告状态不允许，When 发起同一操作，Then 系统应拒绝且不得产生部分业务数据。
- **AC-RES-293：** Given 同一请求重复提交或发生并发修改，When 系统处理请求，Then 应保证幂等或明确报告版本冲突，不得静默覆盖。

# 4. V3演进范围

### FR-RES-020 备件库存告警与预测

- **来源：** REQ-124
- **演进目标：** 提供无库存及替代型号告警、库龄和中转次数监控、命中率与周转率统计。
- **数据前提：** V1/V2相关业务数据完整、可追溯，规则版本和结果反馈可用于评估。
- **控制边界：** 智能结论必须显示依据、置信度和适用范围，不能自动绕过审批、权限或状态机。
- **人工责任：** 业务责任人必须确认后才能产生正式业务影响。
- **当前承诺：** 仅作为V3探索范围，不纳入V1/V2开发验收。

# 5. 领域验收门禁

- 本领域V1、V2功能均已映射至来源需求和验收编号。
- 所有状态变化、权限拒绝、并发冲突和重复提交均具有测试证据。
- 跨模块写入只通过应用服务、API或事件完成，不直接访问其他模块业务表。
- 所有【待确认】项在对应功能进入开发前形成书面决策。
