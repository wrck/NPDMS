{
  "schemaVersion": 1,
  "featureId": "F-CUT-009",
  "status": "REVIEW_REQUIRED",
  "requirementSlices": ["CUT-03@V2=FULL"],
  "ownerContext": "CUT",
  "extends": ["F-CUT-001-physical-contract.json", "F-CUT-003-physical-contract.json"],
  "ownerTables": {
    "cut_cutover_configuration_revision": {
      "requiredColumn": "navigation_rule_snapshot JSON NULL without default; currently absent from executable migrations and DO, add through the actual next Flyway version",
      "lifecycle": "editable only in DRAFT; copied with revision; immutable in PUBLISHED/DISABLED",
      "jsonContract": "JSON null or exact object {target}; target is CURRENT_STAGE_WORKBENCH or TASK_OVERVIEW",
      "nullMeaning": "CURRENT_STAGE_WORKBENCH; never arbitrary jump",
      "migration": "no new table and no historical backfill; add the nullable column forward and wire DO/request/response mapping"
    },
    "cut_cutover_checklist": {
      "usage": "read requested immutable/versioned checklist identity and status for export",
      "changes": "NONE"
    },
    "cut_cutover_checklist_item": {
      "usage": "read authorized applicable item projection for export",
      "changes": "NONE"
    },
    "cut_cutover_checklist_item_result": {
      "usage": "read only the authorized current selected result for export",
      "changes": "NONE"
    },
    "cut_task": {
      "usage": "read task/project/frozen configuration identity and authoritative current stage",
      "changes": "NONE; submit transition remains SURVEYING -> PLAN_DRAFTING"
    }
  },
  "export": {
    "businessTablesReadOnly": true,
    "auditOwner": "PlatformCommandExecution/operation audit without changing CUT aggregate version",
    "allowedItemTypes": ["BUSINESS_SURVEY", "RISK"],
    "forbiddenItemTypes": ["DUAL_MACHINE_CHECK"],
    "workbookContract": "F-CUT-009-api-contract.json AuthorizedChecklistWorkbook and AuthorizedAnswerDisplay; raw answerSnapshot is exportable only for DIRECT and must be forced to XLSX text",
    "stableOrder": ["sheet order BUSINESS_SURVEY,RISK", "item sort_order with null as 0", "stable_item_key"],
    "forbiddenData": ["device credentials", "PLT internal ids", "file bytes from CUT tables", "external raw response", "unselected result history", "fields absent from the authorized detail projection"]
  },
  "navigation": {
    "decisionSource": "task-frozen configuration_revision_id and immutable navigation_rule_snapshot",
    "evaluationPoint": "after all existing checklist submit guards and business writes succeed; returned only when that transaction commits",
    "stateIsolation": "the decision cannot insert/update cut_task, stage history, checklist or result rows",
    "targetContract": ["CURRENT_STAGE_WORKBENCH", "TASK_OVERVIEW"],
    "ruleShape": "one unconditional exact object {target} or JSON null; no condition, priority, list, action or state field",
    "historicalFallback": "null or absent historical rule deterministically produces CURRENT_STAGE_WORKBENCH",
    "stateWrite": "FORBIDDEN"
  },
  "dependencies": {
    "F-CUT-001": "configuration revision lifecycle; navigation_rule_snapshot is an SDS-approved F-CUT-009 forward column, not an already executable carrier",
    "F-CUT-003": "checklist facts, authorized detail projection and submit state transition",
    "PROJ": "ProjectScopeApi ACTION_VIEW reserved consumer contract",
    "PLT": "not used for export artifacts; existing checklist evidence references remain governed by F-CUT-003"
  },
  "controlledPositiveLoop": "CUT tests may assemble a deterministic PROJ scope substitute to prove export and post-submit navigation orchestration; production Owner facts and real browser completion remain separate gates"
}
